# 결정 사항

> 완료되어 더 이상 참고할 필요 없는 진행 로그는 정리했습니다(2026-08-16). 아래는 계속 유효한 결정·제약·참조 정보만 남긴 것입니다.

## 화면 ↔ 참고문서 매핑

참고문서(`참고문서\우리집식당_통합본_v0.5.md`) §10에 번호가 있는 화면과 실제 폴더 대응:

| 화면 | 폴더 |
|---|---|
| 01 가구 설정(온보딩) | `1-3. 온보딩-가구프로필`, `1-4. 온보딩-가족구성원` |
| 02 식사 만들기 | `3-2. 식사 만들기` |
| 03 홈-카드목록(개설자뷰) + 07 카드 단위 역할뷰 | `3-1. 내 식탁`(2026-08-16 이전 이름 `3-1. 홈-카드목록`, 한 화면에 결합, `?as=` 쿼리로 시점 전환) — 하단 탭 "홈"은 이제 별도 화면 `3-0. 홈`(역할별 액션 없는 공유 요약)이고, `3-1. 내 식탁`은 거기서 한 단계 더 들어간 개인화 화면 |
| 04 참여자 응답 | `3-3. 참여자 응답` (`?name=` 쿼리로 인사말 개인화) |
| 05 확정 전 요약 | `3-4. 확정 전 요약` |
| 06 오늘의 식탁 완성 | `3-5. 오늘의 식탁 완성` (유일한 크림/피치 감성 톤) |
| 08 홈-달력뷰 | `4-1. 홈 달력뷰` |
| 09 그날 상세 | `4-2. 그날 상세` |
| 10 홈 빈 상태 | `3-7. 홈 빈 상태` |
| 11 응답 변경 알림 | `3-6. 응답 변경 알림` |
| 12 월 요약 통계 | `4-3. 월 요약 통계` |

참고문서에 번호는 없지만 MoSCoW Must 근거로 추가 제작한 화면: `3-8. 참여자 링크 공유`(§7.1 1인1링크), `3-9. 무가입 응답 연결`(§7.6 B방식 가입 병합), `5-1. 가구 설정`, `5-2. 식사 기록`, `5-3. 설정`. 로그인은 `2-1.로그인 화면`/`2-2. 로그인`.

전체 화면은 `0. 확인용뷰어/code.html`에서 페르소나 트랙별로 순서대로 열어볼 수 있음.

## 계속 유효한 스코프 제약

- **"가족 코드로 참여하기" 기능 없음**: 참고문서 §7의 참여 모델은 "1인 1고유 링크"이지 코드 입력 참여가 아님. §13 미결 질문 5(가구 소유권·복수 관리자)가 확정되기 전엔 임의로 코드 방식을 만들지 않음. `100. 가족코드로 참여하기추가필요` 폴더는 참고용으로만 남겨두고 플로우엔 연결하지 않음.
- **MVP 범위 밖으로 확정 제외**: 비용 정산/예산/송금(§16 "3차+ 확장"), 가구 테마 색상·위치 입력(참고문서 화면01 스펙에 없음), 캘린더의 아침/점심/저녁 고정 슬롯(§0.5 "끼니 미고정" 철학과 상충 — 달력은 시간축 방식으로만).
- **디자인 시스템은 "Warm Table"로 고정**: 각 폴더 `DESIGN.md` 참조(Stone Grey 배경+흰 카드+Peach 감성 배경, Noto Serif/Plus Jakarta Sans, `rounded-xl`). GPT/Manus 참고자료의 비주얼은 기능만 참고하고 그대로 쓰지 않음.
- **`design-taste-frontend` 스킬은 이 프로젝트에 안 맞음**: 스킬 자체가 §13에서 "멀티스텝 폼·촘촘한 프로덕트 UI엔 부적합"이라 명시. 요청받아도 랜딩페이지 패턴(히어로·마퀴·GSAP 스크롤)은 가져오지 말 것 — 범용 위생 규칙(엠대시 금지 등)만 참고.
- **§13 미결 질문(가구 소유권·복수 관리자 등)이 확정되기 전엔 그 이상 화면을 확장하지 않음** — 근거 없는 기능 추가 방지.

## 확인용뷰어의 4개 페르소나

`0. 확인용뷰어`에서 관리자/참여자 흐름을 걸어보기 위해 만든 4명(참고문서 §6.2, §7.6 근거):

1. **김영희** — 가입한 개설자(관리자). 로그인~기록 전체 루프.
2. **박철수** — 이미 가입한 가족. `3-1?as=박철수`에서 자기 시점 홈(자신이 만든 식사=host 카드, 김영희 식사=guest 카드로 전환)을 봄.
3. **최유진** — 가입하지 않은 손님. §6.2대로 화면이 `3-3` 응답 1개뿐(홈 없음).
4. **김하준** — 가입 안 했지만 가입하고 싶은 가족. `3-3` 응답 완료 후 가입 유도 → `3-9. 무가입 응답 연결`(§7.6 B방식) → 연결 후 `3-1?as=김하준`.

**주의**: 페르소나 3(손님)은 처음에 "이지은"으로 뒀다가 "최유진"으로 바꿨습니다. 이지은은 온보딩 단계부터 이미 가구 구성원(`household_members`)으로 등록되어 프로젝트 전역(3-1 roster, 3-2/3-8 기본 참여자, 5-1 구성원 목록 등 10곳 이상)에 쓰이고 있어서, 같은 이름을 "가입 안 한 손님"(`guests` 테이블, §12에서 household_members와 별개)으로도 쓰면 데이터 모델이 충돌합니다. 구성원 목록의 이지은은 그대로 두고, 손님 페르소나만 `3-3`의 `PERSONAS` 객체와 뷰어 트랙에서 최유진으로 바꿨습니다.

## 의도적으로 남겨둔 자리표시자(alert)

실제 백엔드/SDK 연동이 필요해 정적 목업 범위 밖으로 남김: 가구 초대 링크 생성, 카카오톡 실공유, `3-2`의 "저장된 메뉴" 하드코딩 3개.

## 참고: 재조사 불필요

`3-4`(확정 전 요약)는 `3-2`(식사 만들기)의 실제 입력과 무관한 하드코딩 샘플을 보여주지만, 실제 이동 경로상 `3-2` → `3-4` 링크 자체가 없어(`3-2`는 항상 `3-8`로 감) 사용자가 그 불일치를 볼 수 있는 경로가 없음. 문제 아님, 수정 불필요.

## MoSCoW 잔여 항목 마저 제작 (2026-08-16)

- **배경**: "멈추라고 할 때까지 계속 만들어달라"는 요청으로, PRD §15 MoSCoW에서 아직 화면이 없던 항목을 마저 제작.
- **신규 폴더**:
  - `6-1. 오류·복구 상태` — GPT자료가 갖고 있던 유일한 강점(에러 12종)인데 지금까지 실제 화면이 하나도 없었음. 참고문서엔 에러 설계가 없어 전적으로 GPT자료 기반. 각 화면 안에서 조건부로 떠야 할 상태라 파일 하나에 12개를 모은 참고 시트로 제작(카드마다 "붙는 화면" 표기).
  - `6-2. 알림 센터` — §9 "앱 푸시"(Must)가 `5-3` 토글 하나로만 존재하고 목록 화면이 없었음. `3-1` 헤더에 종 아이콘 추가해 연결.
  - `5-4. 요일별 기본 응답` — §15 Could. `5-1`에서 진입.
- **기존 화면에 인라인으로 추가한 기능** (새 파일 없이, ponytail 원칙):
  - `3-4`: "확인 안 됨" 인원에 **미응답 재공유**(Should)·**개설자 대리 입력**(Should, §7.4) 버튼 추가.
  - `3-3`: 완료 화면에 **응답 수정하기** 버튼 추가(Should, 폼으로 되돌아감).
  - `3-5`: **확정 취소하고 다시 열기** 버튼 추가(Should, `3-4`로 되돌아감).
  - `3-2`: **반복 일정**(Could) 접힘 섹션 추가(체크박스만, 로직은 붙이지 않음).
  - `5-2`: 메뉴 검색창 추가(Could "고급 기록 검색"의 최소 버전).
- **의도적으로 만들지 않은 것**: 메뉴 추천 칩(이미 `3-2`의 "저장된 메뉴에서 담기"가 사실상 같은 역할), 가구 소유권 이양·복수 관리자(§13 미결, 확정 전 확장 금지 원칙 유지).
- **전체 링크 재검증**: 총 25개 화면, 쿼리스트링 포함 전수 재검사 — **깨진 링크 0건**. 엠대시(`—`) 사용 여부도 신규 파일 전체에서 재검사 — 0건.

## AYoom(개인 냉장고 인식 앱) 기능 접목 (2026-08-16)

- **배경**: 사용자가 별도로 만든 `Yoomnoom/AI-food-recognition_VC_26.07`(Flask + OpenAI Vision API + Supabase, 실제 동작하는 백엔드)의 화면을 보여주며 우리집식당에 접목 요청. 범위를 물어봤고 **"메뉴 추천 + 가구 냉장고 재고 연동까지"**로 확정(원본의 개인 재고관리·절약통계·개인 레시피 저장·별도 5탭 내비게이션은 가져오지 않음 — 참고문서 §1.2 "가족이 번갈아 식사를 제안·조율·기록하는 앱" 정체성과 충돌).
- **AYoom은 실제 AI 연동 앱**(OpenAI Vision API 확인됨). 다만 우리집식당은 정적 목업이라 이번에 만든 화면들은 실제 API를 호출하지 않고 로딩 애니메이션 + 하드코딩 결과로 흉내만 냄. 실제 연동 시엔 원본 저장소의 Flask 엔드포인트를 그대로 재사용 가능(같은 사용자 소유).
- **신규 폴더**:
  - `7-1. 우리집 냉장고` — 가구 공유 재고 목록(신선/곧소비/오늘먹어야함 3단 요약). `localStorage['household_fridge']`.
  - `7-2. 재료 사진 분석` — 업로드→분석중(체크리스트 애니메이션)→결과, 한 파일 안에서 상태 전환(`3-3` 패턴 재사용). 결과 확인 후 냉장고에 누적.
  - `7-3. 메뉴 추천` — 냉장고 재료와 레시피 4종(감자볶음/오믈렛/된장찌개/크림파스타) 필요재료의 교집합으로 매치율을 **그 자리에서 계산**(AYoom처럼 고정 숫자 하드코딩 아님). 선택 시 `3-2`로 돌아가 대표 메뉴 자동 반영.
- **기존 화면 연결**: `3-2`의 메뉴 섹션에 "냉장고 재료로 추천받기" 버튼 추가(`recipe_pick` localStorage로 `3-2`와 연결). 홈 계열 화면(`3-1`,`3-7`,`4-1`)의 하단 탭에 "냉장고" 4번째 탭 추가(홈/냉장고/가구/설정).
- **전체 링크 재검증**: 총 28개 화면 — **깨진 링크 0건**.

## 페르소나 일관성 마무리 + 색상 토큰 자동 검증 (2026-08-16)

- **자동 색상 토큰 검증**: 각 화면이 트림된 tailwind config를 쓰는 경우가 많아, 실수로 정의 안 된 색상 클래스를 쓸 위험이 있었음(이미 2건 실수 발견해 고친 전적). Node 스크립트로 28개 파일 전수 검증(`colors`/`fontSize`/`fontFamily` 설정을 실제 JSON으로 파싱해 클래스 사용과 대조) — **실제 버그 0건** (남은 2개 히트는 JS 문자열 조합 코드를 정규식이 잘못 파싱한 오탐으로 확인).
- **`3-1`→`3-4`→`3-5` 데이터 흐름을 완전히 동적으로 재작성**: 이전엔 `3-1`의 어느 카드에서 "응답 현황 자세히 보기"를 눌러도 `3-4`가 항상 김영희의 "오늘 저녁" 하드코딩 데이터를 보여줬음(박철수 시점으로 그의 식사 카드에서 눌러도 마찬가지). 이제 `3-1`이 클릭한 카드의 실제 roster를 `localStorage['meal_summary']`로 넘기고, `3-4`가 그걸 받아 그룹(함께 먹어요/늦게 와요/확인 안 됨)·대리입력·미응답재공유를 전부 동적으로 렌더링. 직접 링크로 들어오면 기존 샘플로 폴백.
- **`3-3`(참여자 응답 수정) → `6-2`(알림 센터) 연결**: 참여자가 처음 응답과 다른 값으로 수정 제출하면 `localStorage['notifications']`에 실제 알림이 쌓이고 `6-2`가 이를 하드코딩 샘플과 함께 보여줌 — §10 근거11 "확정 전 변경도 알림"이 실제로 동작.
- **`3-5`의 "확정 취소하고 다시 열기"도 연결**: 이전엔 되돌아가도 `3-4`가 기본 샘플로 리셋됐음. 이제 `confirmed_meal`을 `meal_summary` 형태로 변환해 넘겨서, 실제로 확정했던 내용 그대로 다시 열림.
- **`4-1`(달력)의 날짜 클릭도 정정**: 이전엔 어떤 날짜를 눌러도 항상 "8월 16일" 하드코딩 화면으로 갔음. 이제 식사가 있는 날짜만 클릭 가능하고, 클릭한 실제 날짜가 `4-2` 헤더에 표시됨(내부 샘플 식사 2건은 그대로 — 모든 날짜에 대한 실제 데이터까지는 만들지 않음, 과한 범위라 판단).

## 스코프 확장 결정: 로드맵 항목(밀키트·사진기록·예산·정산)을 실제로 제작 시작 (2026-08-16)

- **결정 변경**: 지금까지 "정산/사진기록/밀키트는 §16 3차+ 확장이라 만들지 않는다"는 원칙을 여러 차례 지켜왔는데, 사용자가 명시적으로 "로드맵 항목도 진짜 만들기 시작"하기로 확정. 이건 이전 결정을 뒤집는 게 아니라 **사용자가 의식적으로 범위를 확장**한 것 — 이후 화면들은 더 이상 "준비 중" 텍스트로 막아두지 않고 실제 목업으로 만듦.
- **제작 순서**: 참고문서 §16 로드맵 단계(1차 밀키트 제휴 → 2차 사진기록 → 3차 식비예산 → 4차 정산)를 그대로 따름 — PRD 스스로도 이 순서가 이유가 있다고 명시함(공개 범위·리스크가 단계별로 커짐, §13 미결 질문7).
- **주의**: 4차(정산)는 PRD §13에서 "가족 돈 문제 관계 리스크"로 명시적으로 플래그된 항목 — 실제 결제 연동 없이 UI/계산 로직만 목업으로 구현.

### 4개 로드맵 화면 완성

- **`8-1. 밀키트 주문`**(1차): `7-3. 메뉴 추천`(재료 부족 레시피, 구체적 재료 전달)과 `3-1` 홈 배너(패턴 기반, 일반 진입) 두 곳에서 진입. 배송 날짜·시간 선택 + 주문 완료 상태까지 구현(실제 결제 없음).
- **`8-2. 식사 사진 기록`**(2차): `3-5.오늘의 식탁 완성`에 "사진 남기기 (선택)" **보조** 버튼으로 연결(§0.5 "사진은 2차, 강제 아님" 철학 유지 — 기본 흐름 "홈으로"를 가리지 않음). 저장한 사진은 `5-2. 식사 기록` 목록에 썸네일로 노출.
- **`8-3. 식비 예산`**(3차): `4-3. 월 요약 통계`에서 진입. `5-2`에서 각 기록에 입력한 비용(`meal_costs`)을 합산해 월 예산 대비 진행률 표시. §13 미결 질문7이 아직 미확정이라 가구 합산 금액만 보여주고 개인별 분리는 안 함.
- **`8-4. 정산`**(4차): `5-2`에서 비용이 입력된 기록에만 "정산하기" 버튼이 뜸. 참여자 수만큼 균등 분담해 각자 정산완료/미정산 토글만 제공 — 실제 송금·결제 연동 없음(§13 리스크 반영, 최소 범위로 제한).
- 신규 4파일 + 연결된 기존 5파일(`3-1`,`3-5`,`4-3`,`5-2`,`7-3`) JS 문법·링크·색상 전체 재검증 통과.

## 달력을 하단 탭으로 승격 + 미완성 상태로 남아있던 기능 2개 마무리 (2026-08-16)

- **달력 위치 변경**: 사용자 지적대로, 홈 헤더의 작은 토글 버튼으로 숨겨져 있던 달력을 하단 탭 4번째(홈·달력·냉장고·더보기)로 승격. `3-1`,`3-7`,`4-1`,`7-1`,`5-5` 전부 동일 구조로 통일. `3-1` 헤더의 토글 버튼은 제거(간결해짐).
- **이전에 계획만 하고 마무리 못한 것 2개 완성** (Manus자료 재검토 중 발견 — "괜찮았던 부분" 논의 때 계획했다가 삼지창/정산 논의로 넘어가며 누락됨):
  - **`3-2`에 "저장된 손님에서 선택" 추가**: `5-1`에 저장된 손님 목록을 칩으로 보여주고 탭하면 바로 참여자로 추가. 지금까지는 매번 새로 타이핑해야 했음.
  - **"수집 중인 식사 수정" 플로우 완성**: `3-1`의 "방금 만든 식사" 카드에 "수정하기" 버튼 추가 → `3-2`를 편집 모드로 열어(`?edit=1`) 기존 값 프리필 → 저장 시 **참여자 응답 링크는 그대로 유지**하며 내용만 갱신(새 링크 재발행 없음, Manus자료의 "수정 저장·링크 유지" 패턴 반영) → 홈으로 돌아오면 토스트로 확인.
- JS 문법·링크·색상 전체 재검증 통과.

## 밀키트 등 수익화 지점 재배치 (2026-08-16)

- **오판단 정정**: 이전에 "확정된 식사 뒤에만 제안"(§16)을 "이미 확정된 이 식사에 붙이라"로 잘못 해석해서 `3-5.오늘의 식탁 완성`(메뉴가 이미 정해진 화면)에 밀키트 카드를 넣었었음. 사용자가 "메뉴 이미 정했는데 밀키트 사서 뭐하냐"고 정확히 지적. 실제 §16/§2의 의도는 **누적된 확정 기록(패턴)을 근거로 제안하라**는 뜻이지, 방금 확정한 그 식사에 붙이라는 게 아니었음.
- **수정**: `3-5`에서 밀키트 카드 제거(관련 토스트 코드도 정리).
- **재배치**: 사용자가 제안한 "먹고싶어요 순간"과 "메인 홈" 두 곳으로 이동.
  - `7-3. 메뉴 추천`: 추천 레시피의 보유 재료 매치율이 100% 미만(재료 부족)일 때만 "부족한 재료 N개, 밀키트로 준비하기 (준비 중)" 버튼 노출 — 뭘 먹을지 정하는 바로 그 순간, 재료가 부족하다는 구체적 이유와 함께 제안.
  - `3-1. 홈-카드목록`: 인사말 아래 "이번 주 집밥이 벌써 3번째예요 · 밀키트 정기배송으로 장보기를 줄여보세요 (준비 중)" 배너 — §2 "N요일 집밥 빈도 → 밀키트 제안" 패턴 통계 예시를 그대로 반영.
- JS 문법(스크립트 블록별) 검증 + 전체 링크·색상 재검증 통과.

## `3-2` 식사 만들기: 종류 → 메뉴 순서 재정렬 + 종류별 도구 분기 (2026-08-16)

- **배경**: 사용자가 배달 선택 화면 스크린샷을 보여주며, 배달인데 위쪽 "메뉴" 섹션(냉장고 재료 추천, 저장된 메뉴 담기)을 그대로 채우게 하는 게 상식적으로 이상하다고 지적. "종류"를 먼저 정하는 게 자연스러운 순서.
- **수정**:
  1. 섹션 순서를 **언제 → 종류 → 메뉴 → 누구에게**로 변경(기존: 언제→메뉴→종류→누구에게).
  2. "냉장고 재료로 추천받기"·"저장된 메뉴에서 담기"는 **집밥 선택 시에만 노출**(`#homeOnlyMenuTools`, `selectMealType()`에서 토글) — 외식/배달엔 냉장고 재료 개념 자체가 안 맞아서 숨김.
  3. 메뉴 입력창 placeholder도 종류별로 변경: 집밥 "메뉴를 입력하세요(예: 김치찌개)" / 외식 "식당·메뉴를 입력하세요(예: OO파스타집)" / 배달 "메뉴를 입력하세요(예: 후라이드 치킨)".
- JS 문법 검증 + 전체 링크 재검증 통과.

## 하단 탭 정리 + 실제 버그 수정 (2026-08-16)

- **배경**: 사용자가 스크린샷으로 두 가지를 지적. (1) 하단 탭이 홈/냉장고/가구/설정 4개인데 "꼭 필요한 것과 아닌 것"으로 안 나뉘어 있다("삼지창" 개념 재요청). (2) 헤더가 360px(Galaxy S)에서 "우리집식당" 제목이 두 줄로 깨짐. (3) 확정 워크플로우에 밀키트 판매 같은 미래 수익화 지점이 안 보임.
- **실제 버그 발견 및 수정**: 스크린샷에서 홈 카드의 구성원 로스터가 "박철수(나)·이지은(나)·김하준(나)"처럼 나 아닌 사람 전원에게 "(나)"가 붙고 정작 보고 있는 김영희 본인에게는 안 붙는 게 보였음 — `greetSuffix()` 함수의 조건이 뒤집혀 있던 실제 로직 버그. 함수를 없애고 `isMe ? ' (나)' : ''`로 직접 수정.
- **하단 탭 재구성**: 홈/냉장고/가구/설정(4개) → **홈·냉장고**(자주 쓰는 핵심) + **더보기**(가구 설정·식사 기록·앱 설정을 모은 신규 허브 `5-5. 더보기`) 3개로 축소. `3-1`,`3-7`,`4-1`,`7-1` 전부 동일하게 적용.
- **헤더 정리**: `3-1`의 별도 프로필 아바타 버튼 제거(가구 설정 접근은 이제 "더보기"로 일원화) — 헤더 여백 확보로 360px 제목 줄바꿈 버그 해결.
- **밀키트 등 수익화 지점 추가**: 참고문서 §16 "확정된 식사 뒤에만 제안" 원칙에 맞춰, `3-5.오늘의 식탁 완성`(확정 직후 화면)에 "필요한 재료, 밀키트로 받아볼까요? (준비 중)" 카드 추가. 실제 기능은 아니고 향후 수익화 지점을 워크플로우 안에 시각적으로 표시.
- **전체 링크·색상 재검증**: 통과.

## 사용성 개선: 전체 페이지 이동 → 인라인 오버레이 (2026-08-16)

- **배경**: 사용자가 "알림처럼 확인하고 닫기만 하면 되는 걸 전체 페이지 이동으로 처리한 게 너무 많다"고 지적. 알림·달력을 예로 듦.
- **알림 벨(`3-1`)**: `6-2. 알림 센터`로 전체 이동하던 것을 **슬라이드인 패널**(배경 딤 처리+오른쪽에서 등장+X 닫기, `2-1.로그인 화면`의 햄버거 메뉴 드로어와 같은 패턴)로 교체. `6-2` 폴더 자체는 독립 참고 화면으로 남겨둠(뷰어에서 계속 볼 수 있음), 실제 사용 흐름만 패널로 대체.
- **응답 변경 알림(`3-6`) 상세도 같은 패널 안에서 인라인으로 확인**: 목록에서 눌러도 페이지 이동 없이 패널 안에서 상세(전/후 비교+확인/전화 버튼)로 전환되고 "목록으로"로 돌아감. `3-6` 폴더도 독립 참고 화면으로 유지, 실제 흐름에선 더 이상 링크 안 함.
- **달력(`4-1`) 날짜 클릭**: `4-2. 그날 상세`로 전체 이동하던 것을 **하단 바텀시트**(배경 딤+아래에서 올라옴+X 닫기)로 교체. `4-2` 폴더도 독립 참고 화면으로 유지.
- **브라우저 기본 `alert()` 전부 제거**: 5개 파일(`3-2`,`3-3`,`3-4`,`3-8`,`5-1`)의 정보성 알림 6건을 Warm Table 톤에 맞춘 **토스트**(하단 중앙, 어두운 pill, 자동 사라짐)로 교체. `confirm()`(로그아웃, 확정 취소 등 Y/N 결정)은 그대로 유지 — 알림이 아니라 실제 선택이 필요해서.
- **점검 중 새로 발견한 버그**: `4-1`의 바텀시트에 정의 안 된 색상 토큰(`secondary-container`)을 썼다가 자동 검증 스크립트로 바로 잡음.
- **패턴 원칙(다음에도 적용)**: 화면 전환이 "다른 섹션으로 이동"이면 전체 네비게이션이 맞고(홈↔가구↔설정↔냉장고), "잠깐 보고 확인/닫기"면 오버레이(패널·바텀시트·토스트)가 맞음. 이 프로젝트에 남은 화면 중 이 기준에 걸리는 게 더 있으면 계속 찾아서 고칠 것.
  - **예외로 수정됨(2026-08-16, 아래 항목 참고)**: 달력의 바텀시트는 사용자가 "달력사이나 하단에 내용이 적혀도 충분하지않나"라고 직접 지적해 **인라인 패널로 되돌림**. 오버레이 원칙이 항상 정답은 아니고, 노인 사용자 등 "레이어가 나타났다 사라지는 것" 자체를 낯설어하는 사용자를 고려하면 상시 노출형 인라인이 더 나을 수 있다는 걸 확인함 — 다음에 오버레이 vs 인라인을 고를 때 이 사례를 기준으로 판단할 것.

## 직관성 진단 + 달력 3뷰 재구현 + "더보기" 대시보드화 (2026-08-16)

- **배경**: 사용자가 4가지를 한 번에 요청 — (1) 달력에 오늘/주/월 각각 실제로 동작하는 화면, (2) 달력 상세를 팝업 대신 인라인으로, (3) 토스·뱅크샐러드처럼 기능이 많아도 "모이는 장소"가 있어야 링크 타고 들어가는 불편이 해소된다는 지적, (4) 20~30대뿐 아니라 60~70대 노인도 쓸 수 있어야 한다는 전제로 직관성 진단.
- **`USABILITY_AUDIT.md` 최상단에 "직관성 진단" 섹션 추가**: 직관적인 것/떨어지는 것 6가지 원인(모이는 장소 부재, "더보기"의 무정보성, 숨겨진 레이어의 등장·소멸, 장식뿐인 탭, 아이콘 단독 사용, 핵심 정보를 보려면 항상 들어가야 함)과 해결 방향을 기록. 전제는 60~70대 기준으로 판단(작은 아이콘·숨겨진 레이어·다단계 탐색에 취약).
- **`5-5. 더보기`를 플랫 3버튼 목록 → 4개 카테고리 대시보드로 재구성**: 가구 / 기록·통계 / 우리집 살림 / 설정. 지금까지 링크가 없어 파묻혀 있던 `5-4`(요일별 기본 응답)·`4-3`(월 요약 통계)·`8-3`(식비 예산)·`8-1`(밀키트 주문)에 처음으로 진입 경로가 생김. 버튼 라벨도 "설정"처럼 무정보하지 않게 "가구 설정 · 구성원 · 저장된 손님"처럼 내용이 드러나게 변경.
- **`4-1. 홈 달력뷰` 전면 재작성**:
  - 월/주/오늘 탭이 실제로 다른 뷰를 렌더링(이전엔 장식만 있고 onclick 없었음): 월=기존 6주 그리드, 주=오늘이 속한 주(일~토) 7칸을 더 크게, 오늘=그리드 없이 오늘 하루만.
  - **바텀시트(`dayBackdrop`/`daySheet`, `translateY` 슬라이드 애니메이션) 완전 제거** → 달력 바로 아래 항상 그 자리에 있는 인라인 `#dayDetail` 영역으로 교체(마스터-디테일 구조, 레이어 등장·소멸 없음). 날짜를 누르면 그 자리 내용만 갱신.
  - 부수 수정: 이전엔 모든 날짜 클릭이 동일한 하드코딩 `DAY_MEALS` 2건을 보여줬음 → `MEALS_BY_DATE`로 날짜별 실제 데이터 분리(16일=2건, 17/20/25일=각기 다른 메뉴), 월 그리드의 점 밑 요약 텍스트도 같은 데이터에서 파생(중복 데이터 제거).
  - 헤더의 목록/달력 토글 아이콘 제거(이미 하단 탭 4번째로 진입 경로 일원화됐으므로 헤더에 남아있던 건 잔재였음).
- **`0. 확인용뷰어` 라벨 동기화**: "홈 · 달력뷰 (👉 바텀시트)" → "(👉 월/주/오늘 탭 + 인라인 패널)"로, "더보기 (하단 탭 3번째)" → "(하단 탭 4번째, 카테고리별 대시보드)"로 수정(실제 구조와 안 맞던 텍스트 정정).
- JS 문법(스크립트 블록별)·링크·색상 토큰 재검증 통과.

## 확인용뷰어 자체 개선: 접기/펼치기 + 묶음 흐름보기 (2026-08-16)

- **배경**: 뷰어(화면들 자체가 아니라 검토 도구)의 사이드바가 항상 전체 펼침 상태라 화면을 많이 차지해 불편하다는 지적, 그리고 "로그인"처럼 화면 2개가 한 묶음인 경우 가로로 나란히 이어서(와이어프레임처럼) 볼 수 있었으면 좋겠다는 요청.
- **사이드바 접기/펼치기**: 페르소나 선택 영역("가족")과 각 스텝 그룹("로그인"·"온보딩"·"홈"·"핵심루프" 등, 트랙마다 다름)이 각각 개별적으로 접고 펼 수 있는 아코디언이 됨 + 상단에 "전체 접기/펼치기" 마스터 토글 1개 추가. 트랙을 바꾸면 접힘 상태는 초기화(트랙마다 그룹 구성이 다르므로). 현재 보고 있는 화면이 속한 그룹은 이동 시 자동으로 펼쳐짐(찾다가 숨어서 안 보이는 걸 방지).
- **가로 묶음 흐름보기**: 상단바 아래 새 "묶음으로 보기" 가로 스트립 추가 — 현재 트랙의 그룹들이 알약 버튼으로 나열되고, 누르면 그 그룹에 속한 모든 화면이 가로로 나란히(화살표로 연결된 와이어프레임 형태) 동시에 렌더링됨. 각 화면 밑에 "이 화면만 크게 보기" 링크로 기존 단일 뷰로 즉시 전환 가능. 같은 묶음을 다시 누르면 단일 뷰로 복귀.
- 순수 이 뷰어 파일(`0. 확인용뷰어/code.html`)만의 변경이며, 28개 실제 화면 파일은 손대지 않음. JS 문법 재검증 통과.

## 철학 정합성 진단 (2026-08-16) — 색은 오진단, 나머지는 유지

- **배경**: 사용자가 "이 프로젝트 메인이 뭐라고 생각하냐, 위치·디자인이 철학 기반으로 결정돼야 하는 거 아니냐"고 질문(답을 알려주지 않고 스스로 찾으라는 의도). 요소별로 뜯어본 전체 내용은 `PHILOSOPHY_AUDIT.md` 참고.
- **색 진단은 틀렸음 — 사용자가 정정**: "Warm Table" 무채색 M3 그레이 팔레트는 §11의 "라벤더/파스텔"과 다르지만, 이건 이탈이 아니라 **의도된 결정**이었다. 사용자 확인: "라벤더 색인건 괜찮지만 너무 ai스럽고 촌스러워서 다 뺀거야." → 34개 파일에 시도했던 라벤더/노랑/주황 재매핑은 전부 원복(`revert_to_grey.js`). **앞으로 색은 무채색 M3 그레이가 확정 방향** — PRD §11 문구를 문자 그대로 다시 시도하지 말 것. `2-1`의 옛 테라코타 텍스트/블롭만 그레이 계열(`text-on-background`/`bg-primary`)로 정리해 다른 화면과 통일 — 이건 색 방향 문제가 아니라 "혼자만 다른 임의색을 쓰지 말자"는 일관성 문제라 원복 대상에서 제외.
- **색과 무관하게 유지된 수정**: `5-1`의 "관리자/구성원" 위계 라벨 제거(§0.5 "위계를 만들지 않는다"), `8-1` 밀키트 화면의 자체 주문·배송 체크아웃 제거하고 "제안+제휴사 연결"로 축소(§0.5 "주문 시스템을 거부한다", §15 Won't), `8-4` 정산에 공개범위 고지 문구 추가(§13-7 미결 사안을 숨기지 않도록), `3-1` 카드 정보 순서를 메뉴 우선으로 재정렬(§5.1), `3-1`의 "이번 달 식비"(금액) 위젯을 "이번 달 N번 함께 먹었어요"(횟수)로 교체(§2 "MVP는 횟수까지, 금액은 확장").
- `3-7`을 감정 화면(크림 톤)으로 바꿨던 것도 색 되돌림과 함께 원복(`bg-surface` 무채색으로 복귀) — "완성·빈 상태는 감정 화면"이라는 PRD 문구 자체는 맞지만, 무채색이 확정 방향인 이상 이걸 어떻게 표현할지는 재검토가 필요해 이번엔 손대지 않음.
- 전 파일(34개) JS 문법·링크(141개) 재검증 통과.

## 홈 화면 정보 순서 재정비 + "먹고싶어요" 기능 신설 + 뷰어 묶음보기 정정 (2026-08-16)

- **배경**: 사용자가 실제 렌더링된 3-1 스크린샷을 보고 4가지 지적. (1) "오늘 내가 알아야 할 건 누가 먹는지 아니야?"인데 통계 위젯·밀키트 배너가 그 위를 가림. (2) "가족들의 먹고싶어요는 어디 갔지?" — 참여자가 먼저 메뉴를 요청하는 경로가 아예 없었음(코드 전체에 `먹고싶어요` 관련 기능 0건, PRD에도 없음 — §7.3 "한 줄 요청"은 이미 만들어진 식사에 대한 응답 메모일 뿐 사전 요청이 아님). (3) "홈은 가족 모두가 들어와서 확인·네비게이션하는 공간이어야 하고, 꼭 필요한 게 다 있어야 한다"는 원칙에 비춰 이 앱이 안 맞아 보인다는 지적. (4) 방금 만든 "묶음으로 보기"가 오히려 흐름을 더 끊어 보이게 함.
- **`3-1` 홈 순서 재배치**: 인사말 → **오늘/내일 카드(로스터 포함)** → 가족이 먹고 싶어해요 → 방금 만든 식사 → (맨 아래로 내림) 이번 달 통계 위젯 · 밀키트 배너. "오늘 당장 알아야 할 것"이 최상단에 오도록, 참고용 정보는 스크롤 하단으로 이동.
- **"가족이 먹고 싶어해요" 신설**: `localStorage['menu_requests']`(배열, {name, menu})로 가족 누구나(역할 고정 없이, §5) "오늘 뭐 먹고 싶어요?" 요청을 남길 수 있고, 목록의 "이걸로 만들기 →"를 누르면 `recipe_pick`(기존 7-3 메뉴추천과 동일한 핸드오프 방식)으로 `3-2`에 대표 메뉴가 미리 채워진 채 열림. 새 파일 없이 `3-1`에 인라인으로 추가.
- **홈 구조 원칙에 대한 판단**: "관리자 전용 기능(가구 설정 등)은 홈 밖(더보기)에 있어야 한다"는 지적은 이미 지켜지고 있음(5-5 더보기로 분리 완료). 반면 "무가입 참여자도 홈이 있어야 하는가"는 §6.2가 "무가입 참여자는 홈 없이 개인 링크로 단일 식사만"이라고 명시적으로 못박은 부분이라, 이번 라운드에서 그 규칙 자체는 바꾸지 않음 — 바꾸려면 3-3/3-9/뷰어 페르소나 등 여러 화면에 걸친 변경이라 별도로 다뤄야 함. 오늘 실행한 건 "홈 안의 정보 순서"까지만.
- **확인용뷰어 "묶음으로 보기" 정정**: 모든 그룹을 화살표로 이었더니, 실제로는 순서가 없는 묶음(가구·기록, 로드맵 등)까지 마치 1→2→3 흐름인 것처럼 보여 오히려 헷갈렸다는 지적이 맞음. `SEQUENTIAL_GROUPS`(온보딩·로그인·핵심루프·냉장고)만 화살표+번호+"흐름" 표기, 나머지는 화살표 없는 줄바꿈 그리드+"모아보기(순서 없음)" 표기로 분리. `핵심루프`도 실제 순서(식사만들기→링크공유→확정전요약→완성)만 남기고, 순서가 아닌 예외 상태(응답변경알림, 홈빈상태)는 `핵심루프 예외 상태`라는 별도 그룹으로 분리.
- 전 파일(34개) JS 문법·링크(142개) 재검증 통과.

## "홈"과 "내 식탁" 분리 (2026-08-16)

- **배경**: 사용자가 직전 지적(3번)을 더 밀어붙여 "홈은 별도로 만들고, 지금 보이는 홈은 다른 이름으로 쓰자"고 명시적으로 요청. 기존 `3-1`은 로그인한 한 사람의 역할 기반 카드 목록(호스트 액션·확정 버튼 포함)이라, "가족 모두가 로그인 여부 상관없이 들어와서 확인하는 공유 공간"이 아니라 사실 "관리자/개인이 필요한 다음 단계"에 가까웠다.
- **신규 `3-0. 홈`**: 역할별 액션이 전혀 없는 중립 화면. 오늘/내일 식사를 "메뉴·시간·함께 N명·확인 안 됨 M명"만 보여주는 요약 카드로(먹어요/안먹어요·확정하기 버튼 없음), 누르면 무조건 `3-1. 내 식탁`으로 이동해 그 사람 역할에 맞는 상세를 봄. "가족이 먹고 싶어해요" 보드도 개인 화면이 아니라 이 공유 화면으로 옮김(누구나 남기고 봐야 하는 정보라서). `+식사 만들기` FAB는 유지(생성은 특정 역할 전용이 아니라 §5상 누구나 할 수 있는 행동).
- **`3-1. 홈-카드목록` → `3-1. 내 식탁`로 개명**: 폴더명 변경 + 내부 타이틀/라벨 수정, "가족이 먹고 싶어해요" 섹션은 제거(3-0으로 이전), 하단 탭의 "홈" 버튼을 비활성 상태로 바꾸고 `3-0`으로 연결(자기 자신이 더 이상 "홈"이 아니므로).
- **하단 탭 "홈"의 의미가 전부 바뀜**: 모든 화면(4-1, 5-5, 7-1, 3-7)의 하단 탭 "홈" 버튼이 이제 `3-1. 내 식탁`이 아니라 `3-0. 홈`으로 연결됨. 반면 액션 완료 후 "홈으로" 버튼들(`3-2` 저장 후, `3-3`/`3-5`/`3-8`/`3-9` 완료 후, `5-1`/`5-2`/`5-3`/`6-2`/`3-6`/`8-1`/`8-2`의 뒤로가기 등)은 결과를 실제로 보여주는 `3-1. 내 식탁`으로 그대로 유지 — "하단 탭 홈"과 "방금 한 일의 결과를 보여주는 화면"은 다른 목적이라 다른 곳으로 보냄.
- **로그인·온보딩 완료 후 랜딩**도 `3-0. 홈`으로 변경(로그인/가입 후 가장 먼저 봐야 할 건 개인화된 카드가 아니라 우리집 전체 현황이라고 판단).
- **확인용뷰어**: 김영희/박철수/김하준 트랙에 `3-0. 홈` 스텝을 `3-1. 내 식탁`보다 먼저 추가해서 새 구조가 실제로 보이게 함. 최유진(무가입 손님)은 원래대로 홈 자체가 없음(§6.2 그대로 유지, 이번에 안 건드림).
- 전 파일(35개, 3-0 신규 포함) JS 문법·링크(156개) 재검증 통과.
- **정정(같은 날)**: "내 식탁"을 홈 안에서 탭해서 들어가야만 하는 구조로 만들었더니, 사용자가 "내비게이션에 내 식탁이 있으면 되잖아"라고 더 단순한 답을 제시. "관리자 기능은 홈 다음 영역"이라는 원칙을 "탭 트리 깊이를 늘려라"로 과하게 해석했던 것 — 그냥 하단 탭에 나란히 두면 되는 문제였음. **하단 탭을 홈/내 식탁/달력/냉장고/더보기 5개로 확정**(아이콘: `table_restaurant`). 6개 화면(3-0,3-1,3-7,4-1,5-5,7-1) 전부 반영, 링크(160개) 재검증 통과.

## `3-0. 홈`에 아고다식 타일 그리드 추가 (2026-08-16, Mobbin/아고다 참고)

- **배경**: Mobbin 연동은 이 계정에서 유료 플랜이 필요해 막혀 있었음(`mobbin.com/pricing`). 대신 사용자가 아고다 홈 화면 스크린샷을 직접 공유 — 큰 타일 2개(Hotels+Homes, Flights) → 중간 타일 3개(Flight+Hotel, Activities, Homes only) → 작은 아이콘 줄(Attractions, Airport Transfer, Car Rentals, eSIM, Trains) 순으로, 기능이 많아도 전부 홈 한 화면에서 크기별로 처리되게 해둔 구조였음. "더보기에 넣은 것도 이렇게 홈에 넣을 수 있잖아"라는 지적.
- **`3-0. 홈`에 3단 타일 그리드 추가** (오늘/내일 카드 바로 아래, "가족이 먹고 싶어해요" 위): 큰 타일 2개(달력, 우리집 냉장고) → 중간 타일 3개(식사 기록, 월 요약 통계, 식비 예산) → 작은 아이콘 줄 4개(가구 설정, 요일별 응답, 밀키트, 설정). `5-5. 더보기`에 있던 8개 항목 전부가 이제 홈에서 탭 한 번으로 바로 닿음.
- **색은 그대로 무채색 유지**: 아고다 타일은 분홍·보라·파랑 등 원색 그라데이션이지만, 우리 팔레트는 무채색 그레이가 의도된 결정(별도 메모리 참고)이라 색은 새로 안 만들고 이미 정의된 톤(`tertiary-container`, `secondary-container`, `surface-container-lowest`)만 재사용해 큰/중간/작은 타일의 시각적 위계만 크기·배경 유무로 표현.
- ~~하단 탭·`5-5. 더보기` 자체는 안 건드림~~ → **정정됨**(아래 "홈과 더보기 중복 해소" 항목 참고). 이건 진짜 중복이었고 그대로 두면 안 됐음.
- 전 파일 JS 문법·링크(169개) 재검증 통과.
- **정정(순서, 같은 날)**: 타일 그리드를 오늘/내일 카드 "아래"에 뒀었는데, 사용자가 "홈이잖아 홈 — 타일이 위에 있어도 충분하지 않냐, 내 식탁에서 어차피 한 번 더 보여줄 텐데"라고 지적. `내 식탁`이 이미 있어서 식사 현황이 중복 노출되니, 3-0에서까지 그걸 최우선으로 지킬 필요는 없다는 것. **타일 그리드(큰 2·중간 3·아이콘 4)를 최상단으로, 오늘/내일 카드를 그 아래로 순서 변경**.

## 확인용뷰어 순서 정리 + 묶음보기에 실제 분기 경로 추가 (2026-08-16)

- **순서 정리**: 김영희 트랙의 그룹 순서가 뒤죽박죽이었음(예: "참고 전용" 그룹이 "홈" 바로 다음에 끼어 있어서 사이드바에서 실제 흐름보다 먼저 나타남 — `orderedGroups()`가 그룹의 "첫 등장 위치" 기준으로 순서를 매기기 때문). 로그인→온보딩→홈→핵심루프→핵심루프 예외 상태→냉장고→달력→가구·기록→로드맵(신규)→참고 전용→참고자료 순으로, 실제 처음 쓰는 사람이 밟을 법한 순서대로 재배열.
- **"묶음으로 보기"에 실제 분기·중복 경로 추가**: 지금까지는 한 그룹 안의 화면들을 무조건 일직선으로만 이어 보여줬는데, 실제 코드에는 그룹 경계를 넘나드는 진짜 분기가 있음(예: `3-2`의 "냉장고 재료로 추천받기"→`7-3`, `3-5`의 "확정 취소하고 다시 열기"→`3-4`로 되돌아감, `3-5`의 "사진 남기기"→`8-2`, `6-2`의 리마인드 알림→`3-4`로 진입). `FLOW_BRANCHES` 데이터(그룹명→분기 목록)를 추가해서, 흐름 보기 화면 아래에 점선으로 구분된 "실제 흐름에서 여기와 겹치는/갈라지는 경로" 줄을 추가로 렌더링(작은 미리보기 + "출발 화면 ↳ 실제 버튼 문구 → 도착 화면"). `핵심루프`와 `냉장고` 그룹에 각각 실제 확인된 분기를 채워둠 — 지어낸 연결이 아니라 화면을 만들며 실제로 넣은 버튼들만 반영.
- 전 파일 JS 문법·링크(175개) 재검증 통과.

## 뒤로가기 버그 점검 (2026-08-16)

- **배경**: 사용자가 "중복으로 보여지거나 뒤로가기를 했을 때 홈으로 이동하지 않거나 하는 버그가 있는 것 같다"고 확인 요청.
- **점검한 것**: 중복 렌더링(id 중복 전수 검사 — 0건), 하단 탭 활성 상태 중복(전수 검사 — 6개 화면 전부 정확히 1개만 활성), `5-5. 더보기` 항목 중복(전수 검사 — 중복 없음), `3-0`/`3-1`의 init·렌더 함수 중복 호출 여부 — 전부 이상 없음.
- **실제로 찾은 버그**: `5-1`(가구 설정)·`5-2`(식사 기록)·`5-3`(설정)·`5-4`(요일별 기본 응답)·`8-1`(밀키트 주문)·`8-3`(식비 예산) 6개 화면의 "뒤로" 버튼이 전부 옛 구조 기준 타겟(`3-1. 내 식탁` 또는 다른 화면)으로 하드코딩돼 있었음. 오늘 이 6개 화면이 `3-0. 홈`의 타일 그리드에서 바로 진입 가능한 항목이 됐는데, 뒤로 버튼은 그 사실을 모른 채 예전 경로로만 돌아갔던 것 — "홈 타일→화면→뒤로"를 누르면 홈이 아니라 엉뚱한 화면(`내 식탁` 등)으로 감. 6개 전부 `../3-0. 홈/code.html`로 수정.
- **손대지 않은 것**: `3-2`(식사 만들기)의 "뒤로"는 여전히 `3-1`을 가리킴 — `3-0`·`3-1` 양쪽에 다 "+" FAB이 있어 완전히 명확하진 않지만, 저장 후 결과를 보여주는 화면이 `3-1`이라 그쪽을 기본값으로 유지(수정 모드 복귀 등 기존 관계가 더 강함). `8-4`(정산)→`5-2`, `7-2`→`7-1`, `7-3`→`3-2`, `4-3`의 "달력으로"→`4-1`은 진입 경로가 안 바뀐 화면들이라 그대로 둠.
- 전 파일 JS 문법·링크(175개) 재검증 통과.

## "홈"과 "더보기" 중복 해소 — 더보기를 하단 탭에서 빼고 설정으로 교체 (2026-08-16)

- **배경**: 사용자가 "홈과 더보기는 중복인데, 이건 왜 체크 안 해?"라고 지적. 정확한 지적 — 아고다 참고해서 `3-0. 홈`에 타일 그리드를 추가하면서 `5-5. 더보기`가 갖고 있던 8개 항목을 전부 그대로 홈에도 얹었는데, 그 결과 두 화면이 똑같은 목록을 보여주는 진짜 중복이 됐다. 지난번엔 이걸 "안전한 추가형 변경"이라고 넘겼는데(위 202번째 줄 기록), 아고다의 실제 패턴을 다시 보면 "More" 탭은 홈 타일과 안 겹치는 것(계정·로그아웃·설정)만 들어있지, 이미 타일로 나온 기능을 또 담고 있지 않음 — 그 원칙을 놓쳤던 것.
- **조치**: `5-3. 설정`(알림·계정·로그아웃 — 유일하게 홈 타일에 없던 내용)을 하단 탭 5번째 자리로 승격(뒤로가기 헤더 → 로고 헤더+하단 탭 구조로 변경). 6개 화면(`3-0`,`3-1`,`3-7`,`4-1`,`7-1`, 그리고 `5-3` 자신)의 하단 탭에서 "더보기"(`5-5`)를 "설정"(`5-3`)으로 교체.
- **`5-5. 더보기`는 참고 전용으로 전환**: 하단 탭에서 완전히 빠짐(라이브 플로우에서 unlink). 파일 자체는 삭제하지 않고 남겨둠 — 이전에도 `3-6`/`4-2`/`6-1`/`6-2`처럼 "예전 버전" 참고 파일을 지우지 않고 뷰어에서만 보이게 남겨온 것과 같은 패턴.
- 전 파일 JS 문법·링크(178개) 재검증 통과.

## 반복 식사 예약 + 미리 답해두기 — 쌍방향 소통 실제 구현 (2026-08-16)

- **배경**: 사용자 질문 두 가지. (1) "밥먹을거냐고 묻는 행위를 예약할 수 있을까? 매번 보내기 하기보다 물어야 하는 날은 예약으로 응답하게." (2) "묻기 전에 가족이 먹어/안먹어 답을 언제 어떻게 할 수 있을까? 쌍방향 소통을 어떻게 풀어야 좋을까?" 둘 다 §15 Could 티어("요일별 기본값", "반복 일정")와 맞닿아 있고, `3-2`엔 이미 "반복 일정" 체크박스가 있었지만 로직이 없었음(이전 결정: "체크박스만, 로직은 붙이지 않음").
- **(1) 반복 식사 예약 — `3-2. 식사 만들기`**: "매주 같은 요일에 반복" 체크 시 요일 다중선택 칩(일~토)이 실제로 나타나고, 저장하면 `localStorage['recurring_meals']`에 규칙(요일·시간·종류·메뉴·참여자)이 쌓임. `3-2` 안에 "반복 중인 식사" 목록도 추가해 기존 규칙을 보고 삭제할 수 있음. **`3-1. 내 식탁`이 열릴 때마다 오늘 요일이 규칙에 해당하고 아직 안 열었으면 자동으로 오늘치 식사를 `created_meals`에 만들어줌**(하루 1회, `recurring_fired`로 중복 방지) — 호스트가 매번 "만들기"를 누르지 않아도 그 요일엔 알아서 열리는 것. 자동 생성된 카드는 "🔁 반복 일정으로 자동 생성" 표시로 구분.
- **(2) 미리 답해두기 — `3-0. 홈`**: "가족이 먹고 싶어해요" 바로 아래에 "미리 답해두기" 섹션 신설. 오늘부터 5일치 날짜 칩이 뜨고, 각 날짜에 먹어요/안먹어요를 미리 눌러둘 수 있음(`localStorage['advance_answers']`, 역할 구분 없이 가족 누구나). **`3-2`에서 그 날짜로 식사를 만들면 자동으로 반영**: 미리 "안 먹어요"라고 답한 사람은 참여자 체크가 자동 해제되고("✓ 미리 안 먹는다고 했어요 · 안 물어봐도 돼요" 표시), "먹어요"라고 답한 사람은 체크 유지하고 표시만 됨 — 호스트가 그 사람에게 굳이 응답 링크를 또 보낼 필요가 없어짐.
- **두 기능이 서로 맞물리는 지점**: 반복 규칙으로 자동 생성된 식사도 참여자 목록은 규칙 저장 시점 기준이라, 그 사이 누군가 미리 답을 바꿨다면 반영이 안 됨(자동 생성은 `3-2`를 거치지 않고 바로 `created_meals`에 꽂히므로) — 이 부분은 자동 생성 시에도 `advance_answers`를 한 번 더 대조하는 후속 작업으로 남김.
- 전 파일 JS 문법·링크(178개) 재검증 통과.

## 외식·배달 장소 추천 + 메뉴 투표(가족에게 골라달라고 하기) (2026-08-16)

- **배경**: 사용자 질문 두 가지. (1) "외식·배달은 식당 API나 신상·단골 식당이 등록·추천돼야 하지 않냐." (2) "정하는 사람이 오늘 뭐 먹을지 고민되면 가족에게 골라달라고 보낼 수도 있지 않냐, 어떻게 만들면 좋을까."
- **(1) 자주 가는 곳 · 요즘 뜨는 곳 — `3-2. 식사 만들기`**: 종류를 외식/배달로 고르면(`eatoutMenuTools`), 집밥의 "저장된 메뉴에서 담기"와 같은 자리에 "자주 가는 곳"(`localStorage['saved_places']`, 식당 이름 넣고 식사를 만들 때마다 자동 저장·최근 8개)과 "요즘 뜨는 곳"(참고용 추천 목록, 실제 API 연동이 아니라 하드코딩 예시임을 라벨로 명시)이 뜸. 실제 외식 장소 API 연동은 정적 목업 범위 밖이라 하지 않고, `7-2`/`7-3`(AI 재료 인식)때와 같은 방식으로 "실제 연동이라면 이렇게 보일 것"만 흉내냄 — 진짜처럼 속이지 않으려고 "참고용 추천"이라고 라벨을 붙임.
- **(2) 메뉴 투표(뭐 먹을지 고민될 때 가족에게 골라달라고 하기)**: `3-2`에서 후보 메뉴를 2개 이상 태그로 넣고 "뭐 먹을지 고민되면, 가족에게 골라달라고 하기"를 누르면 `localStorage['menu_poll']`(후보 목록 + 투표)로 저장되고 메뉴 입력은 비워짐(호스트가 아직 못 정했다는 뜻). **`3-0. 홈`에 투표가 있으면 "오늘 뭐 먹을지 골라주세요" 카드가 뜨고 가족 누구나 눌러서 투표**(역할 무관, `CURRENT_PERSONA` 기준 1인 1표, 다시 누르면 바꿀 수 있음). 호스트는 `3-2`로 돌아오면 "진행 중인 투표" 목록에서 실시간 표수를 보고 "확정하기"를 눌러 그 메뉴로 최종 결정 → 이후 참여자 선택·생성은 기존 흐름 그대로.
- **§15 Won't("주문 시스템 등") 원칙 재확인**: 두 기능 다 "무엇을 고를지 돕는" 선택 보조 도구일 뿐, 실제 예약·주문·결제 플로우는 만들지 않음(밀키트 때 저질렀던 실수를 반복하지 않도록 의식적으로 범위를 제한).
- 전 파일 JS 문법·링크(178개) 재검증 통과.

## 응답 워크플로우 중복 제거 + 대표 메뉴 변경 버튼 (2026-08-16)

- **배경**: 사용자가 스크린샷(박철수님이 만든 내일 식사 카드)을 보여주며 두 가지 지적. (1) "내가 먹어요/안먹어요를 바로 눌렀으면 그냥 결정되면 되지, 왜 3-3(참여자 응답)으로 가서 같은 질문을 다시 해야 하냐 — 클릭이 너무 많다." (2) "대표 메뉴의 대표는 내가 바꿀 수 있어야 하는 거 아니냐 — 사용성이 떨어진다."
- **(1) 원인**: `3-1`의 게스트 카드에서 "먹어요/안먹어요"를 누르면 실제로는 아무것도 결정하지 않고 `3-3`으로 리다이렉트만 시켰음 — `3-3`은 §7.2 "메뉴 확인→먹어요/안먹어요→도착시간→완료"라는 자기 완결적 흐름이라, 이미 카드에서 답한 걸 처음부터 다시 물었던 것. 로그인한 가족(이미 홈에 있는 사람)에게는 명백한 중복이고, `3-3`은 원래 비로그인 손님이 개인 링크로 처음 들어올 때를 위한 화면(§6.2)인데 이미 로그인한 사람도 거길 거쳐야 했던 게 문제.
- **조치**: `3-1`의 먹어요/안먹어요 버튼이 그 자리에서 바로 `localStorage['meal_responses']`에 기록하고 카드가 즉시 "이미 응답했어요" 상태로 바뀜(§5.1 "탭 1~2번"). 도착시간을 더 자세히 남기고 싶을 때만 쓰라고 "바꾸기" 링크로 `3-3`을 여전히 열어둠 — 강제 경유가 아니라 선택적 상세 조정으로 격하. 부수 발견: 기존 코드가 `status==='attending'`일 때만 "이미 응답함" 상태를 보여줬고 `absent`(안 먹어요로 이미 답함)일 땐 매번 버튼을 다시 보여주는 별도 버그도 있었음 — 같이 고침.
- **(2) 대표 메뉴 변경**: PRD상 "메뉴 순서를 드래그하면 대표가 바뀐다"고 돼 있었지만, 실제 구현이 HTML5 드래그&드롭(`draggable`+dragstart/dragover/drop) 기반이라 **터치 화면에서는 사실상 동작하지 않음**(데스크톱 마우스 전용 API) — 모바일 우선 앱에서 핵심 기능이 조용히 고장나 있던 셈. `3-2`의 각 메뉴 태그에 "대표로" 버튼을 추가해 탭 한 번으로 그 메뉴를 대표(0번)로 옮길 수 있게 함 — 드래그는 데스크톱 보너스로 남기고, 버튼을 기본 수단으로 승격.
- 전 파일 JS 문법·링크(179개) 재검증 통과.

## 포니테일 감사 + 죽은 파일 아카이브 (2026-08-16)

- **배경**: 사용자가 "포니테일 기준으로 지금까지 만든 화면들을 평가해달라"고 요청 — 실제 서비스로 갈 수 있는 흐름인지 냉정하게. 전체 감사 내용은 `PONYTAIL_AUDIT.md` 참고. 인터뷰 결과: (1) v1 스코프는 상세 카탈로그를 보고 사용자가 직접 정하기로 함, (2) 밀키트 제휴 수수료가 핵심 수익모델로 확정, (3) 가구 소유권·복수 관리자 모델을 지금 확정하기로 함, (4) 다음 단계로 실사용검증·죽은파일 정리(삭제 아닌 한곳에 모으기)·PRD 재작성을 요청.
- **죽은 파일 5개를 `_archive/`로 이동**(삭제 아님): `100. 가족코드로 참여하기추가필요`, `3-6. 응답 변경 알림`, `4-2. 그날 상세`, `5-5. 더보기`, `6-2. 알림 센터`. 이동한 파일들 내부의 상대경로(`../`)를 한 단계씩 보정해 서로 간 링크는 유지, 확인용뷰어의 참조 경로도 `../_archive/...`로 갱신하고 전부 "참고 전용 (아카이브 · 실제 흐름 아님)" 그룹으로 통일(`3-6`도 이 그룹으로 재분류 — 예전엔 "핵심루프 예외 상태"에 잘못 남아있었음).
- **아카이브 과정에서 발견한 실제 버그 2건**: (1) `3-3`의 응답 변경 알림이 이제 사라진 `3-6`을 가리키고 있었음 → `3-1. 내 식탁`으로 수정. (2) `3-7`(홈 빈 상태)의 알림벨이 아직도 전체화면 이동으로 `6-2`(예전 버전)를 가리키고 있었음(3-0/3-1은 이미 인라인 패널로 바뀐 지 오래인데 3-7만 안 바뀌어 있었음) → 3-0/3-1과 동일한 인라인 알림 패널로 교체. `6-1`(오류·복구 상태)의 `4-2` 참조 라벨도 현재 실제 흐름(`4-1` 인라인 패널)을 가리키게 수정.
- 전 파일(35개, 아카이브 5개 포함) JS 문법·링크(180개) 재검증 통과.
- **가구 소유권 모델 확정 및 `5-1` 반영**: 오너(가구 처음 만든 사람) + 복수 공동관리자 모델로 결정(자세한 규칙은 `우리집식당_통합본_v0.6.md` §6.4). `5-1. 가구 설정`에 "공동 관리자로 지정/해제"(오너만 보임, 나머지 구성원은 아무 표시 없음 — 위계 강조 안 함) + "가구 삭제(오너만 가능)" 버튼 추가.
- **`FEATURE_CATALOG.md` 신규**: 모든 화면·기능을 완성도/실서비스 전환 필요사항/추천(포함·재검토·확정)으로 정리 — v1 스코프를 사용자가 직접 정할 수 있게 상세 나열.
- **PRD v0.6 신규(`참고문서/우리집식당_통합본_v0.6.md`)**: v0.5는 그대로 보존(역사적 기록), v0.6이 새 단일 진실 소스. 3-0/3-1 분리, §9-신규 4기능, 가구소유권 모델(§6.4), 비즈니스 모델 섹션(§18, 밀키트 제휴 수수료 확정) 반영.

## 홈 타일 그리드 중복 재정리 + 기록·통계 병합 + 정보구조 문서화 (2026-08-16)

- **배경**: 사용자가 홈 화면의 "달력·우리집 냉장고" 큰 타일 스크린샷을 보여주며 "이거 하단 탭에도 있는데 중복 아니냐, 크게 표현할 이유가 있냐"고 지적 — 정확한 지적. 이어서 "식사기록·월요약통계도 사실상 한 묶음 아니냐, 왜 나눴냐"고 추가 지적, 그리고 "홈,식사,기록,가구,알림,설정 / 가구설정-가구정보,요일별기본응답,손님수신동의,정산관리 처럼 명확한 묶음정보가 필요하다, 묶음이 불분명해서 계속 확신이 안 서는 것 같다"는 근본 원인 진단.
- **큰 타일 재구성**: 달력·냉장고는 이미 하단 탭에 있어 제거(아고다 원칙 — 큰 타일은 "하단 탭에 없는 것 중 가장 중요한 것"에만 써야 하는데 그 원칙을 스스로 어겼던 것). 대신 하단 탭에 없는 것 중 가장 중요한 밀키트(수익모델 핵심)·식비예산(재정 확인)을 큰 타일로 승격.
- **`4-3. 월 요약 통계`를 `5-2. 식사 기록`에 흡수**: 통계 요약 카드(총 식사/조리/주문 + 예산 진입)를 `5-2` 상단에 추가하고 `4-3`은 `_archive/`로 이동(내부 상대경로 보정, 뷰어·`3-1`·`3-0` 참조 전부 `5-2`로 갱신). 이제 홈 타일은 "밀키트·예산"(큰) + "식사 기록(통계 포함)"(중간) + "가구 설정·요일별 응답"(아이콘 줄, 설정도 하단 탭에 있어 제거)로 축소.
- **`INFORMATION_ARCHITECTURE.md` 신규**: 전체 앱을 "홈/식사/기록/가구/살림/알림/설정" 7개 최상위 묶음으로 명문화(사용자가 제시한 형식 그대로). 지금까지의 "확신이 안 선다"는 느낌의 실제 원인이 이 트리가 문서로 정리된 적이 없었던 것이라고 보고, 앞으로 화면 위치를 정할 때 이 문서를 기준으로 판단하기로 함.
- **`5-1. 가구 설정`을 4행 허브로 재구성**: 사용자가 직접 제시한 순서 그대로 "① 가구 정보 ② 요일별 기본 응답 ③ 손님·수신 동의 ④ 정산 관리"를 명확한 번호 섹션으로 나눠 배치(기존 콘텐츠는 그대로, 배치·라벨만 명확하게). "식사 기록 보기" 링크는 가구 소속이 아니라 "기록" 묶음 소속이라 제거.
- 전 파일(29개 라이브 + 6개 아카이브, `4-3` 추가) JS 문법·링크(177개) 재검증 통과.

## 프로필(내 사진) 미노출 수정 (2026-08-16)

- **배경**: 사용자 지적 — "로그인에 대한 기록도 안 보이고, 내 프로필 설정까지 했는데 정작 '나'는 어디에도 표현되고 있지 않다." 온보딩(`1-1`,`1-3`,`1-4`)에서 `onboarding_profile_photo`를 실제로 저장하는데, 이걸 읽어서 화면에 그리는 곳이 앱 전체에 단 한 군데도 없었음(유일하게 `3-1`의 init()에서 `photo` 변수로 읽긴 했지만 아무 데도 안 씀 — 죽은 코드). `5-3. 설정`은 휴대폰 번호까지 "010-1234-5678"로 하드코딩돼 있어서, 온보딩 때 입력한 진짜 번호(`onboarding_signup.phone`)를 무시하고 있었음.
- **조치**: (1) `5-3. 설정` 상단에 "내 프로필" 카드 추가(아바타+이름+이메일), `renderProfile()`이 `onboarding_signup`/`onboarding_nickname`/`onboarding_profile_photo`에서 읽어와 채움. 사진 있으면 실제 이미지, 없으면 이름 첫 글자로 폴백. 휴대폰 번호도 하드코딩 제거하고 실제 값(없으면 "등록 안 됨") 표시. (2) `3-1. 내 식탁`의 상단바에 알림벨 옆 아바타 버튼 추가(`profileAvatarBtn`) — 매번 앱을 열 때마다 "나"가 눈에 보이는 지점을 하나 만든 것. 탭하면 `5-3`(설정=계정 화면)으로 이동. init()에서 이미 읽고 있던 `photo` 변수를 실제로 이 버튼에 연결(사진 or 이니셜 폴백). (3) `5-1. 가구 설정`의 구성원 목록에서도 "나(isMe)" 항목은 색상 원 대신 실제 업로드 사진을 보여주도록 수정.
- **"로그인 기록" 요청은 보류**: 별도의 "계정/로그인 이력" 화면을 만드는 게 맞는지는 아직 범위가 불명확 — 우선 "내가 어디에도 안 보인다"는 핵심 문제(아바타 미노출)부터 해결. 로그인 이력 화면이 별도로 필요한지는 다음에 사용자와 확인.
- 전 파일(35개) JS 문법·링크·색상토큰 재검증 통과.

## 홈 화면 핵심가치 우선순위 재배치 (2026-08-16)

- **배경**: 사용자 지적 — "지금 홈은 밀키트 주문하는 앱 같다. 핵심가치를 명확히 홈에서 보여줘야 하고, 부수적인 부분도 사용하기 쉽게 배치돼야 한다." 실제로 확인해보니 직전 개편(홈 타일 그리드 재정리, 위 항목)에서 "타일이 먼저, 오늘/내일 카드는 그 다음"이라고 코드 주석에까지 명시해뒀던 순서 자체가 문제였음 — 이 앱의 핵심 가치는 "매번 카톡으로 안 물어봐도 되는 것"인데, 그 증거인 오늘/내일 식사 현황·미리 답해두기가 헤더 4번째 섹션에야 나오고, 첫 화면엔 밀키트 주문·식비예산 타일부터 보였음. 신규 가입자는 오늘/내일 카드가 비어 있어 사실상 "밀키트 사세요" 광고판만 보게 되는 구조.
- **조치**: `3-0. 홈` 섹션 순서를 통째로 뒤집음 — 헤더 바로 아래에 오늘→내일→(있으면)메뉴투표→가족이먹고싶어요→미리답해두기 순으로 배치(핵심가치 클러스터), 그 아래 구분선(`border-t`) 하나 넣고 그 밑으로 밀키트·식비예산 큰 타일, 식사기록 중간타일, 가구설정·요일별응답 아이콘줄(부수 기능 클러스터)을 내림. 섹션은 전부 id 그대로 옮겨서 기존 렌더 함수(`renderNeutralCard` 등)는 손 안 댐.
- **부제 텍스트를 실제 가치 요약으로 교체**: 기존엔 "가족 모두가 여기서 확인하고, 자세한 건 내 식탁에서 봐요"라는 안내 문구였는데, 이건 "어디 가서 봐"만 알려줄 뿐 아무 정보가 없었음. `computeSubtitle()` 함수를 추가해 오늘 식사의 실제 참여/미확인 인원을 계산해 "오늘 저녁 2명 먹어요 · 2명 아직 확인 안 됨" 식으로 헤더 바로 아래 한 줄로 보여줌(오늘 식사가 없으면 "아직 오늘 식사가 없어요 · 매번 묻지 않아도, 여기서 한눈에 확인해요"로 가치 자체를 문장으로 설명). 첫 화면 첫 줄이 상태 그 자체를 말하게 함.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 홈 상태 요약 — 텍스트 한 줄 → 카드로 (2026-08-17)

- **배경**: 바로 위 개편에서 오늘/내일 카드를 없애고 "오늘 저녁 2명 먹어요 · 2명 아직 확인 안 됨"을 회색 본문 텍스트 한 줄로만 넣었더니, 사용자가 `3-1. 내 식탁`의 실제 식사 카드(굵은 헤드라인 + 그림자 있는 카드) 스크린샷과 나란히 보여주며 "너무 눈에 안 띈다, 2번째 이미지처럼까지는 보여야 한다"고 지적.
- **판단**: 콘텐츠(상태 한 줄)를 다시 늘리는 게 아니라(그러면 중복 지적으로 되돌아감) **시각적 무게감만** `3-1` 카드 수준으로 맞추면 됨 — 문제는 "내용이 부족하다"가 아니라 "회색 작은 글씨라 안 보인다"였음.
- **조치**: 상태 텍스트를 `bg-surface-container-lowest` + 그림자 + 패딩이 있는 카드 안에 `headline-lg`(굵은 글씨, `3-1` 메뉴명과 동일 크기)로 넣고 우측에 `chevron_right` 아이콘 추가, 카드 전체를 `3-1`로 가는 링크로 만듦(`myTableLink` id 유지, 기존 JS 로직 그대로 재사용). 메뉴명·시간·만든 사람 같은 개별 식사 정보는 여전히 안 넣음 — 카드 모양만 빌리고 내용은 한 줄 그대로.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## "미리 답해두기"를 홈에서 달력으로 이동 (2026-08-17)

- **배경**: 사용자가 홈의 "미리 답해두기" 위젯(오늘·내일·8/19·8/20·8/21 5개 카드, 각각 먹어요✓/안먹어요✗ 아이콘 버튼) 스크린샷을 보고 두 가지 지적. (1) "이거 달력에 있어야 하는거 아냐?" (2) "협박같고 보기좋지않은것같아."
- **판단**: 둘 다 맞는 지적. (1) 이 기능은 본질적으로 "날짜 → 답변" 데이터라 `INFORMATION_ARCHITECTURE.md`가 이미 정리해둔 원칙("달력 = 식사 묶음을 날짜순으로 보는 방식")과 정확히 맞음 — 홈에 있을 이유가 없었음. (2) 다섯 개의 카드를 한 줄에 늘어놓고 각각 체크/X 아이콘 버튼 쌍을 반복 노출한 게 "답을 안 하면 안 되는" 체크리스트/설문처럼 보였음 — 정작 이 기능의 목적 자체가 "안 물어봐도 되게" 하려는 건데, UI가 오히려 "5일치를 한 번에 추궁하는" 것처럼 보이는 역설.
- **조치**: `3-0. 홈`에서 "미리 답해두기" 섹션과 관련 함수(`renderAdvanceRow`, `setAdvanceAnswer`, `loadAdvanceAnswers`, `toDateKey`, `DAY_LABELS_KO`)를 전부 제거. 대신 `4-1. 홈 달력뷰`의 그날 상세(인라인) 패널에 통합 — 날짜를 하나씩 선택했을 때, 그 날 계획된 식사가 없고(`selectedDay >= TODAY`, 즉 오늘 이후) 아직 답하지 않았으면 "이 날 식사할 계획이 있으신가요? 미리 답해두면 안 물어봐도 돼요"라는 문장 + 먹어요/안먹어요 텍스트 버튼(아이콘 단독 아님, 톤을 부드럽게), 이미 답했으면 "✓ 미리 답해뒀어요 · 안 먹어요"로 간단히 보여주고 "바꾸기"로만 조정. 한 번에 하나씩만 물어보니 체크리스트 느낌이 사라짐.
- **저장소 키 호환**: `3-2. 식사 만들기`의 `applyAdvanceAnswers()`가 읽는 `localStorage['advance_answers']`(`{name, date, status}`, 날짜는 실제 `YYYY-MM-DD`) 형식을 그대로 재사용 — `4-1`은 데모용 고정 달력(2024년 8월, `TODAY=16`)이라 실제 오늘 날짜와 다를 수 있어서, `selectedRealDateKey()`가 "선택한 날 - TODAY" 차이만큼 실제 오늘에서 이동시켜 진짜 날짜 키를 만들어 다른 화면과 호환되게 함.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## "가족이 먹고싶어요" 목록 개수 제한 (2026-08-17)

- **배경**: 사용자 질문 — "이건 10개면 어떻게 해? 홈에서 모두 보여줄 수는 없을 거 아냐." 확인해보니 `renderMenuRequests()`가 `menu_requests` 배열 전체를 제한 없이 다 그려서, 요청이 많아지면 홈 화면이 한없이 길어지고 그 아래(구분선 + 타일들)가 계속 밀려나는 구조였음 — 방금 "핵심가치를 헤더 가까이"로 고친 것과 정반대 결과가 남.
- **조치**: `REQUEST_PREVIEW_COUNT = 3`으로 기본 3개만 보여주고, 그 이상이면 "+ N개 더보기" 버튼(전체 이동 없이 그 자리에서 펼침, `requestsExpanded` 플래그로 토글) 추가. 펼친 상태에선 "접기"로 다시 줄일 수 있음. 알림 패널을 인라인으로 처리한 것과 같은 패턴(그 자리에서 처리, 페이지 이동 최소화).
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 달력 "그날 상세"를 선택한 주 바로 아래로 (2026-08-17)

- **배경**: 사용자가 달력 월 뷰 스크린샷을 보고 "이건 달력 하단이 아니라 누르는 달력에 바로 아래열에 보여주면 어때?"라고 제안 — 6주치 그리드 전체를 다 지나야 나오는 "그날 상세"가 위쪽 주의 날짜를 눌렀을 때 스크롤이 너무 멀었음. 이어서 실제 원하는 동작을 보여주는 참고 스크린샷(다른 캘린더 앱, 11일을 누르니 그 줄 바로 아래에 "2026-08-11 기록 · 기록이 없어요"가 끼어들고 16일 이후 줄은 그 아래로 이어짐)을 제공.
- **조치**: `calGrid`를 단일 그리드에서 "주(week) 단위 그리드 여러 개를 쌓는 flex-col" 구조로 바꿈. `#dayDetail` 노드는 내용을 복제하지 않고 **하나만 두고 위치를 옮기는 방식**(`placeDayDetail()` — `insertBefore`로 re-parent)으로 구현: 월 뷰에서는 선택한 날짜가 속한 주(week) 바로 다음에, 주 뷰에서는 그 한 줄 다음에, 오늘 뷰에서는 그리드가 없으니 미리 남겨둔 `#dayDetailAnchor` 자리로 이동. 탭을 바꾸거나 다른 날짜를 누를 때마다 자동으로 올바른 위치로 다시 옮겨감.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 달력 "그날 상세" 재배치 버그 수정 + 삽입 줄 디자인 보정 (2026-08-17)

- **배경**: 사용자가 바로 앞의 "주(week) 바로 아래로 상세 패널 삽입" 수정을 실제로 써보고 "작동에 버그가 있는것같아" + "달력에 맞춰서 디자인도 수정해서 넣었어야지"라고 지적.
- **버그 원인**: `renderMonthGrid()`가 매번 `container.innerHTML = ''`로 주(week) 그리드를 다시 그리는데, 직전 렌더에서 `placeDayDetail()`이 `#dayDetail` 노드를 바로 그 `container`(`#calGrid`) 안으로 옮겨놓은 상태였음. 그러니까 **두 번째 날짜 클릭부터**: `innerHTML = ''`가 주 그리드뿐 아니라 그 안에 들어가 있던 `#dayDetail` 노드 자체를 통째로 지워버리고, 이후 `document.getElementById('dayDetail')`가 `null`을 반환해 `placeDayDetail()`이 `TypeError`로 죽는 구조였음 — 즉 날짜를 한 번이라도 바꿔 누르면 상세 패널이 사라지고 그 뒤로 안 나타남. `renderWeekGrid()`도 같은 구조라 동일한 버그.
- **수정**: 두 함수 모두 `container.innerHTML = ''`로 지우기 **직전**에 `placeDayDetail(document.getElementById('dayDetailAnchor'))`를 먼저 호출해 `#dayDetail`을 안전한 자리(anchor)로 빼놓은 다음 지우도록 순서를 바꿈 — 지워질 컨테이너 안에 `#dayDetail`이 들어있는 채로 `innerHTML=''`가 불리는 경우 자체를 없앰.
- **검증**: 최소 가짜 DOM(스크래치패드 `test_placeDayDetail.js`, 프로젝트에는 포함 안 함)으로 연속 두 번 렌더링해도 `#dayDetail`이 파괴되지 않는지 확인 + "수정 전 버전"을 대조군으로 같이 돌려서 실제로 그 버전은 두 번째 호출에서 `TypeError`로 죽는 것까지 재현 — 진단이 맞았음을 기계적으로 확인.
- **디자인 보정**: 삽입되는 `#dayDetail`이 기존엔 `border-t border-outline-variant pt-4`(페이지 맨 아래 섹션 구분선 스타일)였는데, 이제 주 그리드 사이에 끼워지다 보니 배경색 차이가 없어 눈에 잘 안 띄었음 — 참고 스크린샷처럼 `bg-surface-container rounded-xl p-4 my-1`로 바꿔서 흰 날짜 셀들 사이에 확실히 구분되는 "펼쳐진 줄"처럼 보이게 함.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 홈에 앱 정체성 비주얼(원형 식탁 + 가족 좌석) 도입 (2026-08-17)

- **배경**: 사용자가 본인이 만든 다른 앱 4개(공복 관리, 알람, 번역 비교 도구, 로또 생성기) 홈 화면 스크린샷을 보여주며 "앱의 정체성이 바로 보이지 않아?"라고 질문. 공통점 — 각 앱마다 화면 중앙을 차지하는 **크고 단 하나뿐인 비주얼**(원형 공복 타이머, 알람시계 일러스트, 번역 비교 카드 UI, 뽑기 버튼)이 곧 그 앱의 정체성이라 텍스트 없이 0.5초 안에 "뭐 하는 앱인지" 보임. 오늘 계속 손봐온 `우리집식당` 홈은 순서·카드 무게감을 아무리 다듬어도 여전히 "섹션 나열"이지, 이런 단일 비주얼이 없다는 게 근본 문제라고 진단.
- **판단**: 정보 구조(순서·중복 제거)는 이미 여러 차례 다뤘고 그걸로는 이 문제가 안 풀림 — 앱 이름 자체("우리집식당"/"오늘 식탁")가 이미 답을 갖고 있어서, 그 메타포를 실제로 그려서 보여주기로 함. 사용자에게 진행 여부를 확인(AskUserQuestion)한 뒤 "만들어보자"로 확정.
- **구현**: `renderTableHero()` 함수 신규 — 원형 "식탁"(`bg-tertiary-container` 원) 둘레에 오늘 식사 참여자 아바타를 좌석처럼 배치(삼각함수로 12시 방향부터 균등 배치). 상태를 그림으로 구분: 참여(attending) = 아바타 우하단에 체크 배지, 대기(pending) = 점선 테두리, 불참(absent) = 반투명(`opacity-40`). 식탁 중앙에는 오늘의 대표 메뉴(여러 개면 첫 식사 기준) 또는 식사가 없으면 "아직 식사가 없어요" 표시. 여러 '오늘' 식사가 있을 경우 같은 사람이 두 번 앉지 않도록 이름 기준으로 병합(마지막 상태 우선). 전체를 `3-1. 내 식탁`으로 가는 링크로 감싸 탭 가능하게 하고, 기존 `computeSubtitle()`이 채우던 상태 한 줄은 비주얼 바로 아래 캡션으로 유지(화살표 아이콘과 함께) — 텍스트는 보조, 그림이 주인공.
- 전 파일(35개) JS 문법·링크·색상토큰 재검증 통과.

## 식탁 좌석에 상태별 움직임 추가 + 확장 아이디어는 문서로만 (2026-08-17)

- **배경**: 원형 식탁 비주얼에 긍정적 반응("확실히 메인에 가족구성원이 들어오니까 확 눈에띈다") + 후속 제안 — "아바타가 움직이면 직관적이겠다(착석=먹음, 배회=미응답), 안 먹는다도 다르게 보여주면 좋겠고, 말풍선으로 감사인사·먹고싶은 메뉴, 채팅창으로도 쓸 수 있겠다."
- **판단**: 제안 중 "상태를 움직임/위치로 구분"은 CSS만으로 싸게 되는 부분이라 지금 바로 반영. "실제 캐릭터 걷기 애니메이션"과 "채팅창으로 확장"은 스프라이트/애니메이션 라이브러리, 메시지 저장·모더레이션 같은 실제 제품 기능이 필요해서 지금의 "정적 목업 + vanilla JS" 범위를 넘어감 — 만들지 않고 `FEATURE_CATALOG.md`에 "아이디어 단계" 항목으로만 남김(다음에 스코프 논의할 후보).
- **조치(구현한 것)**: `renderTableHero()`에 상태별 시각적 차이 추가 — 확인 안 됨(pending)은 `seat-wander` CSS 애니메이션(±6도로 흔들림, "아직 자리를 못 잡고 서성이는" 느낌)을 줌. 안 먹어요(absent)는 반경을 42%→50%로 넓혀 식탁에서 한 걸음 물러난 것처럼 배치(기존 반투명 처리는 유지). 먹어요(attending)는 그대로 흔들림 없이 식탁에 붙어(반경 42%) 체크 배지로 표시 — "착석하면 먹는다, 서성이면 아직, 물러나면 안 먹는다"는 사용자의 직관을 그대로 반영.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 요일별 기본 응답 — 개인별 저장 + 남의 행 수정 확인 (2026-08-17)

- **배경**: 정보구조를 같이 정리하다가 `5-4. 요일별 기본 응답`을 살펴보니 두 가지 문제. (1) `MEMBERS` 배열이 완전히 하드코딩이라 화면을 새로고침하면 이전에 누른 게 다 날아감 — 유일한 저장 경로는 맨 아래 "저장" 버튼 하나뿐이었고, 그마저도 지금 보고 있는 화면의 `MEMBERS` 전체(모두의 응답)를 통째로 덮어써서 각자 따로 저장한다는 개념 자체가 없었음. (2) 역할 구분이 전혀 없어서 누구나 남의 요일별 응답까지 자유롭게 바꿀 수 있었음.
- **사용자 결정**: "다 같이 수정 가능한 건 유지하되, 남의 행을 수정하려고 하면 '이 사람 응답인데 수정해도 괜찮아요?' 확인은 하게 하고, 각자 자기 응답이 실제로 저장되게(각각저장)" 하기로 함 — 권한을 막지 말고 확인 절차 + 개별 저장으로 처리.
- **조치**: `?as=` 쿼리로 `CURRENT_PERSONA` 판별(다른 화면과 동일 패턴). `loadMembers()`가 `localStorage['weekday_defaults']`에서 저장된 사람만 덮어써서 불러오고(새로 추가된 구성원은 하드코딩 기본값 유지), `persistMember(member)`가 그 사람 한 명만 저장 배열에서 찾아 갱신 — 한 사람의 저장이 다른 사람 데이터를 건드리지 않음. 각 셀 클릭 시 `isMine`이 아니면 `confirm('OOO님의 응답인데 수정해도 괜찮아요?')`을 먼저 띄우고, 확인하면 그 자리에서 바로 `persistMember()` 호출(즉시 저장, 별도 제출 불필요). 내 행은 `bg-surface-container` 배경 + "(나)" 라벨로 구분. 맨 아래 "저장" 버튼은 이제 할 일이 없어져서(모든 변경이 클릭 즉시 저장되므로) "완료"로 바꿔 그냥 `5-1`로 돌아가게만 함.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 요일별 기본 응답 — "각각 저장" 버튼 추가 (2026-08-17, 수정의 수정)

- **배경**: 바로 위 항목에서 "자동저장 + 완료 버튼"으로 고쳤는데, 사용자가 즉시 지적 — "각각저장이 있어야 할 것 같아"는 자동저장을 말한 게 아니라 **사람마다 눈에 보이는 저장 버튼이 각각 있어야 한다**는 뜻이었음. "완료"로 바뀐 것 자체가 저장 개념이 사라진 것처럼 보여 혼란을 줌.
- **조치**: 표에 "저장" 열을 추가해 행마다 자기만의 저장 버튼을 둠. 셀 토글은 더 이상 자동으로 저장하지 않고(메모리에서만 상태 변경) 각 행의 "저장" 버튼을 눌러야 그 사람 데이터가 `persistMember()`로 저장됨(눌린 버튼은 "저장됨"으로 잠깐 바뀌었다 복귀). 하단 버튼은 "닫기"로 라벨 변경(저장 의미가 없으니 완료라고 부르지 않음).
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 요일별 기본 응답 ↔ 달력 미리 답해두기 통합 (2026-08-17)

- **배경**: 사용자 지적 — "요일별 기본 응답이 있는데 달력에 미리 답을 한다는 건 일정에 변동이 생겼다는 거잖아. 그러니까 미리 답을 해두는 거잖아? 그런 걸 고려해줬으면 좋겠어." 확인해보니 실제로 두 기능이 완전히 따로 놀고 있었음 — `weekday_defaults`(요일별 기본 응답)를 읽는 코드가 `5-4` 자기 자신 말고 프로젝트 어디에도 없었음. `5-4`의 안내 문구("식사를 만들 때 예상 인원에 자동 반영돼요")가 사실이 아니었던 것.
- **조치**:
  1. `3-2. 식사 만들기`의 `applyAdvanceAnswers()` — 이 날짜에 대한 "예외" 답(`advance_answers`)이 없으면 `weekday_defaults`에서 그 요일의 기본값을 찾아 참가자 체크박스에 반영하도록 폴백 추가. 이제 `5-4`의 안내 문구가 실제로 참이 됨.
  2. `4-1. 홈 달력뷰`의 "미리 답해두기" 프롬프트 — 그 날짜의 요일에 이미 기본값이 있으면 처음부터 다시 묻지 않고 "이 요일은 평소 참여로 답해뒀어요. 이 날은 다른가요?" + "네, 평소대로" / "이 날은 다르게"(예외 지정) 버튼으로 바뀜. 요일 기본값이 없는 요일에만 기존처럼 "먹어요/안 먹어요"를 처음부터 물어봄. `weekdayDefaultFor(persona, date)` 함수 신규(요일 인덱스는 `(date.getDay()+6)%7`로 `5-4`의 월~일 순서와 맞춤).
  3. **문장도 같이 정리**: 사용자가 별도로 "미리 답해두면 안 물어봐도 돼요"라는 문장이 "나는 누구고 너는 누군데 이상하다"고 지적 — 물어보는 주체/안 물어보는 주체가 불명확했던 게 원인. 요일 기본값 없는 경우의 문구를 "이 날 식사에 참여하시나요? 지금 답해두면 나중에 식사를 만들 때 자동으로 반영돼요"로 교체(수동태 "반영돼요"로 바꿔 누가 누구에게 묻는지 따질 필요가 없게 함).
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 싸이월드 미니홈피에서 착안 — 방명록 + 오늘 안 본 사람 (2026-08-17)

- **배경**: 사용자 제안 — "싸이월드 미니홈피처럼 홈 화면을 더 발전시키면 어때? MVP 때 만들어야 핵심이 될 것 같은데." 싸이월드 요소별로 이 앱과의 적합성을 같이 따져봄(미니룸=이미 있는 원형 식탁, 방명록, 오늘 방문자 수, 미니미=이미 있음, 도토리/아이템 구매, 일촌). **도토리/아이템 구매 루프와 일촌은 제외** — 전자는 이미 확정한 핵심 수익모델(밀키트 제휴 수수료)과 수익원이 쪼개져 충돌하고, 후자는 이미 가족(가입 구성원)이라 친밀도 단계가 불필요·위계감만 추가. 방명록과 "오늘 안 본 사람"은 §0.5 철학과 맞고 저비용이라 **둘 다 작게 묶어서 구현**하기로 함.
- **방명록(가족 한마디)**: `localStorage['guestbook_notes']` = `[{name, message, day}]`(사람당 오늘자 최신 한마디 하나만 유지 — 좌석 위에 말풍선 하나씩 대응하니까). `3-0`에 "+ 한마디 남기기" 버튼(prompt 기반, 기존 `addMenuRequest()`와 같은 패턴) 추가, `renderTableHero()`가 오늘 한마디 남긴 사람의 좌석 바로 위에 말풍선(작은 pill)을 띄움 — 좌석 위치 계산을 그대로 재사용해 겹치지 않게 배치.
- **오늘 안 본 사람**: `localStorage['last_seen']` = `{persona: 날짜문자열}`, `3-0` 진입 시(`init()`) 그 페르소나의 오늘 방문을 기록. `renderUnseenLine()`이 오늘 식사 참여자(좌석에 있는 사람들) 중 오늘 이 화면을 안 본 사람을 걸러서 "아직 이 화면 못 본 사람: OOO"로 표시(호스트 자신은 제외). 전체 가구원이 아니라 오늘 좌석에 있는 사람만 대상으로 해서 불필요한 노이즈를 줄임.
- **하지 않은 것**: 꾸미기(식탁보 등 실사용 기반 데코)는 "재검토" 후보로만 `FEATURE_CATALOG.md`에 남기고 지금은 안 만듦 — 재밌지만 실제 조율에 꼭 필요한지는 별개 문제라 판단.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 방명록을 달력의 "가족 기록장"으로 연결 (2026-08-17)

- **배경**: 사용자가 방명록이 "얼마나 유지되냐"고 물어서 답한 직후 — "달력이라는 공간이라던가 뭔가 뿌듯함을 위한 장치가 필요하지 않아? 리뷰남기기도 PRD에 있었잖아." PRD(v0.5·v0.6·워크시트)를 다시 훑었지만 "리뷰" 표현은 못 찾음(사용자에게 출처 확인 요청함) — 그와 별개로 "달력=기록 공간, 뿌듯함 장치" 제안은 그 자체로 타당해서 바로 반영.
- **판단**: 배지·스트릭 같은 게이미피케이션을 새로 만들기보다, **방명록이 하루 지나면 사라지지 않고 달력에서 계속 다시 보이게만 해도** "쌓여가는 가족 기록"이라는 뿌듯함이 생김 — 별도 보상 장치보다 저비용이고 방명록 기능과 자연스럽게 이어짐.
- **조치**:
  1. `3-0`의 `todayKey()`를 `toDateString()`에서 `3-2`/`4-1`과 같은 `YYYY-MM-DD` 형식으로 변경 — 달력이 정확히 그 날짜로 매칭할 수 있게 형식을 통일.
  2. `addGuestbookNote()`가 더 이상 같은 사람의 이전 메시지를 지우지 않고 계속 쌓음(`guestbook_notes`에 append만, 삭제 없음) — 과거 기록이 실제로 남도록.
  3. `4-1. 홈 달력뷰`의 `renderDayDetail()`에 "그날의 한마디" 카드 추가 — 선택한 날짜(`selectedRealDateKey()`)에 해당하는 방명록을 모두 보여줌. 식사가 있는 날/없는 날 둘 다 공통으로 적용(메시지는 식사 유무와 무관하게 남길 수 있으니까).
- **하지 않은 것**: 스트릭 카운터·배지·마일스톤 같은 명시적 보상 장치는 지금 안 만듦 — "쌓여가는 기록을 다시 볼 수 있다"는 것 자체가 충분한 보상이라고 판단, 필요하면 나중에 별도로 스코프.
- **후속 — 메모 비주얼(2026-08-17)**: 방명록 목록이 스크린샷상 밋밋한 텍스트 줄 나열로 보인다는 지적("엑셀 메모 느낌처럼 보이면 좋겠는데") — `4-1`의 "그날의 한마디"와 `3-0` 좌석 위 말풍선 둘 다 옅은 노란 포스트잇 스타일(배경 `#fff6d6`, 테두리 `#ecdc9c`, 그림자, 살짝 회전)로 통일. 일회성 장식용 색이라 tailwind config에 추가하지 않고 인라인 스타일로 처리(이 프로젝트의 기존 관행 — 좌석 상태색 등도 인라인).
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 홈 화면 — 싸이월드 미니홈피 스타일 실험 (2026-08-17)

- **배경**: 사용자가 "전체 디자인을 싸이월드 미니홈피 스타일로 바꿔볼래? 홈 화면부터 확 바뀌어야겠지? 홈 화면만 먼저 변경해볼래?"라고 요청. **이건 앞서 확정한 "무채색 M3 그레이 팔레트가 의도한 것"(그레이 팔레트 관련 메모리 참고)을 뒤집는 결정** — 하지만 이번엔 사용자가 명시적으로, 의식적으로 요청한 별개의 실험이라 그대로 반영. `3-0. 홈`에만 적용하고 나머지 화면(`3-1`,`4-1`,`5-x` 등)은 그대로 그레이 유지 — 지금은 앱 전체가 시각적으로 안 맞아 보이는 게 정상(실험 단계).
- **조치(전부 `3-0. 홈`에만 적용)**:
  1. **색 토큰 값만 교체**(토큰 이름/구조는 그대로 둬서 나머지 코드는 안 건드림): `primary` → 핫핑크(`#ff6f9c`), `surface`/`background` → 연분홍(`#fff0f5`), `secondary-container`(식비예산 타일) → 라벤더(`#e0d4ff`), `tertiary-container`(밀키트 타일 + 식탁 히어로 원) → 민트(`#d4f5e3`), `surface-container`/`outline`/`outline-variant` → 핑크 톤, `on-surface`/`on-surface-variant` → 진한 자두색 계열. 페르소나 아바타 색(`IDENTITY_COLOR`)은 앱 전체에서 그 사람을 상징하는 색이라 안 건드림.
  2. **폰트**: `headline-lg`/`display-serif`를 "Gaegu"(동글동글한 손글씨 느낌 한글 폰트)로 교체 — 미니홈피 다이어리 감성. 본문(`body-md`)은 가독성 위해 Plus Jakarta Sans 유지.
  3. **배경**: `body`에 분홍→라벤더→민트 세로 그라데이션 추가(`bg-surface` 클래스는 이 그라데이션을 가리므로 body에서 제거).
  4. **장식**: 식탁 히어로 원 둘레에 핑크 점선 프레임(`.table-frame`, 스티커/미니룸 프레임 느낌) 추가. 헤더 타이틀에 "°˖✧ 우리집식당 ✧˖°" 반짝이 장식 추가.
- **메모리 갱신**: `feedback_grey_palette_intentional.md`에 "이번 예외는 `3-0`에 한정된 의식적 실험이며 그레이 팔레트 원칙 자체를 뒤집는 게 아니다"라는 내용을 추가해뒀음 — 다음 세션이 `3-0`을 실수로 그레이로 되돌리거나, 반대로 이 팔레트를 전체로 확산시키지 않도록.
- 전 파일(35개) JS 문법·링크·색상토큰 재검증 통과.

## 싸이월드 실험 즉시 롤백 (2026-08-17)

- **배경**: 위 실험을 보여주자마자 사용자가 바로 "야 이거 아니야 복구시켜"라고 반려. 색 토큰 값, 폰트(Gaegu), body 그라데이션 배경, 식탁 히어로의 점선 프레임, 헤더 반짝이 장식을 전부 원래 그레이 시스템 값으로 정확히 되돌림(`bg-surface` 클래스도 body에 복원). 방명록·오늘 안 본 사람 같은 그 전에 만든 기능들은 이 요청과 무관해서 그대로 둠(리버트 대상은 순수 시각 스타일뿐).
- `feedback_grey_palette_intentional.md`도 "실험 후 반려됨 — 원래 결정이 다시 한번 확인된 것"으로 갱신. 앞으로 이 프로젝트에 파스텔/장식적 팔레트를 사용자가 먼저 요청하지 않는 한 다시 제안하지 않음.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 테이스트 스킬 기반 UX/UI 전반 점검 (2026-08-17)

- **배경**: 사용자가 "테이스트 스킬 사용해서 UX/UI 전반적으로 수정좀 해줄래" 요청. `design-taste-frontend` 스킬을 로드했으나, 이 스킬은 랜딩페이지/포트폴리오용(히어로, GSAP 스크롤, 벤토 그리드, React/Motion 스택)이라 스킬 자체 §13에서 "멀티스텝 폼·조밀한 프로덕트 UI는 범위 밖"이라고 명시함 — 우리 앱이 정확히 그 범위 밖 케이스. 그래서 스킬 안의 **범용 품질관리 원칙만**(타이포/색/모서리 반경 일관성, 버튼 대비, em-dash 금지, 콘텐츠 밀도/롱리스트 처리, 카피 자기검열, 장식용 점 남용) 적용하기로 결정.
- **감사 방법**: 35개 라이브 화면 전체를 Explore 에이전트로 기계적 점검(em-dash, 모서리 반경, 버튼 대비, 중복 CTA, 롱리스트, 빈 상태, 장식용 점, 카피 오류).
- **감사 결과 요약**: em-dash 없음/모서리 반경 일관/버튼 대비 문제 없음/카피 문제 없음 — 전반적으로 이미 양호. 실제 개선이 필요했던 항목:
  1. **롱리스트 3곳 미대응**: `5-2. 식사 기록`(전체 기록 무제한 렌더), 알림 패널(`3-0`/`3-1`/`3-7`, 무제한 렌더), `7-1. 우리집 냉장고`(재료 목록 무제한 렌더) — 전에 홈의 "가족이 먹고싶어요"에 적용했던 것과 같은 문제. 전부 같은 패턴(미리보기 N개 + "+N개 더보기"/"접기")으로 통일.
  2. **알림 벨 점이 가짜 상태**: 헤더의 빨간 점이 실제 안읽음 여부와 무관하게 항상 고정으로 떠 있었음(정적 마크업, JS가 한 번도 건드린 적 없음) — `updateNotiDot()` 신규로 `liveNotis`+`NOTIS` 중 `unread:true`가 하나라도 있을 때만 보이게 수정, 3개 파일(`3-0`,`3-1`,`3-7`) 모두 반영.
  3. **`7-1` 냉장고에 빈 상태 없음**: 다른 목록들(요청·손님·검색 결과)엔 다 있는 "아직 없어요" 문구가 냉장고만 빠져 있었음 — 추가.
- **손대지 않은 것**: 중복 CTA로 지적된 `3-1`의 헤더 아바타(→5-3) vs 하단 탭 "설정"(→5-3)은 검토 후 유지 — 아바타=빠른 개인 진입, 하단 탭=일반 내비게이션이라는 흔한 앱 패턴이라 실제 결함이 아니라고 판단. `8-2`의 뒤로가기/건너뛰기 쌍도 정상적인 옵셔널 스텝 패턴이라 그대로 둠.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 달력 그날 상세 — 같은 날짜 다시 누르면 접힘 (2026-08-17)

- **배경**: 사용자 지적 — "달력을 한번더 누르면 접혀야 하는거 아님?" 확인해보니 맞는 지적. 날짜를 눌러 상세를 펼친 뒤, 이미 선택된 그 날짜를 다시 눌러도 아무 반응이 없었음(같은 값으로 `selectedDay`를 재대입할 뿐이라 리렌더해도 그대로) — 아코디언처럼 펼쳤다 접었다 할 수 있어야 하는데 펼치기만 가능하고 접을 방법이 없었음.
- **조치**: `dayDetailOpen` 상태 추가. 날짜 셀 클릭 시 `cell.d === selectedDay`(이미 선택된 그 날짜)면 `dayDetailOpen`을 토글, 다른 날짜면 그 날짜를 선택하고 항상 펼침(`dayDetailOpen = true`). `renderAll()` 끝에서 `#dayDetail`에 `hidden` 클래스를 이 상태에 맞춰 토글. 월/주/오늘 탭을 전환할 때도 `dayDetailOpen = true`로 리셋해 새 탭에서는 항상 펼쳐진 채로 시작.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 방명록 삭제 기능 추가 (2026-08-17)

- **배경**: 사용자 지적 — "한마디 남기면 삭제가 안되네?" 맞는 지적, 남기는 기능만 있고 지우는 방법이 아예 없었음.
- **조치**:
  1. `3-0. 홈`: "+ 한마디 남기기" 버튼 자리를 `renderMyNoteRow()`로 대체 — 오늘 내가 남긴 한마디가 있으면 "내 한마디 · '메시지' 지우기"로 바뀌고, 없으면 기존 버튼을 그대로 보여줌. 새 노트에 고유 `id`를 부여해(`'note'+Date.now()+random`) `removeGuestbookNote(id)`로 지울 수 있게 함.
  2. `4-1. 홈 달력뷰`: "그날의 한마디" 카드의 각 포스트잇에 내가 남긴 것만(`n.name === CURRENT_PERSONA`) × 삭제 버튼을 추가. 기존/신규 노트 모두에서 안전하게 동작하도록 id가 아니라 `notes.indexOf(n)`로 원본 배열에서의 실제 위치를 찾아 `removeNoteAt(index)`로 삭제(배열 인덱스 기반이라 예전에 id 없이 저장된 테스트 데이터도 문제없이 지워짐).
  3. 둘 다 **남의 한마디는 지울 수 없음** — 요일별 응답처럼 "확인 후 수정"이 아니라 아예 본인 것만 삭제 버튼이 보이게 해서, 남의 메시지를 실수로 지우는 일 자체가 안 생기게 함.
- **후속(2026-08-17)**: 사용자 지적 — "삭제버튼은 누르면 꼭 확인을 해야 한다고 생각해, 잘못 누를 수도 있고 수정하려던 걸 수도 있으니까." 두 삭제 함수(`removeGuestbookNote`, `removeNoteAt`) 맨 앞에 `confirm('이 한마디를 지울까요? 되돌릴 수 없어요.')` 추가 — 취소하면 아무 일도 안 일어나므로, 실수로 눌렀거나 사실은 수정하고 싶었던 경우 모두 되돌릴 기회가 생김.
- **후속 2(2026-08-17)**: "내 한마디" 표시가 스크린샷으로 보니 "내 한마디 · '메시지' 지우기"가 한 줄에 뭉쳐진 밋밋한 회색 텍스트라 "야 이건 아니지"라는 지적. 좌석 말풍선·달력 카드에 이미 적용해둔 포스트잇 톤(배경 `#fff6d6`, 테두리 `#ecdc9c`)으로 이것도 통일 — "내 한마디" 라벨 + × 삭제 버튼을 한 줄에, 메시지는 그 아래 별도 줄로 분리한 작은 카드로 재구성. 같은 기능이 세 군데(홈 좌석 말풍선·달력 카드·이 요약 줄)에 나오는데 스타일이 서로 다르면 안 되니 전부 같은 포스트잇 레시피로 맞춤.
- **후속 3(2026-08-17) — 지난 날짜에도 한마디를 남길 수 있게**: 사용자가 "내일 어제꺼에 남길 수 있어?"라고 질문 — 확인해보니 실제로 불가능했음. 유일한 작성 경로(`3-0`의 "+ 한마디 남기기")가 항상 `todayKey()`(진짜 오늘 날짜)로만 저장돼서, 달력에서는 과거 날짜의 한마디를 보고 지울 수만 있었지 새로 남길 방법이 없었음. "달력 = 기록장"이라는 컨셉과 앞뒤가 안 맞는 빈틈이라 바로 메움 — `4-1`에 `addNoteForSelectedDay()` 신규, 선택한 날짜(`selectedRealDateKey()`) 기준으로 저장하는 "+ 이 날 한마디 남기기" 버튼을 그날 상세 패널에 추가(기존 "+ 이 날 식사 추가"와 같은 위치·스타일). 이제 오늘이든 지난 날짜든 달력에서 바로 남길 수 있음.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 0~8단계 워크시트 작성 (2026-08-17 밤, 자동 작업)

- **배경**: 사용자가 자기 전에 "0단계~8단계까지 지금 만든 걸 기준으로 채워줘, docx 문서에 그대로. 모르는 건 메모로 남기고 인터뷰가 필요한 부분까지 포함해서."라고 요청. 템플릿을 `0. 확인용자료/0.문서/`에서 전부 확인(0단계 미니PRD 가이드, 1~8단계 워크시트, 4단계는 이미 `참고문서/4단계_워크시트_우리집식당_작성완료.md`로 완료돼있었음).
- **산출물**: `참고문서/워크시트/` 폴더에 0~8단계 + 작업메모, 총 10개 `.md` 파일 작성. 3단계 경쟁사 벤치마킹은 실제 웹 검색(TableMesh·Meal Train·Cozi Family Organizer)으로, 6단계 데이터베이스 설계는 전 35개 라이브 화면에서 실제 사용 중인 `localStorage` 키 41개를 전부 추출해 근거로 삼음(추측 아님).
- **docx도 함께 시도**: 기존 템플릿 docx를 직접 XML로 수정하면 파일이 깨질 위험이 있어(사용자에게 사전 확인), 대신 매번 새 문서를 처음부터 만드는 방식(md→OOXML 변환 스크립트 + PowerShell 조립)으로 `참고문서/워크시트/docx/`에 10개 `.docx`를 별도 생성. 생성 직후 zip 엔트리 경로가 백슬래시로 저장되는 버그(Word가 못 읽는 상태)를 발견해 forward-slash 경로로 재조립 후 전체 파일 XML 유효성 재검증 완료.
- **모르는 것은 메모로**: 실제 인터뷰 1건(3단계), 네이버 데이터랩 조회(1단계), 팀명·팀원 확정(0단계), 발산 아이디어 7개 중 5개(1단계) — 이 4가지는 AI가 대신 채우지 않고 명시적으로 비워둠. 그 외 재구성/추정한 항목(5 Whys, TAM/SAM/SOM, PRD 성공지표 등)은 전부 "🔖 메모"로 근거·불확실성을 표시. 전체 목록은 `참고문서/워크시트/00_작업메모.md` 참고.
- 이 세션 동안 코드(35개 화면)는 건드리지 않음 — 순수 문서 작업.

## 워크시트 결과를 실제로 반영 (2026-08-17)

- **배경**: 사용자가 "0~8단계 훑어보니 뭐 바뀌거나 추가해야 할 거 있었어?"라고 물어서 5가지를 답했는데, 이어서 "너가 반영해야 하는 거 아냐? 시켰잖아"라고 지적 — 맞는 말이라 실제로 반영함.
- **PRD(`참고문서/우리집식당_통합본_v0.6.md`) 수정**:
  1. **§1.3 경쟁 구도 신규 추가**: TableMesh·Meal Train·Cozi Family Organizer 실제 조사 결과 + 차별점 문장.
  2. **§12 데이터 모델에 아키텍처 규칙 추가**: "무가입 참여자는 users 테이블 없이도 존재할 수 있어야 한다" — 지금까지 코드에만 암묵적으로 있던 원칙을 문서화.
  3. **§0 개정 이력에 위 두 항목 기록**.
  4. **정정**: 7단계 워크시트에 "PRD에 성공 지표가 없어서 새로 제안함"이라고 적었던 게 실수였음 — v0.5 §14를 전체로 안 읽고 v0.6의 "동일" 한 줄만 보고 판단한 것. 실제로는 "가구 단위 반복 조율률" 등 이미 상세한 지표가 있었음. `참고문서/워크시트/7단계_PRD작성.md`에 정정 내용 반영.
- **코드 버그 2건도 같이 발견해서 수정**:
  1. **알림 "읽음 처리"가 실제로 저장 안 됨**: 헤더의 안읽음 점이 페이지 로드 시 한 번만 계산되고, 알림을 눌러봐도 그게 "읽었다"고 어디에도 안 남았음(다시 열면 또 안읽음으로 보임). `3-0`/`3-1`/`3-7`에 `localStorage['notification_reads']`(읽은 알림 id 배열) 추가, `showNotiDetail()`에서 열람 즉시 `markNotiRead(id)` 호출, 점 표시는 `isUnread(n)`(원래 unread 플래그 && 안 읽음 목록에 없음)로 판정. 정적 데모 알림 4개에 안정적인 `id` 부여.
  2. **`3-3. 참여자 응답`이 만드는 실시간 알림이 새 타입 시스템과 안 맞았음**: `notifyHostOfChange()`가 옛날 `path` 필드만 있는 객체를 만들고 있었는데, `3-0`/`3-1`/`3-7`의 `showNotiDetail()`은 이미 `type` 기준으로 분기하도록 바뀌어 있어서(알림 패널 항목별 반응 불일치 수정, 아래 항목 참고) 이 실시간 알림을 누르면 엉뚱하게 "환영합니다" 화면이 떴을 것 — `type:'response_change'` + `name`/`from`/`to`로 스키마를 맞춰서 고침. `expectedFrom`/`expectedTo`(예상 인원)는 이 화면에서 계산할 수 없는 값이라 억지로 안 넣고, 렌더러 쪽(`hasCount` 체크)에서 없으면 그 줄 자체를 생략하도록 방어 처리.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 냉장고 화면 — 러버블 참고본 기준 재구성 (2026-08-17)

- **배경**: 사용자가 같은 냉장고 화면을 러버블(Lovable)로 만든 참고 스크린샷을 보여주며 "이게 훨씬 깔끔하네, 한눈에 보이잖아" + "사진·영수증도 좋은데 검색으로 추가하는 것도 넣었으면 좋겠어."
- **조치**:
  1. **빠른 추가(검색 스타일)**: 상단에 재료 이름+수량 입력 + "+" 버튼 추가(`addIngredient()`). 기존 FAB(사진/영수증 분석, `7-2`)는 그대로 두고 추가만 됨 — 대체 아님.
  2. **삭제 기능 신규**: 지금까지 재료를 지울 방법 자체가 없었음(감사 때 "빈 상태 없음 — 삭제 함수가 없어서 도달 불가능"으로 남겨뒀던 항목) — 각 행에 휴지통 버튼 추가(`removeIngredient(index)`).
  3. **재료별 색깔 뱃지 제거, "곧 상해요" 카드로 통합**: 기존엔 매 줄마다 신선/소비임박/오늘소진 뱃지가 달려 있었는데, 참고본처럼 급한 것만 상단 카드 하나로 모아서("두부 — 다음 집밥에 먼저 쓰면 좋아요" 톤) 보여주고, 목록은 "이름 · 수량"만 담백하게. 상단의 3칸 통계 뱃지(신선/임박/소진 개수)도 제거 — 곧 상해요 카드 하나로 충분.
  4. **수량(qty) 필드 추가**: 기존엔 "N일 전 추가"만 있었는데, 참고본처럼 "두부 · 1모" 식으로 표시하도록 데이터 모델에 `qty` 추가(기존 9개 기본 재료에 임의 수량 부여).
  5. **밀키트 제안 미니 섹션 추가**: `7-3. 메뉴 추천`과 동일한 레시피·매칭 로직을 재사용(로직 갈라지지 않게)해서, 냉장고 재료로 절반 이상 완성되는 레시피가 있으면 상위 2개를 여기서도 바로 보여줌. "보러가기"는 `7-3`과 똑같이 `milkit_order`를 저장하고 `8-1`로 이동.
  6. **하단 문구 추가**: "제안은 필요할 때만 조용히 보여요. 알림으로 재촉하지 않아요." — §0.5 철학과 직결되는 안심 문구, 참고본에서 그대로 가져옴.
- 전 파일(35개) JS 문법·링크·색상토큰 재검증 통과.

### 후속 — 참고본을 그대로 베끼지 말고 우리 강점은 남기기 (2026-08-17)

- **배경**: 사용자 지적 — "야 똑같이 만들라는게 아니었잖아. 우리가 만든것에서 장점이라 생각되는건 남겨야지." 맞는 말 — 참고본이 재료 3개짜리 예시라 심플해 보였던 거지, 우리 냉장고는 기본 9개+추가로 늘어나는 목록이라 그대로 옮기면 정보가 사라짐.
- **되살린 것**:
  1. **3칸 통계(신선해요/소비임박/오늘소진 개수)**: "곧 상해요" 카드는 상위 2개만 보여주는데, 그 뒤에 더 있는 급한 재료가 있어도 통계가 없으면 알 방법이 없었음 — 목록을 굳이 안 펼쳐도 전체 현황을 알 수 있게 원래 있던 3칸 통계를 되살림.
  2. **재료별 신선도 표시**: 완전히 없앴던 걸 되살리되, 예전처럼 글자 가득한 색깔 뱃지가 아니라 작은 색점(●) 하나로 — 목록은 여전히 담백하게 유지하면서 "곧 상해요"에 안 뽑힌 3번째·4번째 재료의 상태도 계속 보이게 함.
- **그대로 둔 것**: 빠른 추가, 삭제 버튼, 수량 표시, 밀키트 제안, 하단 안심 문구 — 이건 참고본이 실제로 없던 걸 채워준 개선이라 유지.
- 전 파일(35개) JS 문법·링크 재검증 통과.

### 후속 2 — 중복 표시 제거 + 신선도를 진짜 날짜 계산으로 (2026-08-17)

- **배경**: 사용자 지적 두 가지. (1) "곧 상하는 재료를 위로 올리면 되는 거 아냐, 중복으로 쓰일 필요가 있어?" — "곧 상해요" 카드와 목록의 색점이 같은 정보를 두 번 보여주고 있었음(급한 재료가 카드에도, 목록에도 따로 표시됨). (2) "신선한지 소비임박인지 등록날짜·소비기한도 없는데 어떻게 알아. 일을 위한 일을 만들지 말라고" — 더 근본적인 지적. `freshness` 필드가 애초부터(오늘 손댄 부분 이전, 원본 목업 때부터) 그냥 하드코딩된 값이었지 실제 등록일이나 소비기한에서 계산된 적이 한 번도 없었음 — 겉보기엔 그럴듯한데 실제로는 아무 근거 없는 가짜 상태였음.
- **조치**:
  1. **"곧 상해요" 카드 삭제, 목록 자체를 급한 순서로 정렬**: 별도 카드 없이 `renderFridgeList()`에서 `computeFreshness()` 기준으로 urgent→expiring→fresh 순 정렬. 같은 정보를 두 곳에서 안 보여줌.
  2. **신선도를 실제 날짜 계산으로 교체**: 각 재료에 `freshness`를 직접 박아두는 대신 `addedAt`(등록일)만 저장하고, `computeFreshness(item)`이 오늘 날짜와의 차이로 매번 계산(등록 후 5일 이상=오늘 소진, 3일 이상=소비 임박, 그 이하=신선). 빠른 추가로 넣은 재료도 `addedAt: todayISO()`로 실제 등록 시각을 남김. 재료별 실제 유통기한까지는 반영 안 함(그건 이 규모엔 과한 일이라 ponytail 주석으로 한계 명시) — 그래도 "며칠째 있었는지"라는 진짜 근거는 생김.
  3. `7-2. 재료 사진 분석`에서 넘어오는 항목(옛 `freshness` 필드만 있고 `addedAt` 없음)은 `computeFreshness()`가 안전하게 폴백 처리해서 그대로 작동함 — 이 화면은 안 건드림.
- 전 파일(35개) JS 문법·링크 재검증 통과.

### 후속 3 — 재료 추가 방법을 흩어두지 말고 한곳에 (2026-08-17)

- **배경**: 사용자 지적 — "냉장고 재료를 넣는데 함께 있으면 되는 거 아냐? 꼭 카메라 모양으로 따로 빼야겠어?" 확인해보니 재료 추가 방법이 두 군데로 흩어져 있었음 — 빠른 추가(이름+수량) 입력줄은 페이지 중간에, 사진·영수증 분석은 화면 우하단 떠 있는 별도 동그란 카메라 버튼으로 눌러야 `7-2`라는 완전히 다른 화면으로 이동. "재료 추가"라는 같은 목적의 방법들이 위치도 다르고 시각적 무게도 달라서 하나로 안 읽혔음.
- **조치**: 우하단 플로팅 카메라 버튼(FAB)을 없애고, 빠른 추가 입력줄 바로 아래에 "사진·영수증으로 한 번에 추가" 텍스트 버튼을 배치 — 두 방법이 같은 블록 안에서 나란히 보이게 함. `7-2` 자체는 그대로 유지(사진과 영수증 중 고르는 화면이라 이미 두 방식을 다 다루고 있어서 손 안 댐), 진입 지점만 빠른 추가와 같은 자리로 모음.
- **후속(2026-08-17) — 그래도 여전히 줄이 나뉘어 있었음**: 사용자가 스크린샷을 다시 보여주며 "말귀를 못알아듣니?"라고 지적 — 입력줄과 "사진·영수증으로 추가" 버튼이 여전히 위아래 두 줄로 나뉘어 있었음(같은 블록 안이긴 해도 시각적으로 두 개의 별개 컨트롤). "함께 있으면 되는거 아냐"는 물리적으로 가깝게 두라는 뜻이 아니라 **한 줄로 합치라는 뜻**이었음. 사진·영수증 진입 버튼을 별도 줄에서 빼서, 입력창·수량·카메라 아이콘·+ 버튼을 전부 한 줄(`flex`) 안에 나란히 배치 — 이제 재료 추가는 시각적으로도 하나의 컨트롤.
- 전 파일(35개) JS 문법·링크 재검증 통과.

### 후속 4 — "곧 상해요" 카드 복원: 중복이 아니라 조언이었음 (2026-08-17)

- **배경**: 사용자가 참고본 스크린샷을 다시 보여주며 "이 구분이 뭐라고 생각하니? 따라할라면 이런 걸 따라해야지"라고 질문 — 후속 2에서 "곧 상해요" 카드를 지우고 목록 정렬로 합친 게 실수였음을 깨달음.
- **진짜 구분**: "곧 상해요" 카드는 목록의 색점(상태 표시)과 같은 정보가 아니라 **다른 층위의 정보**임 — 색점은 "이게 임박했다는 사실"을, 카드는 "그래서 다음 집밥에 먼저 쓰면 좋겠다"는 **조언 문장**을 전달함. 재고(무엇이 있는지)와 조언(그래서 뭘 하면 좋을지)은 서로 다른 목적이라 하나로 합치면 안 되는 거였음 — 지난번엔 "같은 정보를 두 번 보여준다"고 잘못 판단해서 조언 레이어 자체를 지워버렸음.
- **조치**: `#expiringSection`/`renderExpiringSection()` 복원 — 단, 이번엔 하드코딩된 값이 아니라 실제 `computeFreshness()`(등록일 기반 계산)로 골라낸 상위 2개를 보여줌. 목록 정렬·색점(후속 2)은 그대로 유지 — 재고 보기와 조언 보기 둘 다 있는 게 맞는 구성.
- 전 파일(35개) JS 문법·링크 재검증 통과.

### 후속 5 — 등록일을 실제로 보여주고, 추정치라고 정직하게 밝힘 + 추가 UI 재조정 (2026-08-17)

- **배경**: 사용자가 연달아 두 가지 지적. (1) "냉장고 재료 언제 등록했는지 왜 없냐니까? 소비기한은 왜 없냐고 곧상하는지 니가 어떻게 알건데?" — 후속 2에서 `addedAt`을 실제로 계산에 쓰기 시작했지만, 그 근거(등록일)를 화면 어디에도 안 보여주고 있었음. 소비기한(실제 유통기한)은 여전히 없고 "등록 후 며칠"이라는 대체 지표일 뿐인데, 그걸 밝히지 않고 색점만 보여주면 마치 진짜 소비기한을 아는 것처럼 보임. (2) "좆같은 카메라 빼고 한번에 등록해요라는 문장과 함께 냉장고재료와 영수증을 넣으면 되잖아 검색아래에" — 직전에 입력줄에 카메라 아이콘을 욱여넣은 게 오히려 더 안 좋았음. 아이콘 하나로는 "사진도 되고 영수증도 된다"는 걸 전달 못 함.
- **조치**:
  1. **등록일 표시**: 각 재료 행에 이름·수량 아래 작은 글씨로 "등록 N일째"(또는 "오늘 등록") 추가(`fridgeDateLabel()`). "곧 상해요" 카드의 문장에도 "두부(등록 5일째) — 다음 집밥에 먼저 쓰면 좋아요"처럼 근거를 같이 보여줌.
  2. **정직한 고지**: 통계 3칸 바로 아래에 "신선도는 실제 소비기한이 아니라 등록 후 며칠 지났는지로 추정해요"라는 문장 추가 — 소비기한 데이터가 없다는 한계를 숨기지 않고 밝힘.
  3. **입력줄 재조정**: 카메라 아이콘을 입력줄에서 다시 빼고, 검색(이름+수량+추가) 한 줄만 남긴 뒤 그 아래에 "냉장고 사진·영수증으로 한 번에 등록해요"라는 문장형 링크로 복귀.
- **하지 않은 것**: 재료별 실제 소비기한 입력 필드는 추가 안 함 — 매번 입력해야 하는 수고가 생기는 데다("일을 위한 일" 경계), 그렇게 해도 결국 사용자가 직접 알아내서 입력해야 하는 값이라 지금 단계에서 필수로 만들 근거가 약함. 대신 근거(등록일)를 투명하게 보여주는 쪽을 선택.
- 전 파일(35개) JS 문법·링크 재검증 통과.

### 후속 6 — "한 번에 등록해요"가 사실 한 번이 아니었음 (2026-08-17)

- **배경**: 사용자 지적 — "한번에 등록해요를 클릭하면 들어가서 다시 눌러야 하잖아. 카메라 버튼이랑 뭐가 다르냐고. 사용자가 눌러야 하는 코스트가 하나 더 늘어나는 거잖아. 무조건 사용자 중심으로 대답하는 거야 난." 정확한 지적 — `7-2`로 이동하면 "냉장고 사진"/"영수증 업로드" 두 버튼 중 하나를 또 눌러야 분석이 시작됐음. 라벨은 "한 번에 등록해요"인데 실제로는 탭이 두 번 필요했던 것.
- **확인**: `7-2`의 두 버튼(냉장고 사진 / 영수증 업로드)이 실제로는 **완전히 똑같은 `startAnalysis()`**를 호출하고 있었음(이 목업엔 사진과 영수증을 다르게 처리하는 로직 자체가 없음) — 즉 "고르게 하는" 화면이 사용자에게 아무 실질적 선택권도 안 주면서 클릭 비용만 추가하고 있었던 것.
- **조치**: `7-1`의 링크에 `?auto=1` 쿼리를 붙이고, `7-2`가 이 파라미터를 보면 업로드 선택 화면을 건너뛰고 `startAnalysis()`를 바로 호출하도록 함. 이제 "냉장고 사진·영수증으로 한 번에 등록해요"를 누르면 정말 한 번에 분석이 시작됨 — 라벨과 실제 동작이 일치하게 됨. `7-2`를 다른 경로로 직접 열면(쿼리 없이) 기존처럼 선택 화면이 그대로 나와서 다른 곳에 영향 없음.
- 전 파일(35개) JS 문법·링크 재검증 통과.

### 후속 7 — "잘못 받아들인 것 같은데?"에 바로 추측하지 않고 물어봄 (2026-08-17)

- **배경**: 후속 6(auto=1로 클릭 수 줄이기) 이후 사용자가 "야 내말을 잘못받아들인것같은데?"라고만 짧게 말함. 여기서 또 추측해서 고치는 대신, "모르겠으면 질문을 해야지 왜 그냥 만들어버리는건?"이라는 지적을 받기 전에 먼저 AskUserQuestion으로 뭘 잘못 받아들였는지 직접 확인함(페이지 이동 자체가 문제 / 아이콘·문장 스타일 차이 없음 / 결과 확인 단계도 없애야 함, 3가지 후보 제시) — 답은 **"페이지 이동 자체가 문제"**.
- **조치**: `7-2. 재료 사진 분석`으로 이동시키는 것 자체를 없앰 — 분석 애니메이션·결과 그리드·"냉장고에 추가" 버튼을 전부 `7-1` 안으로 인라인으로 가져옴(`startInlineAnalysis()`/`showInlineResult()`/`confirmInlineAnalysis()`). 이제 "사진·영수증으로 한 번에 등록해요"를 누르면 페이지 이동 없이 같은 화면에서 애니메이션이 뜨고, 결과 확인 후 추가하면 그대로 목록에 반영됨.
- **`7-2`는 삭제 대신 `_archive/`로 이동**(기존 원칙 유지) — 이제 라이브 흐름 어디서도 안 쓰이고 확인용뷰어에서만 참고로 남음. 내부 상대경로 보정, 뷰어 참조를 "참고 전용" 그룹으로 이동.
- **메모리 저장**: `feedback_ask_when_unsure.md` — 애매한 지적이 반복해서 틀리기 시작하면(2회 이상) 더 추측하지 말고 바로 질문할 것.
- 전 파일(35개, 아카이브 포함) JS 문법·링크 재검증 통과.

## 알림 패널 항목별 반응 불일치 수정 (2026-08-17)

- **배경**: 사용자가 알림 패널 스크린샷 두 장을 보여줌 — 1번째("박철수님이 응답을 바꿨어요")는 눌렀을 때 패널 안에서 바로 상세(변경 전후, 전화하기 버튼)를 보여주는데, 2~4번째("이지은님이 아직 응답 안 함", "오늘의 식탁이 확정됐어요", "환영해요")는 왜 그렇게 안 해놨냐는 지적.
- **원인**: `NOTIS` 배열에서 1번째 항목만 `inline` 필드(상세 정보)를 갖고 있었고, 나머지는 `path`만 있어서 누르면 패널이 닫히고 전체 페이지 이동을 해버렸음 — 같은 패널 안의 항목인데 반응 방식이 항목마다 달랐던 것. `3-0. 홈`, `3-1. 내 식탁`, `3-7. 홈 빈 상태` 세 화면 모두 `var NOTIS` 알림 패널을 복사해 쓰고 있어서 세 곳 다 같은 문제였음.
- **조치**: `inline`/`path` 이분법을 없애고 모든 항목에 `type`을 부여(`response_change`/`reminder`/`confirmed`/`welcome`), `showNotiDetail(n)`을 타입별 분기로 일반화 — 이제 어떤 알림이든 누르면 패널 안에서 상세 + 그 상황에 맞는 액션 버튼을 바로 보여줌(전체 이동 없음). `reminder`엔 "리마인드 보내기"(토스트로 즉시 처리), `confirmed`·`welcome`엔 "확인했어요" + "자세히 보기/식사 만들러 가기"(더 보고 싶을 때만 선택적으로 이동). `welcome` 항목의 목적지도 원래 `3-0`(온보딩 웰컴 메시지인데 3-0에서 열면 제자리로 되돌아가는 죽은 링크였음)에서 실제로 할 일인 `3-2. 식사 만들기`로 수정. 세 화면 모두 동일하게 반영.
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 홈의 오늘/내일 카드 중복 제거 (2026-08-17)

- **배경**: 바로 위 개편(핵심가치 우선순위 재배치)에서 오늘/내일 식사 카드를 홈 최상단으로 옮겼는데, 사용자가 스크린샷을 보고 바로 지적 — "이거 내 식탁이랑 중복 아니냐, '내 식탁에서 확인하기' 링크만 넣어두면 되는 거 아니냐."
- **확인**: `3-1. 내 식탁`의 코드를 실제로 검색해봄 — `renderAllMeals`(호스트/게스트 카드+응답 버튼)는 있지만 `가족이 먹고 싶어해요`, `미리 답해두기`(`advance_answers`), `menu_poll`(메뉴 투표)은 `3-1`에 전혀 없음. 즉 **오늘/내일 카드(`renderNeutralCard`)만 `3-1`과 진짜 중복**이었고(같은 식사 데이터를 역할 없는 요약본으로 한 번 더 보여줄 뿐, 카드를 눌러도 결국 `3-1`로 이동) 나머지 세 기능(메뉴투표·먹고싶어요·미리답해두기)은 홈에만 있는 고유 기능 — 사용자 지적은 정확했지만 그 지적이 홈의 나머지 섹션까지 다 지우라는 뜻은 아니었음.
- **조치**: `todayCards`/`tomorrowCards` 섹션과 `renderNeutralCard()` 함수, `init()`의 카드 렌더 루프를 전부 삭제. 대신 부제(`computeSubtitle()`, 바로 위 개편에서 추가) 바로 아래에 "내 식탁에서 확인하기 →" 링크 하나를 배치 — 상태는 한 줄로 이미 보이니 나머지 상세·액션은 전부 `3-1`에서 하도록 통일. 페이지 맨 아래에 있던 중복 "내 식탁에서 자세히 보기" 링크도 제거(같은 도착지로 가는 링크가 위/아래/하단탭 세 곳에 있을 필요 없음 — 위 CTA + 하단탭 두 곳으로 축소).
- 전 파일(35개) JS 문법·링크 재검증 통과.

## 냉장고 사진·영수증 추가를 페이지 이동 없이 인라인으로 통합 + 실제 파일 선택 복원 (2026-08-17)

- **배경**: `7-1. 우리집 냉장고`의 "한 번에 등록해요" 버튼이 눌러도 `7-2. 재료 사진 분석`로 이동해서 그 안에서 또 한 번 눌러야 분석이 시작됐음 — 사용자: "한번에 등록해요를 클릭하면 들어가서 다시 눌러야하잖아... 사용자가 눌러야 하는 코스트가 하나더 늘어나는거잖아 그걸 없애고싶다니까?" 이후 애매한 정정이 반복되어(AskUserQuestion으로 확인 결과) 진짜 문제는 클릭 수가 아니라 **페이지 이동 자체**였음. 이를 없애려고 만든 첫 인라인 버전은 카메라·영수증 구분과 실제 파일 선택 자체를 모두 생략한 채 텍스트 버튼 하나 → 가짜 분석 애니메이션으로 바로 넘어갔는데, 사용자가 "카메라와 영수증 어디갔어!!!"에 이어 "너 뭘로 찍어서 결과 낼건데??? 목업이라고 무시하냐!!!"라고 지적 — AI 인식 결과 자체는 목업이어도, 사진을 실제로 고르는 동작(카메라/영수증 구분 포함)까지 생략하는 건 목업을 성의없이 만든 것이라는 지적.
- **조치**: `7-2`를 `_archive/`로 이동(두 업로드 버튼이 원래 동일한 `startAnalysis()`를 호출해 실제 분기가 없었고, 이제 라이브 흐름 어디서도 참조되지 않음 — 내부 상대경로 `../7-1.../code.html` → `../../7-1.../code.html`로 보정, `0. 확인용뷰어`도 "참고 전용" 그룹으로 이동). `7-1` 안에 `1-1. 온보딩-회원가입`의 프로필 사진 업로드와 동일한 패턴(`<label>`로 감싼 진짜 `<input type="file" accept="image/*">`)으로 카메라(`capture="environment"`)·영수증 두 개의 실제 파일 입력을 나란히 배치, `change` 이벤트에서 `FileReader.readAsDataURL()`로 실제 선택한 사진을 읽어 분석 애니메이션 중 썸네일로 보여줌(`startInlineAnalysis(previewDataUrl)`) — 사진 선택 자체는 진짜, AI 인식 결과(`ANALYZE_FOUND`)만 목업. 확인 후 `confirmInlineAnalysis()`가 입력값·미리보기를 초기화해 재사용 가능하게 함.
- **하지 않은 것**: 실제 AI 이미지 인식(백엔드 없는 정적 목업이라 원천적으로 불가) — 대신 "선택한 사진으로 인식 중"이라는 진행 표시와 실제 썸네일로 그 한계를 숨기지 않고 보여줌.
- 전 파일(35개, 아카이브 포함) JS 문법·링크 재검증 통과.

## localStorage → Supabase 실서비스 전환 시작 (2026-08-17)

- **배경**: 사용자가 "실제 동작하도록 진짜 만들어보자"고 요청. AskUserQuestion으로 확인한 범위: (1) 지금 폴더 그대로 유지, (2) `FEATURE_CATALOG.md` "포함" 항목 전부 한번에(냉장고AI·8-2/3/4 제외), (3) 이메일+카카오 로그인, (4) 실시간 동기화(Realtime) 적용. "MVP가 아니라 완성본을 만든다는 마음으로, 대신 쉽게 가자"는 방향 확인.
- **조사**: Explore 에이전트 3개로 (a)전체 35개 화면의 localStorage 키 39개 전수조사, (b)PRD `우리집식당_통합본_v0.6.md` §12/§6.4/§7 + `6단계_데이터베이스설계.md`의 기존 데이터모델, (c)로그인/온보딩 8개 파일의 정확한 현재 로직을 조사. 계획은 `C:\Users\Yoom\.claude\plans\parallel-mixing-bee.md`에 저장.
- **핵심 설계 결정**: 정적 HTML 유지, Supabase JS SDK를 CDN으로 로드. 신규 공유 파일 `shared/supabase-client.js`(클라이언트 초기화 + 세션 헬퍼), `shared/api.js`(도메인별 함수 모음, 화면마다 새 파일 안 만듦). 스키마는 `supabase/schema.sql`에 전체 작성 — 이름 문자열 대신 `household_members.id` FK로 통일(가장 큰 정규화 갭 수정), 무가입 참여자(`user_id` nullable) 규칙 유지, 가구 소유권(owner/co-admin) role 컬럼, 1인1링크 토큰은 `meal_participants.response_token`(uuid, 익명 접근은 SECURITY DEFINER RPC 2개로만 허용 — 테이블 자체엔 anon 권한 없음).
- **1단계 완료**: `supabase/schema.sql` 작성(테이블 18개 + RLS 정책 + Storage 버킷). 사용자가 실제 Supabase 프로젝트를 만들어 SQL Editor에서 이 파일을 실행하고, `shared/supabase-client.js`의 `SUPABASE_URL`/`SUPABASE_ANON_KEY`를 실제 값으로 채워야 실제로 동작함(아직 플레이스홀더 상태).
- **2단계 완료**: 로그인/온보딩 8개 화면(`1-1`~`1-4`, `2-1`, `2-2`, `5-1`, `5-3`)을 가짜 `localStorage` 로그인에서 실제 Supabase Auth로 교체.
  - `1-1`: `supabase.auth.signUp()` + 크롭한 프로필 사진을 실제 Storage(`avatars` 버킷)에 업로드.
  - `1-3`: `households` 행 실제 생성.
  - `1-4`: `household_members` 실제 bulk insert — "나"만 `user_id` 연결(role: owner), 나머지는 무가입(user_id null)으로 §12 규칙 그대로 구현. 멤버별 사진도 실제 업로드.
  - `2-1`/`2-2`: 로그인 게이트를 `supabase.auth.getSession()` 기반으로 교체, 로그인 폼은 `signInWithPassword()`, 카카오 로그인 버튼 신규 추가(기존에 있던 미구현 "지문으로 로그인" 자리 대체 — WebAuthn 없이 항상 성공하는 가짜 기능이었음).
  - `5-1`: `co_admins`(이름 문자열 배열) → `household_members.role` 컬럼 기반으로 교체, 가구 삭제 버튼을 오너에게만 노출 + 실제 삭제 연결, 저장된 손님도 실제 테이블(`saved_guests`) 연동.
  - `5-3`: 프로필 표시를 실제 세션/멤버 데이터 기반으로 교체. **버그 발견 겸 수정**: 기존 로그아웃 버튼이 `uc_logged_in` 플래그를 안 지우고 그냥 페이지 이동만 해서, 로그아웃해도 로그인 화면이 자동으로 다시 홈으로 돌려보내는 죽은 버그가 있었음 — 실제 `supabase.auth.signOut()`으로 교체하며 자동 해결.
- **하지 않은 것(이번 커밋에서 범위 밖)**: 온보딩 이후 "가구에 새 구성원 추가"(5-1의 "+구성원 초대" 버튼은 여전히 안내 토스트만 — 이 마이그레이션 계획엔 없던 별도 신규 기능이라 범위에 안 넣음). 카카오 Provider 자체의 Supabase 프로젝트 설정(외부 계정 작업, 사용자가 Kakao Developers에서 앱 등록 후 Supabase Auth에 연결해야 함).
- 전 파일(35개) JS 문법·링크 재검증 통과(`shared/*.js` 두 파일도 별도 문법 검사 통과).
- **다음 단계(3단계)**: 식사 만들기~응답~확정 핵심 루프(`3-0`,`3-1`,`3-2`,`3-3`,`3-4`,`3-5`,`3-7`) + Realtime 구독.

## Supabase MCP 연결 + 기존 `_vc2608` 백엔드 발견, 그 위로 스키마 전환 (2026-08-17)

- **배경**: 사용자가 "슈퍼베이스 mcp연결되어있을걸? 같은걸로 만들고 있던게 있어"라고 언급. `claude mcp add`로 슈퍼베이스 MCP를 연결(`SUPABASE_ACCESS_TOKEN` + `--project-ref=pkucszwwnwpzvzqczmhh`)한 뒤 `list_tables`로 실제 프로젝트를 확인해보니, 여러 수업 과제 테이블(student, posts, cafes_260727 등)과 함께 **이미 만들어진 우리집식당 백엔드**가 `_vc2608` 접미사로 존재했음(households_vc2608, meals_vc2608, meal_responses_vc2608 등 11개 테이블, RLS·RPC 함수까지 갖춘 상태, 실제 데이터도 일부 있음 — 가구1·식사1·초대3건). 처음부터 새로 설계했던 `supabase/schema.sql`은 이 발견 전에 작성한 것이라 실제로는 사용하지 않음(참고용으로만 남겨둠).
- **결정**: AskUserQuestion으로 확인 — 기존 `_vc2608`을 이어서 확장하기로 함(새 스키마로 가지 않음).
- **핵심 충돌 발견 및 해결**: 기존 `_vc2608` 스키마는 `household_members_vc2608.profile_id`가 NOT NULL이라 계정 있는 사람만 가구 구성원이 될 수 있었음 — 이 프로젝트의 핵심 차별점인 "무가입 참여자"(PRD §12)와 정면으로 부딪힘. 다시 AskUserQuestion으로 확인 후 **기존 스키마를 확장하는 쪽으로 결정**(UX를 초대코드 방식으로 바꾸지 않음). `apply_migration`으로 다음을 반영(사용자의 명시적 "진행" 승인 받음 — 자동모드 분류기가 큰 DB 변경이라 한 번 막았음):
  - `household_members_vc2608.profile_id` nullable로 변경 + `name`/`avatar_url`/`color`/`default_response` 컬럼 추가, `profile_id IS NOT NULL OR name IS NOT NULL` 체크 제약.
  - `meal_participants_vc2608`/`menu_requests_vc2608`/`notifications_vc2608`/`meal_responses_vc2608`가 전부 `profiles_vc2608`(계정)만 참조하던 걸 `household_members_vc2608`(가구 구성원, 무가입 포함) 참조로 교체 — 기존 데이터는 백필, RLS 정책도 새로 작성.
  - `meals_vc2608.time` nullable화(시간 미정 지원), `menu`를 text → text[]로 변경(메뉴 여러 개 지원).
  - 새 도메인 테이블 9개 추가: `advance_answers_vc2608`, `weekday_defaults_vc2608`, `recurring_meals_vc2608`, `menu_poll_vc2608`, `guestbook_notes_vc2608`, `last_seen_vc2608`, `saved_places_vc2608`, `saved_guests_vc2608`, `milkit_orders_vc2608`.
  - Realtime 구독 대상 테이블 등록(meals/meal_responses/notifications/guestbook_notes/advance_answers).
  - 후속 마이그레이션 1건 추가: PRD §7.1 "1인1링크"(가족 구성원 개개인에게 고유 응답 링크)가 기존 스키마엔 없었음(`meal_share_links_vc2608`는 식사당 1개의 공용 손님 링크였음) — `meal_participants_vc2608.response_token_hash` 컬럼 + `get_participant_by_token_vc2608`/`respond_via_participant_token_vc2608` RPC 2개 추가로 해결. 손님용 공용 링크(`meal_share_links_vc2608`)는 그대로 남겨 "가구에 등록 안 된 진짜 낯선 손님"용으로 구분해서 씀.
  - `get_advisors(security)` 확인: 새로 추가한 함수들의 SECURITY DEFINER 경고는 기존 자매 함수들과 동일한 패턴(의도된 것). 다른 수업 테이블들의 RLS 미비는 이 프로젝트 범위 밖이라 손대지 않음.
- **`shared/supabase-client.js`/`shared/api.js` 전면 재작성**: 실제 프로젝트 URL/anon key로 교체, 모든 함수를 `_vc2608` 테이블·RPC 이름에 맞춰 다시 작성(예: 가구 생성은 `create_household_vc2608` RPC 호출, 프로필은 `profiles_vc2608`). 3-2에서 즉석으로 입력하는 "손님"은 `household_members_vc2608`에 `role:'guest'`로 등록해 가족과 동일한 참여자·1인1링크 메커니즘을 타도록 설계(5-1 목록에선 숨김).
- **`5-1`/`5-3` 수정**: 이미 짜둔 코드가 옛 테이블명(`profiles`)·역할 문자열(`co_admin`)을 쓰고 있어서 `profiles_vc2608`/`co-admin`(하이픈, 기존 컨벤션)으로 수정.
- **3단계 진행**: `3-2. 식사 만들기` 전체를 실제 DB 연동으로 재작성 완료(식사 생성/수정, 참여자 목록을 가구 구성원+즉석손님으로 구성, 반복일정·메뉴투표·자주가는곳·저장된손님 전부 실 테이블 연동, 요일별기본값·미리답변 캐시로 자동 체크).
- 전 파일(35개) JS 문법·링크 재검증 통과.
- **다음**: 3-1, 3-3(1인1링크 응답 화면 — 신규 토큰 스킴 적용), 3-0, 3-4, 3-5, 3-7, 3-8(공유 화면도 새 토큰 스킴으로), 3-9 순서로 계속.

## Vercel 배포 + 실사용 테스트 중 발견한 버그 2건 수정 (2026-08-17)

- **배경**: `ourfamily-restaurant_VC_26.08` 저장소를 Vercel에 배포하고 실제로 로그인·온보딩을 테스트하다가 "로그인 실패", "가구 생성 실패" 두 가지를 겪음. 둘 다 슈퍼베이스 로그(`query_logs`)와 직접 REST 호출 재현으로 근본 원인을 찾음.
- **버그 1 — 로그인은 성공하는데 "로그인 실패"로 뜸**: `getMyMember()`의 `household_members_vc2608 → households_vc2608` embed 쿼리가 PostgREST 에러(PGRST201, "관계가 모호함")를 던졌음. 원인: `last_seen_vc2608`이 `household_members_vc2608`과 `households_vc2608` 둘 다를 참조해서, PostgREST가 두 테이블을 연결할 FK 후보가 2개(직접 FK vs `last_seen_vc2608` 경유)라고 판단. 로그인 자체(Supabase Auth `/token`)는 실제로 200으로 성공했는데, 그 직후 이 쿼리가 에러를 던지고 `.catch()`가 이를 "이메일/비밀번호 확인" 메시지로 잘못 표시한 것. **조치**: `shared/supabase-client.js`의 select문에 FK 이름을 명시(`households_vc2608!household_members_vc2608_household_id_fkey`)해서 해결.
- **버그 2 — 가구 생성 실패(`households_vc2608_owner_id_fkey` 위반)**: `admin@admin.com` 계정이 `handle_new_user_vc2608` 트리거가 생기기 전(또는 다른 경로로) 만들어진 계정이라 `profiles_vc2608` 행이 없었음. `households_vc2608.owner_id`가 `profiles_vc2608(id)`를 참조하는데 그 행이 없어서 FK 위반. 조사 중 이런 "고아 계정"이 이 슈퍼베이스 프로젝트에 여러 개 있는 걸 발견(대부분 다른 수업 프로젝트의 테스트 계정). **조치**: `API.households.create()`가 가구 생성 RPC 호출 전에 `profiles_vc2608`을 self-heal upsert하도록 수정(있으면 무시, 없으면 생성) — 특정 계정 하나만 땜질하지 않고 앞으로 비슷한 계정이 또 나와도 자동 복구되게 함. DB에는 `profiles_vc2608`에 대한 `INSERT` 권한(GRANT)과 본인 행 삽입 RLS 정책을 추가로 부여.
- 두 수정 모두 커밋·푸시 완료, Vercel 자동 재배포됨.
- **아바타 버그**: `1-4`에서 사진 등록 후 이름을 바꾸면 사진이 사라지는 버그 발견·수정 — `avatar.textContent = ''`로 "아바타를 안 건드리려던" 코드가 실제로는 안에 든 `<img>` 자식까지 지워버렸음. 사진이 있으면 아예 손대지 않도록 수정.
- **버그 3 — 가족 구성원 등록 실패("permission denied for table household_members_vc2608")**: 1단계에서 새로 만든 도메인 테이블 9개(advance_answers·weekday_defaults·recurring_meals·menu_poll·guestbook_notes·last_seen·saved_places·saved_guests·milkit_orders)와 기존 테이블 2개(household_members_vc2608, notifications_vc2608)에 RLS 정책만 만들고 **테이블 자체의 GRANT(SQL 권한)를 `authenticated` 롤에 준 적이 없었음** — Postgres는 새 테이블 생성 시 자동으로 권한을 안 주고, RLS 정책은 GRANT가 있어야만 의미가 있음(둘은 별개 레이어). 그래서 클라이언트에서 이 테이블들에 직접 insert/update하는 모든 동작이 다 이 에러로 실패하고 있었음. **조치**: 위 11개 테이블에 필요한 GRANT(select/insert/update/delete)를 authenticated 롤에 부여. 직접 REST 호출로 재현·수정 확인 후 테스트용 행은 정리함.
- **참고**: 디버깅 중 `admin@admin.com` 계정에 가구가 2개 생겨있음(반복 테스트로 생긴 것) — 실사용에 지장 없지만 나중에 정리하고 싶으면 알려달라고 안내함.
