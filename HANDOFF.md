# 인수인계 자료 (2026-08-19 새벽 기준)

> 이 문서는 "지금까지 뭘 했고, 다음에 뭘 해야 하는지"를 빠르게 파악하기 위한 요약입니다. 개별 결정의 배경·근거는 `DECISIONS.md`(시간순 상세 로그)를 참고하세요.

## 지금 상태 한 줄 요약
우리집식당(35개 화면 정적 목업)을 Supabase 실 백엔드로 전환하는 작업이 **핵심 기능은 전부 완료**됐고, 지금은 참고문서의 9장 이미지 시안대로 UI 리디자인을 진행 중입니다.

## 완료된 것

### 1. 전 화면 실 DB 전환 (FEATURE_CATALOG.md "포함" 항목 전부)
- 온보딩·로그인(1-1~1-4, 2-1, 2-2)
- 핵심 조율 루프: 식사 만들기(3-2), 홈(3-0), 내 식탁(3-1), 참여자 응답(3-3, 1인1링크 토큰), 확정 전 요약(3-4), 완성 화면(3-5), 참여자 링크 공유(3-8), 무가입 응답 연결(3-9)
- 캘린더/통계: 홈 달력뷰(4-1, 4-3은 5-2에 흡수되어 아카이브)
- 가구 관리: 가구 설정(5-1), 식사 기록(5-2), 설정(5-3), 요일별 기본 응답(5-4)
- 8-1 밀키트 주문(제안형, 수익모델 핵심)
- **8-2 식사 사진 기록 + SNS 공유**(우선순위 최하로 미뤄뒀다가 2026-08-19 새벽 완료) — 실제 사진 업로드(Storage, canvas 축소·압축) + 브라우저 표준 Web Share API로 실제 공유 시트 연결
- 인증: 이메일/비밀번호 실동작. **카카오 소셜 로그인은 버튼만 있고 Kakao Developers 앱 등록이 안 되어 있어 미완성** — 외부 계정 작업이라 사용자가 직접 해야 함.

### 2. 백엔드(Supabase, 프로젝트 `pkucszwwnwpzvzqczmhh`, `_vc2608` 접미사)
- 신규 도메인 테이블 10개 이상 + RLS 정책 + GRANT (첫 배포 때 GRANT 빠뜨렸던 버그 이미 수정됨)
- 1인1링크 토큰 스킴(SHA-256 해시 저장), 손님 공용 링크, 무가입 참여자 계정 연결(§7.6 B방식) 구현
- **⚠️ 중요**: 이 Supabase 프로젝트는 **다른 실제 Next.js 개발 프로젝트와 공유**되고 있음. `household_invites_vc2608`, `webauthn_credentials_vc2608` 같은 테이블·`is_admin`류 함수는 그 프로젝트 것이니 **절대 손대지 말 것**.
- 2026-08-18 밤 성능 하드닝: FK 인덱스 22개 추가, RLS의 `auth.uid()` 행당 재평가 문제 8개 정책 수정(우리 테이블만).

### 3. 실제로 재현되고 고친 버그
- **홈 화면이 "-" 플레이스홀더에서 멈추는 버그**: `admin@admin.com` 테스트 계정이 가구 2개를 갖게 되면서 `getMyMember()`의 `.maybeSingle()`이 에러를 던져 전체 화면이 멈췄음. `getMyMember()`를 최신 가입 가구 1개만 반환하도록 수정 완료(근본 수정, 재발 안 함).
- 로그인 실패로 보였던 PGRST201 임베드 조인 모호성, 아바타 재업로드 400, 가구 생성 FK 위반 등 배포 초기 버그들 — 전부 수정 완료(자세한 내용 DECISIONS.md).

### 4. 문장 줄바꿈 규칙 (사용자 지시로 신설된 컨벤션)
여러 문장이 한 텍스트에 CSS 자동 줄바꿈으로만 이어지지 않도록, 문장 경계마다 `<br>`/`\n` 삽입. 앞으로 새 UI 문구를 쓸 때도 이 규칙 유지할 것(`.claude` 메모리에 `feedback_sentence_per_line`로 저장돼 있어 다음 세션에서도 자동 적용됨).

### 5. 리디자인 1단계 — 하단 탭 재구성 (진행 중, 방금 시작)
사용자가 `참고문서/목표/`의 9장 시안 이미지를 보고 "네비게이션에 식사등록이 없어 불편하다"고 직접 지적해서 착수:
- 하단 탭: 홈 · 내 식탁 · **+**(식사 만들기로 직행) · 달력 · **기록**(5-2, 신규 편입)
- 냉장고(7-1)·설정(5-3)은 하단 탭에서 빠지고, 3-0 상단의 새 햄버거 메뉴(전체 메뉴 슬라이드 패널)로 이동 — 패널엔 **실제로 존재하는 화면만** 연결(가구 설정/요일별 기본 응답/냉장고/밀키트/계정 설정)
- 5-3·7-1엔 뒤로가기 버튼 신규 추가(탭이 빠졌으니 필요)
- 완료, 커밋·푸시됨.

## 다음에 할 일 (우선순위 순)

1. **9장 이미지 리디자인 나머지 부분** — 아직 안 함, 범위 훨씬 큼:
   - 전체 색상 리브랜딩(현재 그레이 계열 → 참고 이미지의 초록 계열)
   - 음식 이미지 스타일(일러스트 → 실사 느낌)
   - 내 식탁(3-1)/달력(4-1)/기록(5-2) 각 화면의 상세 레이아웃 재구성(참고 이미지 `02_my_table.png`, `03_calendar.png`, `04_records.png` 참고)
   - **주의**: 참고 이미지 `07_household_selector.png`는 "가구 여러 개 전환 드롭다운"을 보여주는데, 지금 스키마·코드는 "사용자당 가구 1개" 전제(`getMyMember()`가 그렇게 짜여 있음) — 다중 가구 지원을 진짜 원하는 건지, 단순 UI 드롭다운만 원하는 건지 **사용자에게 반드시 확인 후 진행**.
2. **카카오 로그인 실제 연결** — Kakao Developers 앱 등록 + Supabase Auth Provider 설정(사용자가 직접).
3. **실제 브라우저로 전체 흐름 테스트** — 로그인→온보딩→식사 만들기→응답→확정→사진 기록까지, 이 세션에선 코드/DB 조회로만 검증했고 실제 화면을 직접 본 적은 없음.
4. **관리 필요**: `admin@admin.com` 테스트 계정에 남아있는 중복 가구(구성원 1명, 데이터 없음, household_id `7eb25b93-...`) — 코드 수정으로 더는 문제를 안 일으키지만, 지우고 싶으면 사용자가 직접(auto mode classifier가 삭제를 막아서 이 세션에서 못 지움).

## 어디를 보면 되는지

- **결정 로그**: `DECISIONS.md` — 시간순, 배경/조치 형식. 이 문서보다 훨씬 상세함.
- **기능 범위 결정**: `FEATURE_CATALOG.md` — 뭐가 포함/재검토/제외인지.
- **리디자인 참고 자료**: `참고문서/목표/*.png` (9장) — 아직 착수 안 한 나머지 리디자인의 기준.
- **배포**: GitHub `https://github.com/Yoomnoom/ourfamily-restaurant_VC_26.08`, Vercel에 배포되어 있음(Deployment Protection 꺼둔 상태로 공개 접근 가능).
- **검증 방법**: 코드 수정 후엔 항상 전체 35개 `code.html`의 JS 문법(`new Function` 파스)·상대링크 무결성을 체크하는 Node 스크립트를 돌려왔음(이 세션 안에서 매번 임시로 작성해 실행 — 별도 파일로 저장돼 있지 않으니, 필요하면 다시 작성해서 쓰면 됨. 패턴: 모든 code.html의 인라인 `<script>` 내용을 `new Function()`으로 파스 + `href`/`onclick location.href` 상대경로가 실제 파일로 resolve되는지 확인).

## 세션 진행 방식 (참고)
이 세션은 `/loop` 방식으로 자율 진행 중이었음 — 약 25분 간격으로 깨어나서 다음 작업을 이어감. 지금 사용자 요청으로 **일시 정지**한 상태. 다시 이어가려면 `/loop <하고싶은 작업>` 또는 그냥 이 문서를 참고해서 다음 우선순위부터 지시하면 됨.
