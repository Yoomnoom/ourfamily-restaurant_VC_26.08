# 인수인계 자료 (2026-08-19 새벽 기준, 계속 갱신 중)

> 이 문서는 "지금까지 뭘 했고, 다음에 뭘 해야 하는지"를 빠르게 파악하기 위한 요약입니다. 개별 결정의 배경·근거는 `DECISIONS.md`(시간순 상세 로그)를 참고하세요. 사용자가 자는 동안 `/loop`로 계속 작업 중이라 이 문서도 계속 갱신됩니다.

## 지금 상태 한 줄 요약
Supabase 실 DB 전환은 전부 끝났고, 지금은 **참고문서/목표/의 9장 이미지 시안 + 새로 설치한 `apple-design` 스킬** 기준으로 리디자인을 진행 중입니다. **온보딩(1-1~1-4)·로그인(2-1/2-2)은 사용자가 나중에 별도 요청한다고 해서 리디자인 범위에서 제외**했습니다.

## 완료된 것 (실 DB 전환 — 이전 세션분, 요약만)
- FEATURE_CATALOG.md "포함" 전체 화면 실 DB 전환 완료(온보딩~가구관리, 신규기능, 밀키트, 사진기록+공유)
- 실제 재현 버그 다수 수정(홈 화면이 "-" 플레이스홀더에서 멈추던 getMyMember() 버그, API.milkitOrders 중복 정의로 markOrdered가 사라졌던 버그 등)
- 성능 하드닝(FK 인덱스, RLS auth.uid() 재평가 문제) — 단, 이 프로젝트가 만들지 않은 다른 실제 앱의 테이블(`household_invites_vc2608`, `webauthn_credentials_vc2608` 등)은 손대지 않음(같은 Supabase 프로젝트를 공유 중)
- 문장 끝나면 줄바꿈 규칙 신설 및 적용(여러 문장이 한 텍스트에 이어붙던 곳 정리)
- 3-2(식사 만들기) 참여자 자동 체크에 온보딩 "평소 안 먹음" 설정 반영

## 진행 중 — 리디자인 (2026-08-19 새벽 시작)

**1단계 완료 — 하단 탭 재구성**: 홈 · 내 식탁 · **+**(식사 만들기로 직행) · 달력 · **기록**(5-2, 신규 편입). 냉장고(7-1)·설정(5-3)은 하단 탭에서 빠지고 3-0 상단의 새 햄버거 메뉴로 이동(실제 존재하는 화면만 연결). 5-3·7-1엔 뒤로가기 버튼 신규 추가.

**2단계 완료 — 색상 리브랜딩**: primary 색상 그레이(`#5d5e62`) → 그린(`#3f6b4f`). 온보딩·로그인 제외 21개 화면 전부 적용. (참고: `feedback_grey_palette_intentional` 메모리는 "그레이가 의도적이었다"는 옛 기록인데, 이제 그린이 명시적 지시라서 메모리도 갱신해둠 — 헷갈리지 말 것.)

**3단계 완료 — 애플 디자인 모션**: `active:scale-*` 눌림 피드백에 스프링형 타이밍(`cubic-bezier(0.34, 1.3, 0.64, 1)`) 적용. 21개 화면 동일 범위.

**다음 단계 (아직 안 함)**: 9장 이미지의 화면별 상세 구조 반영 —
- 내 식탁(3-1): 상태별 카드 구분(응답 필요/내가 만든 식사/확정된 식사)로 재편
- 달력(4-1): 미리 답해두기 카드를 더 전면에 배치
- 기록(5-2): "이번 달 한눈에" 통계 카드 스타일 다듬기
- 식사 만들기(3-2): 2단계 위저드 구조 검토(지금은 1화면)
- 상단 "우리집 ▾" 가구 선택 드롭다운: **장식만**(실제 다중 가구 전환 기능 아님 — 아래 참고)
- 실사 음식 이미지: 사용자가 자고 일어나서 직접 준비해서 주기로 함(추가 상세는 아래)

## 나중에 만들 것 — 기록만 해두고 지금은 스킵 (사용자 확인 완료)

- **다중 가구 지원** + **"오늘의 요리사" 전환 UI**: 참고 이미지 07의 가구 선택 드롭다운을 보고 "진짜 다중 가구 원하냐" 물었더니, 실제 의도는 "아빠가 요리할 때/엄마가 요리할 때"처럼 **같은 집 안에서 누가 만드는지 구분**하는 것도 포함이었음. 사용자가 "둘 다 맞지만 지금은 스킵, 아이디어만 기록"이라고 확답. 나중에 만들 때 방향: (1) 진짜 다중 가구는 `profiles_vc2608.active_household_id` 컬럼 추가 + 스위처 UI, (2) 오늘의 요리사는 이미 있는 `meals_vc2608.creator_id`(4-1 달력에서 색상 구분 중) 재사용.
- **메뉴 이미지 실제 생성(AI)**: `menu_default_images_vc2608` 테이블 + `menu-images` Storage 버킷 + `API.menuImages.get/save()` + 로컬 플레이스홀더(`foodPlaceholderHtml`, 메뉴 이름 해시 기반 이모지+색 타일) 인프라는 이미 만들어둠(5-2에 연결 완료). **실제 이미지 생성은 보류** — Gemini API 키를 테스트해봤는데 이미지 생성 모델(`gemini-2.5-flash-image`)은 무료 티어 할당량이 0이라 결제 없이는 불가능(OpenAI API도 ChatGPT Pro 구독과 별개라 마찬가지). **사용자가 직접 이미지를 여러 장 만들어서 줄 예정**("자고 일어나서 만들어줄게") — 받으면 `menu-images` 버킷에 업로드 + `menu_default_images_vc2608`에 `source='manual'`로 등록하면 바로 연결됨(메뉴 이름 매칭).

## ⚠️ 절대 하지 말 것
- Supabase 프로젝트(`pkucszwwnwpzvzqczmhh`)는 **다른 실제 Next.js 개발 프로젝트와 공유** 중. `household_invites_vc2608`, `webauthn_credentials_vc2608` 같은 테이블, `is_admin`류 함수 등 이 프로젝트가 만들지 않은 것들은 절대 손대지 말 것.
- API 키·비밀번호 같은 민감정보는 절대 git에 커밋하지 말 것(코드/커밋 메시지 어디에도). Supabase Edge Function 비밀값 등 서버 전용 저장소만 사용.

## 어디를 보면 되는지
- **결정 로그**: `DECISIONS.md` — 시간순 상세, 이 문서보다 훨씬 자세함.
- **리디자인 참고 자료**: `참고문서/목표/*.png` (9장).
- **배포**: GitHub `https://github.com/Yoomnoom/ourfamily-restaurant_VC_2608`(저장소 이름이 `_26.08`에서 변경됨, 기존 URL은 리다이렉트됨), Vercel 공개 배포 중.
- **검증 방법**: 코드 수정 후 항상 전체 35개 `code.html`의 JS 문법(`new Function` 파스)·상대링크 무결성을 체크하는 Node 스크립트 실행(별도 파일로 저장 안 해뒀으니 필요하면 다시 작성).

## 세션 진행 방식
`/loop` 방식으로 자율 진행 중 — 약 20~25분 간격으로 깨어나 다음 작업 이어감. 사용자는 "멈추지 말고 계속 돌려"라고 명시적으로 지시함 — 그만하라고 할 때까지 계속.
