# 인수인계 자료 (2026-08-21 기준, 계속 갱신 중)

> 이 문서는 "지금까지 뭘 했고, 다음에 뭘 해야 하는지"를 빠르게 파악하기 위한 요약입니다. 개별 결정의 배경·근거는 `DECISIONS.md`(시간순 상세 로그)를 참고하세요.

## 방금(2026-08-21, 커밋 da58fae까지) — 사용자가 스크린샷으로 지적한 버그 3건 수정 + 라이브 검증 완료

- **3-1(내 식탁) 가짜 통계 제거**: "이번 달 18번 함께 먹었어요/직접 조리 14·주문 4"와 "이번 주 집밥이 벌써 3번째예요/밀키트 정기배송으로..."가 전부 하드코딩된 가짜 숫자였음(후자는 존재하지도 않는 "정기배송" 기능까지 언급). `meals` 실데이터에서 이번 달 횟수를 실제로 세어 표시하는 걸로 교체, 가짜였던 두 번째 버튼은 완전히 제거(aada6bd).
- **FAB를 하단 네비 중앙으로 원복**: 이전에 사용자가 직접 요청해서 오른쪽 아래 FAB로 옮겼었는데(8ec4290), 실제로 써보고 "왜 오른쪽으로 바뀌었냐"고 재문의 → AskUserQuestion으로 "하단 네비 중앙으로 되돌리기" 확답받고 3-0/3-1/3-2/4-1/5-2 5개 파일에 `git show 8ec4290` 기준 원본 마크업 재적용(aada6bd). 이 과정에서 3-1에 FAB가 두 개(이전 버전 잔재 + 8ec4290 버전) 동시에 남아 겹쳐 보이던 실제 버그도 같이 발견·제거.
- **4-1(달력) 캐러셀 스크롤바 숨김**: "미리 답해두기" 5일 캐러셀에 두꺼운 회색 스크롤바가 그대로 노출되던 문제 → `#advanceCarousel`에만 `scrollbar-width:none`/`::-webkit-scrollbar{display:none}` 적용, 터치/드래그 스크롤 자체는 유지(da58fae). 5-4의 데이터 테이블은 스크롤 가능함을 알려주는 용도라 판단해 그대로 둠(스크롤바 숨김 미적용).
- **라이브 검증(Playwright, 3건 전부)**: 새 테스트 계정으로 가구 생성 → 3-0/3-1/3-2/4-1/5-2 전체에서 남은 FAB 0개 확인, 네비 중앙 버튼 클릭 시 정상 이동 확인, 3-1에 가짜 문구("18번"/"정기배송") 완전히 사라짐 확인, 4-1 캐러셀은 `scrollbarWidth:none`이면서 `scrollWidth(592)>clientWidth(568)`로 여전히 스크롤 가능함(scrollLeft 조작 시 실제로 이동)까지 확인 — 콘솔 에러 0건. 테스트 계정 2개 전부 Supabase SQL로 정리 완료(household_members→households→profiles→auth.users 순).
- **부가 발견 + 즉시 수정 — 실제 프로덕션 버그**: `handle_new_user_vc2608` 트리거가 신규 가입자 `avatar_url`을 한 번도 저장한 적이 없었음(name만 저장). 실제 카카오 로그인 사용자 3명(사용자 본인 포함)의 raw_user_meta_data엔 카카오 프로필 사진이 이미 있었는데 DB엔 전부 null이었던 실사용자 영향 버그 — 트리거에 `coalesce(avatar_url, picture)` 추가 + 기존 3명 즉시 백필 완료. 신규 카카오/구글 로그인부터는 자동으로 프로필 사진이 채워짐.
- **다음**: 접근성 3단계(구조 단순화)는 3-0만 완료된 상태 그대로, 나머지 화면은 필요시 계속.

## 방금(2026-08-20, 커밋 063ce5a까지) — 접근성 패스 3단계(구조 단순화) 시작

- 홈(3-0) 하단 부수 기능 5개(밀키트/식비예산/가구설정/요일별응답/기록바로가기)를 "더 보기" 토글 뒤로 접음 — 기능 삭제 없이 기본 화면 선택지만 축소. Playwright로 토글 동작·내부 버튼 여전히 존재함을 확인, 에러 0건.
- 3-1(내 식탁)/4-1(달력)은 이미 조건부 섹션 구조라 추가로 접을 게 없음 확인 — 불필요한 변경은 안 함.
- **다음**: 나머지 화면(특히 3-2 식사 만들기처럼 밀도 높은 화면)의 구조 단순화는 화면별 판단이 필요해서 계속 진행 예정. 구글 로그인은 여전히 외부 설정 대기 중(위 안내 참고).

## 방금(2026-08-20, 커밋 1f92956까지) — 보안 점검 + 신규 코드 정적 감사 통과

- 오늘 밤 새로 만든 SECURITY DEFINER 함수 2개(`create_household_vc2608`, `update_my_notification_channel_vc2608`)가 Postgres 기본 동작으로 PUBLIC(비로그인 포함)에 실행 권한이 열려있던 것 발견·수정. 내부 로직상 실제 악용은 불가능했지만(auth.uid() null 체크/NULL 비교) 원칙대로 authenticated로만 제한. 수정 후 실제 로그인 플로우 재검증 완료, 에러 0건.
- 오늘 밤 추가한 5개 기능(식성정보/밀키트배송/알림채널/그룹유형/구글로그인)의 신규 코드 전체를 Explore 에이전트로 정적 감사(모든 함수 정의·DOM ID·API 호출 매칭 확인) — **버그 0건, 전부 정상**.
- **지금 상태**: 6가지 예정 작업 사실상 전부 완료(구글 로그인은 외부 설정만 남음). 접근성 패스는 폰트·터치타겟 2단계까지 완료, 구조 단순화(3단계)는 화면별 판단이 필요해서 사용자 확인 없이 진행 안 함 — 다음에 사용자가 있을 때 방향 확인 후 진행.
- 테스트 계정은 매번 생성 직후 Supabase SQL로 정리, 현재 프로덕션에 잔여물 없음.

## 방금(2026-08-20, 커밋 053fc44까지) — 6가지 예정 작업 사실상 완료 + 식성 정보 신규 기능

- 사용자가 4가지 질문(밀키트 긴급성/알림채널/구글로그인/그룹유형)에 답변 → 전부 구현·검증 완료.
- **회원가입 식성 정보**(신규 요청): 알레르기/못 먹는 것/잘 먹는 것을 1-1 회원가입에서 입력, 5-3 설정에서 계속 추가·수정 가능(32caa47).
- **밀키트 배송 일정**: 가짜 긴급성 대신 실제 배송 리드타임 기반 "예상 도착일" 안내로 설계(053fc44).
- **알림 채널**: 카카오/슬랙/문자 선택 UI, 지금은 카카오만 실동작(ffd022a).
- **그룹 유형**: 가족/회사/기타 지원, 온보딩에서 선택 + 5-1에서 나중에 변경(a83adf5).
- **구글 로그인**: 버튼·코드 완료(8e02bf4) — **아직 실제로 안 됨, 외부 설정 필요**(아래 참고).
- 전부 Playwright로 라이브 검증(회원가입→저장→재로드 확인, 새 계정 생성→설정 변경→재로드 확인 등), 에러 0건. 테스트 계정 전부 정리 완료.
- **다음(사용자 액션 필요)**: 구글 로그인을 실제로 켜려면 (1) Google Cloud Console에서 OAuth 2.0 클라이언트 ID 발급(승인된 리디렉션 URI: `https://pkucszwwnwpzvzqczmhh.supabase.co/auth/v1/callback`), (2) Supabase 대시보드 Authentication → Providers → Google에서 활성화하고 위 클라이언트 ID/시크릿 입력. 이거 하시면 바로 동작함(코드는 이미 다 돼있음).
- 그 다음: 접근성 패스 3단계(구조 단순화, 아직 미착수 — 판단 필요해서 보류 중).

## 방금(2026-08-20, 커밋 6b3be7c까지) — 무한 루프 재개: 접근성 폰트 확대 1단계 완료

- 사용자가 "왜 중단했냐, 시켜놓은거 많은데" 라고 루프 재개 요청 → 접근성 패스를 화면 1개(홈)로 먼저 검증 후 나머지 25개 실사용 화면에 동일 적용(body-md 16→18px/headline-lg 20→23px/label-sm 13→15px/display-serif 24→26px). 커밋 aab243a(홈), e88b5f4(나머지 24개+로그인게이트 수동수정).
- Playwright로 홈/회원가입/식사만들기/달력 라이브 스크린샷 확인 — 텍스트 넘침 없음, 에러 0건.
- 배치 스크립트가 못 잡은 예외 2건 직접 수정: `4-1` label-sm이 원래 11px로 더 작았던 것, `2-1.로그인 화면`은 한 줄 압축 JSON이라 정규식이 안 먹혀서 수동 수정.
- `0. 확인용뷰어`/`6-1. 오류·복구 상태`는 개발자용 참고 도구라 접근성 대상에서 제외(실사용자가 안 봄).
- **미해결 사소한 이슈**: 이번 라운드 테스트 계정 2개가 Supabase SQL 조회에서 안 잡힘(MCP 재연결 여파로 추정, 실제 앱 동작엔 문제 없음) — 다음에 대시보드에서 한 번 확인 권장.
- 사용자가 밀키트 화면에 "가짜 데이터삭제 카운트다운으로 겁줘서 구매 유도" 패턴을 요청했는데, 다크패턴(허위 긴급성)이라 판단해 **정중히 거절**하고 대안 제시함(냉장고 재료 유통기한 기반 알림 / 오늘 식사 시간 임박 알림 / 제휴사 실제 프로모션 연동) — 사용자 응답 대기 중, 아직 착수 안 함.
- **다음 순서**: 접근성 패스 2단계(버튼/터치 타겟 크기, 구조 단순화) 계속, 이어서 6가지 작업 중 남은 1(알림채널)/2(구글로그인)/3(그룹유형).


- **6가지 예정 작업 중 4번(사용법 가이드) 완료**: 1-2(온보딩)와 5-3(설정) 양쪽에 동일한 점선 테두리 카드 모달 추가(a294a8d). Playwright로 양쪽 다 정상 확인.
- **핵심 아키텍처(무가입 손님 참여) 실제 e2e 검증**: 손님 추가→식사 생성→3-8 링크 추출→비로그인 브라우저로 접속→응답 시작→먹어요 선택→도착시간 질문까지 전부 정상 동작, 에러 0건.
- **6가지 예정 작업 현황**: 4/5/6번 완료. **남은 1(알림채널)/2(구글로그인)/3(그룹유형)은 외부 연동·스키마 설계가 필요해서 사용자 확인 없이 진행하기 어려움** — 다음 대화에서 방향 확인 필요(특히 1번: 슬랙/문자를 실제로 어떻게 발송할지).
- 이번 루프 세션 전체 요약: 헤더 통일 4개 화면 + 카카오 공유 실연동 + 시간기본값 버그 + 09_empty_home 완료(인사말+빈상태카드) + 실제 로직버그(다음 식사 표시) + 06 모두읽음 + 정적감사 실버그 1건 + 가이드 추가, 총 커밋 15개 이상. 테스트 계정은 전부 정리 완료(최종 회귀 테스트까지 에러 0건 확인 후 종료).
- **루프를 여기서 멈춘 이유**: 70대 고령층 접근성 패스(글씨·버튼 크게, 구조 단순화)는 36개 파일 전체에 영향을 주는 큰 디자인 작업인데, 이 세션 초반에 이미 한 번 "9장 이미지를 제 마음대로 해석해서 기계적으로 넓게 적용했다가 사용자가 되돌리라고 한" 사건이 있었음(`feedback_redesign_one_at_a_time` 메모리 참고) — 오늘 밤처럼 사용자가 자고 있어 화면을 봐줄 수 없는 상태에서 확인 없이 폰트 크기 등을 전체 파일에 기계적으로 밀어붙이면 같은 실수가 재발할 위험이 큼. 그래서 이번엔 시작하지 않고 여기서 멈춤 — **다음에 사용자가 있을 때 먼저 화면 1~2개로 방향(얼마나 크게? 어떤 화면부터?) 확인받고 진행할 것**.
- **다음 순서**: (1) 사용자 복귀 시 접근성 패스 방향 확인 후 진행, (2) 6가지 작업 중 남은 1(알림채널)/2(구글로그인)/3(그룹유형) — 셋 다 사용자의 결정/외부 계정 작업이 필요함.

## 방금(2026-08-20, 커밋 4c50d8f까지) — 무한 루프 세션 계속: 09 완료 + 실제 로직 버그 2건 수정

- **09_empty_home.png 완료**: 시간대별 인사말("좋은 OO이에요, 이름 님") 추가(01/09 둘 다에서 확인됐던 항목인데 누락돼 있었음), 오늘 식사 없을 때 전용 큰 카드(#tableHeroEmpty: 아이콘+안내+"오늘 식사 만들기" CTA)로 교체. Playwright 라이브 확인 완료(b326931).
- **실제 로직 버그 수정 — 6가지 예정 작업 중 5번**: 홈이 항상 오늘 중 가장 이른 시간 식사만 보여줘서, 아침 시간이 지나도 계속 아침 메뉴만 뜨던 문제. 아직 안 지난 식사 중 가장 빠른 걸 고르도록 수정(4c50d8f) — Playwright로 과거/미래 식사 둘 다 넣어서 실제로 미래 것이 뜨는지 검증 완료.
- **06_notifications.png**: "모두 읽음" 일괄 처리 추가(API.notifications.markAllRead + 4개 화면 알림 패널, c48ce0c).
- **정적 코드 감사(Explore 에이전트)로 실제 버그 1건 발견·수정**: 1-3 온보딩의 죽은 `onchange="handleProfilePhotoChange"` 참조 제거(f949357, 기능은 안 깨졌었지만 콘솔 에러 유발).
- **07_household_selector.png**: 다중 가구 전환 드롭다운은 실제 기능(다중 가구 지원)이 없어서 그대로 "준비 중" 유지 — 가짜 기능 금지 원칙, 이미 이전에 확정된 결정이라 재작업 없음.
- 9장 리디자인(03~09) 전부 완료. 테스트마다 만든 임시 계정/가구는 전부 Supabase SQL로 정리함, 현재 프로덕션에 테스트 잔여물 0건.
- **다음**: 70대 고령층 접근성 패스, 그 다음 6가지 예정 기능 중 남은 4개(알림채널선택/구글로그인/그룹유형/가이드설명 — 5번·6번은 완료).

## 방금(2026-08-20, 커밋 75f206a까지) — 무한 루프 세션: 실동작 검증 + 08 완료

- 사용자가 "그만하라고 할 때까지 무한루프, 진짜 필요한 것만" 지시 → Playwright로 배포된 실제 사이트를 회원가입→온보딩→홈→식사 만들기→카카오 공유→4탭 헤더까지 전부 구동해 런타임 에러 점검. **결과: 에러 0건**, 카카오 SDK 로드+초기화까지 실제 확인됨.
- Supabase 보안/성능 어드바이저 확인 — 실제로 깨진 것 없음(다른 프로젝트 테이블/의도된 anon 허용 함수뿐).
- 실사용 체감 버그 1건 발견·수정: 홈 빈 상태 문구 중복("아직 식사가 없어요" 반복) → 축약(63a68ad).
- **08_create_meal.png 완료**: 헤더 X(닫기) 버튼 + 제출 버튼 하단 고정바 이동(75f206a). Playwright로 라이브 재확인함(스크린샷 확인: X버튼/시간미정 기본체크/세그먼트 종류선택/하단 고정CTA 전부 정상).
- 테스트로 만든 임시 계정 2개는 Supabase SQL로 바로 정리함(household_members→households→profiles→auth.users 순).
- **다음**: 09_empty_home.png 진행 중. 그 다음 70대 고령층 접근성 패스, 그 다음 6가지 예정 기능(아래 "나중에 만들 것" 참고).

## 방금(2026-08-20, 커밋 76d20c8까지) — 하단탭 4개 화면 상단바 통일 + 시간 기본값 버그 수정

- 사용자 지적("홈,식탁,달력,기록 넘어가도 상단바 다 보이는데 왜 내 앱은 안보여?" → "알람이랑 햄버거도 보이잖아") 대응: 3-0(이미 있었음)을 기준으로 3-1·4-1·5-2에 동일한 헤더(로고+가구이름 알약+알림벨+햄버거)와 전체 메뉴 패널을 이식. 가짜 링크 없이 실제 화면(5-1/5-3 등)만 연결. 커밋 00fe0d1, 76d20c8.
- 4-1·5-2 Tailwind 설정에 없던 `error`/`display-serif` 토큰을 배포 전에 grep으로 미리 확인 후 추가(예전 WEEKDAY_NAMES 미스 이후 "쓰기 전에 존재 확인" 습관 계속 적용 중).
- 사용자 지적("새식사에서 시간지정되어있는게 이상함") 대응: 3-2 새 식사 만들기 시작 시 시간 hidden input 기본값(19:00)이 남아있어 아무것도 안 골랐는데 이미 정해진 것처럼 보이던 버그 → 새로 만들기 시작 시에도 '시간 미정' 기본 체크(수정 모드와 동일 로직). 커밋 3883824.
- 전체 36개 code.html 검증(0 에러/0 깨진 링크) 통과, 전부 커밋+푸시 완료.
- **다음**: 9장 리디자인 중 남은 07_household_selector.png(헤더 알약은 방금 작업으로 이미 반영됨 — 별도 확인 필요할 수 있음)·08_create_meal.png·09_empty_home.png 진행. 그 다음 70대 고령층 접근성 패스, 그 다음 사용자가 순서 지정한 6가지 기능(알림채널선택/구글로그인/그룹유형/가이드설명/홈 다음식사전환/식사만들기버튼위치).

## 방금(2026-08-19, 커밋 864509b까지) — 02_my_table.png(3-1) 리디자인

- 사용자 지시: "모양 똑같이 가져가야해... 색깔말고 모양말하는거야" — 참고 이미지를 정확한 비율/구조까지 그대로 따라야 함(단순 "느낌만 비슷하게" 금지). `feedback_redesign_one_at_a_time` 메모리에 이 원칙 추가 기록함.
- 02_my_table.png 기준으로 3-1 카드 전면 재구성(왼쪽 텍스트 55%/오른쪽 큰 사진 45% 풀블리드, 굵은 상태 헤더→시간줄→메뉴명→색 상태줄, 호스트 버튼을 테두리 버튼으로, "확정된 식사" 전용 렌더링 신설).
- **미해결 — 사용자 확인 대기 중**: 호스트 카드의 인원 요약바+전체 로스터 목록+TTS 스피커(참고 이미지엔 없음, 사용자가 예전에 요청해서 만든 기능)를 "응답 현황 보기" 화면(3-4)으로 옮길지, 지금처럼 카드 안에 유지할지 — 질문 남겨뒀는데 사용자가 잠들어서 답 없음. **그대로 카드 안에 유지한 채로 놔뒀음, 다음에 답 받으면 처리.**
- **실제 버그 발견**: scale-[2]/scale-150 준 사진이 담는 칸에 overflow-hidden이 없어서 확대된 부분이 옆 텍스트·아래 버튼 위로 번져 보이던 문제(3-0/3-1 둘 다) — 사진 칸 자체에 overflow-hidden 추가로 수정.
- **다음 순서**: 03_calendar.png부터 나머지 7장(03~09)이 남아있음 — 단, 각 이미지마다 이번처럼 "확실한 부분만 반영, 애매한 건 확인받기" 방식 유지. **사용자 확인 없이 다음 이미지로 넘어가지 말 것** — 02 하나에서도 실제 버그(overflow-hidden 누락, 확정 카드 없음) + 애매한 결정(로스터/TTS 위치) 둘 다 나왔음, 여러 장을 연달아 혼자 판단하면 오차가 누적됨.
- **8장 다 끝나면**: 사용자 지시대로 전체 앱을 70대 고령층도 쓸 수 있게 글씨/버튼 크게, 구조 단순화하는 별도 패스 진행(아래 "나중에 만들 것" 참고).

## 방금(2026-08-19, 커밋 068b7cc까지) 추가로 한 일

- **홈 오늘의 식탁 카드, 여러 차례 구조 재수정 끝에 현재 모습으로 정착**: (1) 원형 좌석 배치 → (2) 확인/대기 두 줄 → (3) 최종적으로 "왼쪽 텍스트(55%)/오른쪽 큰 사진(45%, 카드 오른쪽·위·아래 끝까지 풀블리드)" 구조. 사용자가 "3/4"이라고 한 건 카드 폭 비율이 아니라 **사진을 원본 그대로 안 보여주고 일부만 크롭**하라는 뜻이었음(재확인 완료) — 사진에 `scale-150`(3-0)/`scale-[2]`(3-1)로 확대해 반찬은 안 보이고 메인 요리만 보이게 크롭. 텍스트 wrap 문제(단어 중간에서 깨짐)는 Playwright로 폭을 실측해서 55%/45%로 해결, `word-break:keep-all` 추가.
- **음식 사진 41장 등록**: 사용자가 음식사진/ 폴더에 41개 메뉴 실사 사진을 추가로 넣어줌 → 전부 압축(Playwright, 1000px/JPEG0.85) 후 menu-images 버킷 업로드 + menu_default_images_vc2608 등록 완료. (버그 발견: Storage 오브젝트 키는 한글 불가 — sha1 해시로 키를 만들어 우회.) 앞으로도 파일을 더 주면 같은 스크립트 패턴으로 이어서 처리하면 됨.
- **대표 메뉴 1개→2개로 확장**: 3-2의 "대표"/"대표로"가 이제 상위 2개 기준. 3-0/3-1 모두 메뉴를 "OO 외 N개"로 뭉개지 않고 대표 메뉴 이름을 최대 2개까지 그대로 표시.
- **실제 버그 발견·수정 4건**(전부 사용자가 직접 써보다 발견):
  1. `meal_responses_vc2608`에 (meal_id, member_id) UNIQUE 제약이 원래부터 없어서 응답 저장(`respondSelf`)이 처음부터 실패하던 심각한 버그 — 3-1 응답 버튼/3-2 자동 확정/3-4 호스트 대신 응답 전부가 영향받고 있었음. DB에 제약 추가로 근본 수정.
  2. 메뉴를 한 번 정하고 나면 3-1에 수정 진입점이 아예 없던 문제 — 호스트에게 연필 아이콘 추가(→3-2 수정 모드로 이동).
  3. 날씨 배경을 사용자가 "삼지창"(=햄버거 메뉴 아이콘)이라 부른 곳으로 옮겨달라는 요청을 도구 오류로 놓쳤다가 재확인 후 반영.
  4. 3-2 메뉴 수정 시 알림이 전혀 안 가던 것 → 메뉴가 실제로 바뀐 경우에만 기존 attending 응답자에게 자동 알림(사용자 결정: 매번 물어보지 않고 자동 발송, 호스트 본인 제외).
- **범위 실수(교훈)**: 3-1 썸네일 확대(200%) 요청을 3-0 홈에도 확대 적용했다가 "홈은 수정하지마" 지적받고 롤백함 — 한 화면 요청을 다른 화면에 자동으로 넓혀 적용하지 말 것.

## 방금(2026-08-19, 커밋 63e54cd까지) — "빠르게 수정할 수 있는 것부터" 라운드

- 5-2(기록)에 음식군 통계 추가(menu_categories_vc2608 재사용) — "나중에 할 것" 항목 완료.
- 3-3(참여자 응답) 메뉴 표시를 3-0/3-1과 동일한 "대표 메뉴 최대 2개" 규칙으로 통일.
- git grep으로 전체 화면 raw time(DB time 컬럼) 노출부 전수 조사 → 3-3/4-1/3-2까지 전부 초단위 방어(slice(0,5)) 적용 완료. 이제 전체 화면에서 이 패턴 남은 곳 없음.
- 다음 후보(빠른 것부터): 나머지 8장 리디자인은 화면당 확인이 필요해 "빠른 수정"은 아님 — 카카오 공유하기는 외부 키 필요해서 사용자 액션 대기 중. 당장 더 빠르게 할 게 없으면 나머지 8장 리디자인을 하나씩 시작하는 게 다음 순서.

## 지금 상태 한 줄 요약
Supabase 실 DB 전환·알람 앱 4개 기능 이식 완료. 오늘 (1) 실제 음식 사진으로 3-0 홈 카드 재구성, (2) 사용자가 써보다 찾은 버그 3건 수정, (3) 3-2에 음식군/조리유형/세부태그 3단계 메뉴 카테고리 추가까지 완료. **카카오 공유하기(JS SDK)는 사용자가 "기록만 해두고 나중에" 라고 명시적으로 보류함** — 급하지 않지만 언젠가 진행할 일로 남아있음(아래 "나중에 만들 것" 참고). 9장 이미지 리디자인은 **사용자와 하나씩 맞춰가는 방식**으로 계속 진행 중.

## 방금(가장 최근) 세션에서 한 일 (2026-08-19, 커밋 a8079ea까지)

- **3-2 메뉴 카테고리 3단계 추가**: 음식군(한식/분식/중식/일식/양식/동남아/퓨전·기타 7개) → 조리·메뉴 유형(국물요리/밥/면/볶음·조림/구이·전/찜·수육/샐러드·무침/기타 8개) → 세부 태그(찌개/국/탕/전골/매운맛/고기/생선/채소/한 그릇/공유 메뉴, 다중선택) 순으로 좁히면 구체적인 메뉴 이름(62개 직접 작성한 DISH_CATALOG)을 추천. 고르면 메뉴에 추가되면서 카테고리가 `menu_categories_vc2608`(신규 테이블, `menu_default_images_vc2608`와 동일 패턴)에 저장돼 나중에(5-2 기록 통계 등) 재사용 가능 — **단 5-2 쪽 실제 통계 화면 반영은 아직 안 함, 다음에 할 일**.
- 기존 "저장된 메뉴에서 담기"(계란말이/나물무침/된장국 3개뿐이던 하드코딩)는 이 카테고리 브라우저로 대체됨.

## 방금 세션에서 한 일 (2026-08-19, 커밋 3fa4f21)

- **음식 사진 반영**: 사용자가 `음식사진/김치찌개.png`, `된장찌개.png` 제공 → Playwright로 압축(1000px/JPEG) → `menu-images` 버킷 업로드 → `menu_default_images_vc2608`에 `source='manual'`로 등록 완료. 이제 이 두 메뉴 이름으로 만든 식사는 실사 사진이 자동으로 뜸.
- **3-0 홈 카드 재구성**: 화면 꽉 채우던 원형 테이블 히어로 → `01_home_corrected.png` 구조대로 카드(왼쪽: 메뉴명·인원 현황·응답 현황 보기, 오른쪽: 작은 원형 식탁 사진+좌석) 로 변경. 사진 우선순위는 3-1과 동일(실제 식사 사진→등록된 메뉴 사진→이모지). "미응답자에게 다시 보내기"는 실제 알림 DB insert(가짜 버튼 아님).
- **버그 수정 3건(전부 사용자가 직접 써보다 발견)**:
  1. 내가 만든 식사인데 나도 응답 대기로 뜨던 문제 → 생성 시 본인 체크돼 있으면 자동 참석 확정(3-2).
  2. "NaN분 남았어요" → 근본 원인은 DB `time` 컬럼이 초 단위(`19:00:00`)로 오는 걸 모르고 `:00`을 한 번 더 붙였던 것(3-1). 표시 텍스트도 같이 정리.
  3. 3-8 참여자 링크 공유 화면에서 링크가 `%EC%B0%B8%EC%97%AC...` 퍼센트 인코딩으로 보이던 것 → 실제 링크는 정상 동작 확인됨(버그 아님), 표시만 `decodeURI`로 사람이 읽기 좋게 변경.
- **카카오 자동 전송 문의 응답 대기 중**: 지금 카카오 연동은 로그인(Supabase OAuth)뿐 — 카카오톡 메시지 발송 API와는 무관하고, 무가입 손님도 참여하는 구조상 완전 자동 발송은 불가. 대안으로 "공유하기"(카카오 JS SDK, 사람이 받는 사람 직접 선택)를 제안했고 사용자 답 대기 중 — 진행하려면 카카오 디벨로퍼스 앱 등록 + JS 키 필요.

## 완료된 것

### 1. 실 DB 전환 (이전 세션분)
FEATURE_CATALOG.md "포함" 전체 화면 실 DB 전환 완료. 실제 재현 버그 다수 수정(홈 화면 멈춤 버그, API.milkitOrders 중복 정의 버그 등). 성능 하드닝(FK 인덱스, RLS 최적화 — 이 프로젝트가 만들지 않은 다른 실제 앱 테이블은 손대지 않음). 문장 줄바꿈 규칙 신설. 3-2 참여자 자동 체크에 온보딩 설정 반영.

### 2. 리디자인 1~3단계 (색상은 되돌림, 나머지는 유지)
- 하단 탭 재구성(홈/내식탁/+/달력/기록, 냉장고·설정은 3-0 상단 햄버거 메뉴로) — **유지**.
- 애플 디자인(`apple-design` 스킬) 눌림 피드백 스프링 곡선 — **유지**. (첫 시도에서 CSS 콜론 이스케이프 버그로 전혀 작동 안 하고 있었던 걸 발견해 수정함.)
- 색상 그레이(`#5d5e62`)→그린(`#3f6b4f`) 리브랜딩 — **사용자가 되돌리라고 해서 그레이로 롤백함**. 9장 이미지를 제 마음대로 해석해서 넓게 적용한 게 문제였음(아래 "교훈" 참고).

### 3. 알람 앱(`E:\ai_바이브코딩\Yoom\수업21일차_알람`) 기능 이식 — 4개 전부 완료
사용자가 이전 수업 프로젝트에서 만든 알람 앱의 기능을 요청해서 이식:
1. **시간선택 아날로그 시계 다이얼** → 3-2(식사 만들기)의 시간 입력 교체. pointer 각도 기반 드래그, 순수 vanilla JS.
2. **TTS(음성 읽기)** → 3-1(내 식탁)에서 "가족의 응답여부"를 읽어주는 스피커 버튼. 네이티브 Web Speech API.
3. **실시간 시계 기반 카운트다운 + 알람** → 3-1에서 오늘 시간 정해진 식사마다 실시간 카운트다운, 시간 되면 알림창+소리(Web Audio 오실레이터)+진동.
4. **날씨 API + 배경 애니메이션** → 3-0(홈) 배경에 OpenWeatherMap 기반 날씨 애니메이션(맑음/비/눈/안개/천둥 등). API 키는 알람 앱과 동일한 것 재사용(무료 티어, 낮은 위험도, curl로 실제 동작 확인함).

모두 순수 HTML/CSS/vanilla JS라 이 프로젝트(빌드도구 없는 정적 사이트)에 그대로 포팅 가능했음.

## 진행 중 — 9장 이미지 리디자인 (방향 전환함)

**중요한 교훈**: 처음엔 9장 이미지를 제가 스스로 해석해서 "공통 방향"(그린 액센트 등)을 뽑아 전체 화면에 기계적으로 적용했는데, 사용자가 `01_home_corrected.png`를 실제로 비교해보고 "전혀 안 고쳐진 것 같다"고 지적함 — 색상 문제가 아니라 **레이아웃 구조 자체가 완전히 다름**(예: 3-0의 원형 테이블 히어로 방식 자체가 이미지와 다르고, "내가 할 일"/"앞으로 예정된 식사" 카드가 아예 없음 등). 그래서 **9장을 하나씩 같이 보면서 확인하고 진행하는 방식**으로 전환하기로 함(`feedback_redesign_one_at_a_time` 메모리에 기록).

**01_home_corrected.png 분석까지 완료, 사용자 확인 대기 중** — 발견한 구조 차이:
1. 상단바 "우리집 ▾" 드롭다운 (장식만, 실제 가구 전환 기능 아님 — 다중 가구는 스킵하기로 확정)
2. 시간대별 인사말("좋은 저녁이에요, 김영희 님")
3. "오늘의 식탁"이 카드 안에서 왼쪽 텍스트 정보 + 오른쪽 작은 원형 테이블 그림으로 나란히 배치(지금은 원형 테이블이 화면 꽉 채우는 히어로)
4. "내가 할 일" 카드(응답 안 한 인원 수 + "현재 인원으로 확정" 버튼) — 지금 없음
5. "앞으로 예정된 식사 N개" 카드("미리 답해두기" 바로가기) — 지금 없음
6. "가족이 먹고 싶어 해요" / "지난 식사 다시 만들기" 배치·스타일 차이

**다음에 할 일**: 사용자에게 이 목록이 맞는지 확인받고, 순서 정해서 하나씩 구현. 그다음 나머지 8장(02_my_table, 03_calendar, 04_records, 05_hamburger_menu, 06_notifications, 08_create_meal, 09_empty_home)도 같은 방식으로 하나씩.

## 나중에 만들 것 — 기록만 해두고 지금은 스킵

- **[9장 리디자인 + 고령층 패스 끝난 뒤] 사용자가 순서 지정한 6가지 큰 작업(2026-08-19, 2026-08-20 재확인)** — 사용자가 2026-08-20에 동일 목록을 다시 보내며 "루프 돌릴 때 이어서 시킬 것"이라고 명시함, 순서 그대로 유지:
  1. ~~알림 채널을 카카오 고정이 아니라 사용자가 선택~~ — **완료(2026-08-20, 커밋 ffd022a)**. 카카오/슬랙/문자 선택 UI, 실제 발송은 카카오만(사용자 확인: "지금은 카카오만").
  2. ~~구글 로그인 추가~~ — **완전히 완료(2026-08-21)**. 사용자가 Google Cloud Console에서 OAuth 클라이언트 발급 + Supabase Provider 활성화까지 마침. Playwright로 실제 "Google로 로그인" 버튼 클릭 → 정확한 client_id/redirect_uri로 accounts.google.com까지 정상 리다이렉트 확인.
  3. ~~가족 외 다른 그룹 유형 지원~~ — **완료(2026-08-20, 커밋 a83adf5)**. family/company/other, 온보딩에서 선택 + 5-1에서 나중에 변경. 다중 가구 전환(헤더 "우리집 ▾" 드롭다운)은 여전히 별개의 큰 기능이라 미착수 — group_type은 "지금 속한 가구 하나"의 속성으로만 우선 구현.
  4. ~~가이드 설명을 설정에서 다시 볼 수 있게, 점선(dashed) 스타일로~~ — **완료(2026-08-20, 커밋 a294a8d)**.
  5. ~~홈에서 식사 시간 지나면 다음 식사로 자동 전환~~ — **완료(2026-08-20, 커밋 4c50d8f)**. 카운트다운 알람 UI는 그대로 유지, "어떤 식사를 대표로 보여줄지" 로직만 수정.
  6. ~~"식사 만들기" 버튼을 하단 네비 중앙 → 오른쪽 아래로 이동~~ — **완료**(커밋 8ec4290, FAB로 이동 확인됨).
  9장 리디자인(03~09) 전부 완료. **다음 순서**: 70대 고령층 접근성 패스 → 남은 4개(1/2/3/4번).
- **고령층(70대) 접근성 단순화**: 사용자 지시(2026-08-19) — 9장 리디자인 끝난 뒤 진행. **1단계(글자 크기)·2단계(터치 타겟) 완료(2026-08-20)**. **3단계(구조 단순화)는 홈(3-0) 화면만 완료**(더 보기 토글, 커밋 063ce5a) — 나머지 화면(3-1/4-1/3-2/5-1)은 살펴봤으나 이미 정돈돼 있어 추가 변경 안 함. 앞으로 다른 화면에서 정리할 만한 부분이 보이면 계속 진행.
- ~~카카오 공유하기(JS SDK)~~ — **완료(2026-08-20)**. 3-8의 "카카오톡" 버튼을 실제 Kakao.Share.sendDefault()로 연결. 사용자가 카카오 디벨로퍼스 기존 앱에서 JavaScript 키 발급 + 배포 도메인을 JS SDK 도메인으로 등록 완료, 배포 후 curl로 반영 확인함. 완전 자동 발송(사람이 대상 안 고름)은 여전히 불가능하다고 안내함 — "공유하기"(사람이 대상 직접 선택)만 범위.
- ~~5-2 기록 통계에 메뉴 카테고리 반영~~ — **완료(2026-08-19)**. 종류(집밥/외식/배달) 막대 아래에 음식군 막대+범례 추가함.
- **다중 가구 지원 + "오늘의 요리사" 전환 UI**: 사용자 확답 완료("둘 다 맞지만 지금은 스킵"). 나중 방향: (1) `profiles_vc2608.active_household_id` 컬럼 + 스위처, (2) 이미 있는 `meals_vc2608.creator_id`(색상 구분 중) 재사용.
- **메뉴 이미지 실제 생성(AI)**: 인프라(`menu_default_images_vc2608` 테이블, `menu-images` 버킷, `API.menuImages`, 로컬 플레이스홀더 `foodPlaceholderHtml`) 이미 완성, 3-0/3-1/5-2에 연결 완료. 실제 생성은 보류 — Gemini 이미지 생성은 무료 티어 할당량 0(결제 필요 확인함), OpenAI API도 ChatGPT Pro와 별개라 마찬가지. **사용자가 실제 사진 2장(김치찌개/된장찌개)을 이미 줘서 반영 완료** — 앞으로도 사진을 더 주면 같은 방식(버킷 업로드 + `source='manual'` 등록)으로 바로 연결하면 됨.

## ⚠️ 절대 하지 말 것
- Supabase 프로젝트(`pkucszwwnwpzvzqczmhh`)는 **다른 실제 Next.js 개발 프로젝트와 공유** 중. `household_invites_vc2608`, `webauthn_credentials_vc2608` 등 이 프로젝트가 만들지 않은 테이블·함수는 절대 손대지 말 것.
- API 키·비밀번호는 절대 git에 커밋하지 말 것. (단, 알람 앱의 OpenWeatherMap 키처럼 원래 사용자 본인 프로젝트에 이미 공개돼 있던 무료 티어·저위험 키는 본인 확인 하에 재사용함 — Gemini/OpenAI 같은 유료/과금 가능 키와는 다른 판단.)
- **9장 이미지 리디자인은 한 번에 넓게 적용하지 말 것** — 이미지 하나씩 구체적으로 비교하고 확인받은 뒤 구현.

## 어디를 보면 되는지
- **결정 로그**: `DECISIONS.md` — 시간순 상세.
- **리디자인 참고 자료**: `참고문서/목표/*.png` (9장).
- **알람 앱 원본**: `E:\ai_바이브코딩\Yoom\수업21일차_알람` (이식 완료했지만 다른 기능도 더 있을 수 있음).
- **배포**: GitHub `https://github.com/Yoomnoom/ourfamily-restaurant_VC_2608`(저장소 이름이 `_26.08`에서 변경됨, 기존 URL은 리다이렉트됨), Vercel 공개 배포 중.
- **검증 방법**: 코드 수정 후 항상 전체 35개 `code.html`의 JS 문법(`new Function` 파스)·상대링크 무결성을 체크하는 Node 스크립트 실행(별도 파일로 저장 안 해뒀으니 필요하면 다시 작성).

## 세션 진행 방식
`/loop` 방식으로 자율 진행 중. 사용자는 "멈추지 말고 계속 돌려"라고 명시적으로 지시함 — 그만하라고 할 때까지 계속. 단, 큰 방향(색상, 리디자인 해석)에 대해서는 넓게 적용하기 전에 확인받을 것.
