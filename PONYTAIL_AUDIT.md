# 포니테일 감사 + 실제 서비스화 평가 (2026-08-16)

> 요청: "포니테일 기준으로 지금 만든 화면들을 평가해줘. 실제 서비스까지 갈 수 있는 흐름으로 냉정하게." 정답을 먼저 내지 않고 감사→구조 정리→PRD 필요성 판단→인터뷰 순으로 정리한다.

## 총평 (한 줄)

**핵심 루프(온보딩~기록)는 실제 서비스 후보로 무리 없이 견고하다. 하지만 이번 세션 후반부에 쌓인 것들(냉장고 AI, 로드맵 4종, 반복예약, 메뉴투표, 외식추천)은 "만들 수 있는지"를 탐색한 결과물이지, "출시에 반드시 필요한지"를 기준으로 검증된 게 아니다.** 포니테일의 1번 질문("이게 정말 존재해야 하나?")을 다시 던지면 상당수가 시기상조다.

---

## 1. 요소별 판정

### 1-1. 유지 — 핵심이고 실제로 필요함
`1-1~1-4`(온보딩), `2-1·2-2`(로그인), `3-0`(홈), `3-1`(내 식탁), `3-2`(식사 만들기), `3-3`(참여자 응답), `3-4`(확정 전 요약), `3-5`(완성), `3-7`(빈 상태), `3-8`(링크 공유), `3-9`(무가입 연결), `4-1`(달력).

이유: PRD §15 Must와 정확히 겹치고, "가족 안의 반복되는 조율 노동을 던다"는 §0.5 본질에 직접 기여한다. 여기엔 낄 자리가 없다.

### 1-2. 재검토 필요 — 만들었지만 v1에 꼭 있어야 하는지 증명 안 됨

| 항목 | 포니테일 질문 | 판정 |
|---|---|---|
| `7-1·7-2·7-3` 냉장고 AI 인식 | "사진 찍어 재고관리"는 그 자체로 **지속적 노동**이다. §0.5 "노동을 늘리지 않는다"와 정면으로 부딪힐 위험 — 실사용자가 매번 사진을 찍을지 검증된 적 없음(§13처럼 참여자 인터뷰 0건과 같은 문제). | 보류 후보 |
| `8-1~8-4` 밀키트·사진기록·예산·정산 | PRD가 스스로 "3차+ 확장"이라 분류. 이번에 "진짜 만들기 시작"한 건 **"만들 수 있다"의 증명**이었지 **"출시 때 있어야 한다"의 결정**이 아니었음. 특히 정산·예산은 §13 미결(가구 소유권, "가족 돈 문제 관계 리스크")이 안 풀린 채 실사용에 노출하면 위험. | v1 제외 유력 |
| 반복 식사 예약 / 미리 답해두기 / 메뉴 투표 / 외식 추천 | §15 Could 이하 티어. 아이디어 자체는 좋지만 검증된 반복 조율률(§14 최상위 지표) 없이 기능만 계속 늘어난 상태. | 이후 라운드 |

### 1-3. 죽은 무게 — 지우거나 아카이브해야 함

`100. 가족코드로 참여하기추가필요`, `3-6`(예전 전체화면 버전), `4-2`(예전 전체화면 버전), `5-5. 더보기`(방금 세션에서 스스로 은퇴시킴), `6-2`(예전 전체화면 버전) — **5개 폴더가 실제 네비게이션에서 도달 불가능한데도 프로젝트에 계속 쌓여 있음.** "나중에 참고용"이라는 이유로 늘어나는 중 — 포니테일 "나중은 나중이 알아서"(No boilerplate "for later") 위반. 이 파일들의 내용은 이미 `DECISIONS.md`에 텍스트로 남아 있으므로, 폴더 자체를 지워도 정보 손실은 없다.

### 1-4. 도구 자체의 복잡도

`0. 확인용뷰어`는 실제 제품이 아니라 우리끼리 쓰는 검토 도구인데, 흐름 분기(`FLOW_BRANCHES`)·순서정렬·묶음보기 같은 기능이 계속 늘었다. 이건 실제로 네비게이션 버그(뒤로가기, 홈/더보기 중복)를 잡는 데 유용했으니 방어 가능하지만 — "이 도구가 제품이 아니다"라는 경계는 분명히 해둬야 한다.

---

## 2. 구조가 "두서없다"는 지적에 대해

지금 36개 폴더가 번호는 있지만 카테고리 경계가 흐릿하다:
- `3-x` 안에 홈(3-0,3-1)·핵심루프(3-2,3-4,3-5,3-8)·응답(3-3,3-9)·예외상태(3-6,3-7)가 다 섞여 있고, 그중 2개(3-6, 3-7 아님 — 정정: 3-6)는 죽은 파일.
- `5-x`는 가구설정 계열인데 `5-5`(더보기)는 이제 죽음.
- `8-x`(로드맵)는 번호상 확장처럼 보이지만 실제 코드 완성도는 핵심 루프와 동급으로 다듬어져 있어 "이게 진짜 확장 단계인지" 시각적으로 구분이 안 됨.

**제안하는 정리 기준** (실행은 아래 인터뷰 답을 받은 뒤):
1. 죽은 5개 폴더 → 삭제(또는 `_archive/` 하위로 이동).
2. 살아있는 폴더를 실제 단계로 재그룹: `핵심(MVP)` / `확장(로드맵, v1 제외 가능)` / `도구`.
3. PRD를 이 재그룹 기준으로 다시 쓴다(3절 참고).

---

## 3. PRD를 새로 써야 하는가 — 그렇다

`참고문서/우리집식당_통합본_v0.5.md`는 지금 코드 현실과 상당히 어긋나 있다:
- `3-0`/`3-1` 분리(홈=공유 화면, 내 식탁=개인화 화면)가 PRD엔 없음 — PRD는 화면03/07을 한 화면으로 합치라고 했는데, 이번 세션에 다시 둘로 쪼갰다.
- 반복 예약, 미리 답해두기, 메뉴 투표, 외식 추천 — PRD 어디에도 없는 완전히 새 기능.
- 로드맵 4종이 "3차+ 확장"이라고 문서엔 쓰여 있는데 실제로는 이미 완성돼 있음.
- `DECISIONS.md`가 사실상 v0.5 위에 덧댄 패치노트 역할을 하고 있는데, 이대로면 실제 개발팀에 넘길 "단일 진실 소스"가 없다.

**결론: v0.6을 새로 쓰는 게 맞다.** 다만 무엇을 v1 스코프에 넣을지, 수익 모델이 뭔지, §13 미결을 지금 정할지는 전부 사용자가 답할 문제라 — 문서부터 먼저 쓰지 않고 아래를 먼저 묻는다.
