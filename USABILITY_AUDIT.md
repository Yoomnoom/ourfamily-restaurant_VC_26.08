# 워크플로우 중복 · 터치 드래그 버그 (2026-08-16, 사용자 스크린샷 제보)

- [x] **`3-1` 게스트 카드의 먹어요/안먹어요가 결정이 아니라 `3-3`으로의 리다이렉트였음**: 로그인한 가족이 홈 카드에서 이미 답을 눌렀는데도 `3-3`에서 같은 질문을 처음부터 다시 받음 — 클릭 수 과다. 카드에서 즉시 응답을 확정하도록 수정(`meal_responses`), `3-3`은 "바꾸기"로만 선택적으로 열리게 격하.
- [x] **`3-1` 게스트 카드가 "안 먹어요"로 이미 응답한 경우를 처리 안 함**: `status==='attending'`만 "이미 응답함" 처리해서 `absent` 상태는 매번 버튼이 다시 뜨던 버그. 같이 고침.
- [x] **`3-2` 대표 메뉴 변경이 HTML5 드래그&드롭 전용이라 터치 화면에서 사실상 동작 안 함**: 모바일 우선 앱에서 조용히 고장나 있던 핵심 기능. "대표로" 탭 버튼을 기본 수단으로 추가.

# 직관성 진단 (2026-08-16, 사용자 요청)

**전제**: 20~30대뿐 아니라 60~70대도 써야 함 — 디지털 문해력이 낮고, 작은 아이콘·숨겨진 레이어·다단계 탐색에 취약함을 기준으로 판단.

## 직관적이라고 판단되는 것
- 홈 카드의 오늘/내일 구분 + 메뉴·참여인원이 한 화면에 보이는 정보 위계
- "먹어요/안먹어요" 큰 2버튼 — 텍스트가 곧 행동이라 학습 불필요
- 온보딩의 "N/4단계" 진행 표시
- 확정 전 요약의 3그룹 분리(함께 먹어요/늦게 와요/확인 안 됨) — 라벨이 곧 상태

## 직관성이 떨어지는 것과 원인
1. **"모이는 장소"가 없는 깊은 네비게이션 체인**: `식비 예산`(8-3)에 가려면 홈→달력→(스크롤)→월요약통계→식비예산, 4단계. `정산`(8-4)·`밀키트`(8-1)도 각각 다른 화면에 파묻혀 있어 "이 기능이 어디 있었더라"를 기억해야만 다시 찾을 수 있음. 사용자가 반복 지적한 원인이 정확히 이것 — 토스·뱅크샐러드는 기능이 많아도 "전체" 탭 하나에 카테고리별로 다 모아둬서 처음 보는 사람도 훑으면 뭐가 있는지 파악됨.
2. **"더보기"라는 라벨의 무정보성**: 이름만 보고 안에 무엇이 있는지 전혀 유추가 안 됨(특히 노인 사용자는 "가구 설정을 찾으려면 더보기를 눌러야 한다"는 걸 시행착오로만 배움).
3. **숨겨진 레이어(슬라이드 패널·바텀시트)의 등장·소멸**: 알림 패널이 옆에서 슬라이드, 달력 상세가 아래서 슬라이드 — 젊은 층에겐 익숙한 패턴이지만, 이런 경험이 적은 사용자에겐 "화면이 갑자기 나타났다 사라진다"는 것 자체가 낯설고 불안할 수 있음. **정보가 항상 같은 자리에 그대로 있는(인라인) 편이 더 직관적**이라는 사용자 지적이 정확히 이 지점.
4. **달력의 월/주/오늘 탭이 장식뿐**: 눌러도 아무 반응이 없어 "고장난 버튼"처럼 느껴질 위험.
5. **아이콘 단독 사용**: 알림 벨, 뒤로가기 화살표 등 텍스트 라벨 없는 아이콘이 여러 곳에 있어, 아이콘 관습에 익숙하지 않은 사용자는 의미 유추가 어려움.
6. **핵심 정보를 보려면 항상 "들어가야" 함**: 홈 화면만 봐서는 이번 달 식비가 얼마인지, 정산할 게 있는지 전혀 안 보임 — 토스는 홈 최상단에 핵심 숫자(자산 등)를 항상 노출.

## 해결 방향 (이번 라운드에서 실행)
- [x] **"더보기"를 실제 대시보드로 재구성**: 카테고리(가구·기록/통계·우리집 살림·설정)로 섹션을 나눠 지금까지 파묻혀 있던 `4-3 통계`·`8-3 식비예산`·`8-1 밀키트`·`5-4 요일별 기본 응답`을 전부 한곳에 모음.
- [x] **달력 팝업 → 인라인 패널**: 바텀시트를 없애고, 날짜를 누르면 달력 바로 아래 항상 있는 영역에 그날 내용이 표시(마스터-디테일 구조, 레이어가 나타났다 사라지지 않음).
- [x] **달력 월/주/오늘 탭을 실제로 동작하게 구현**: 월=전체 그리드, 주=오늘이 속한 주 7칸, 오늘=오늘 하루만.
- [x] **홈 화면에 핵심 요약 위젯 추가** — 두 갈래로 해결됨: `3_핵심조율 3-0. 홈`에 아고다식 타일 그리드를 추가해 식비예산·통계·기록 등에 탭 한 번으로 닿게 했고, `3_핵심조율 3-1. 내 식탁`엔 "이번 달 N번 함께 먹었어요" 요약 위젯이 있음(§2 "MVP는 횟수까지" 원칙에 맞춰 금액이 아니라 횟수로).

---

# 사용성 점검 기록 (2026-08-16)

4개 에이전트가 28개 화면을 병렬 점검(온보딩/로그인, 핵심루프 전반, 핵심루프 후반+달력, 가구부가+냉장고). 5개 디바이스 폭(iPhone SE 375 / iPhone 14 390 / Galaxy S 360 / 태블릿 834 / PC 1280) 기준.

각 항목: [ ] 미수정 / [x] 수정완료 / [-] 의도적 보류(이유 명시)

## P0 — 즉시 수정 (심각도 높음, 광범위)

- [x] **`spacing` 테마 설정 누락**: 실제로는 16개 파일에서만 확인됨(2-1은 따옴표 없는 키 스타일이라 처음엔 오탐, 재확인 결과 이미 정상 정의돼 있었음). 3-5,3-6,3-7,3-8,3-9,4-1,4-2,4-3,5-1,5-2,5-3,5-4,6-2,7-1,7-2,7-3 — 스크립트로 일괄 수정 완료, 색상/설정 자동검증 재통과.
- [x] ~~`2_로그인 2-1.로그인 화면`의 핵심 CTA 미작동~~ — **오탐 정정**: 실제로는 `100. 가족코드로 참여하기추가필요`에 있는 버튼이었음(점검 에이전트가 파일 착각). `2-1`의 실제 CTA("식당열기")는 정상 작동 확인. `100` 폴더는 애초부터 실제 플로우에 연결되지 않은 폐기된 초기 프로토타입이라(어디서도 링크 안 됨, 전체 링크 감사에서도 확인됨) 버튼이 죽어있어도 실사용에 영향 없음 — 아래 "의도적으로 수정하지 않는 것"으로 이동.
- [x] **`4-1` 달력 셀 텍스트 오버플로**: `overflow:hidden`+`text-overflow:ellipsis`+`white-space:nowrap` 추가, 셀 자체도 `overflow:hidden`으로 감싸 아래 행 침범 방지.
- [x] **`1-4` 사진추가(20px)·삭제(28px) 버튼**: 삭제 버튼은 36px로 확대. 사진추가 배지는 아바타(40px) 대비 비율상 그대로 두되, **아바타 전체를 클릭 가능하게** 만들어 실질 터치 영역을 40px로 확대(배지는 시각적 표시로만 유지).
- [x] **`4-1` 안내문구-동작 불일치**: 식사 없는 날짜도 클릭 가능하게 하고 "이 날은 계획된 식사가 없어요" 빈 상태를 보여주도록 수정(문구를 낮추는 대신 실제 동작을 문구에 맞춤).

## P1 — 수정 (중간~높음, 반복 패턴)

- [x] 뒤로가기 아이콘 버튼 `w-9 h-9`(36px) → **19개 파일 스크립트 일괄 44px(`w-11 h-11`)로 확대**.
- [x] `3-1` 알림벨/프로필(40px) → 44px로 확대. 뷰 토글(32px)은 세그먼트 컨트롤 특성상 낮은 우선순위로 보류(아래 참고).
- [-] `3-1` FAB·알림패널이 데스크톱/태블릿에서 중앙 본문과 분리되어 보이는 것 — DESIGN.md의 "app-like intimacy" 의도된 좁은 컨테이너 설계와 맞물린 구조적 이슈라, 지금 범위에서 레이아웃 재설계 없이 고치기 어려움. 낮은 우선순위로 보류.
- [-] `3-1` FAB와 카드 액션 텍스트 겹침 위험 — 실측 확인 결과 `pb-28`(112px)이 FAB 영역(84~140px)과 일부 겹치나 카드 마지막 요소는 링크 텍스트라 실제 클릭 방해는 제한적. 낮은 우선순위로 보류.
- [x] `3-2`/`3-4` 토스트가 하단 고정 액션바와 겹치는 문제 — 토스트 `bottom` 오프셋을 액션바 높이만큼 올림(100px/120px).
- [x] `4-1` 날짜 클릭 불일치 — 식사 없는 날짜도 클릭 가능하게 하고 빈 상태 표시(P0에서 함께 수정).
- [-] `1-4` 구성원 행 이름 입력칸 압박 — 삭제 버튼을 36px로 키우면서 약간 더 압박됐으나, 실제 이름 입력은 4~6자 내외가 대부분이라 실사용 영향 낮다고 판단, 보류.
- [x] `3-8` 3버튼 줄바꿈 위험 — 2행 배치(복사+카카오톡 / 미리보기 전체폭)로 재구성.
- [x] `7-1` 통계 카드 라벨 줄바꿈 위험 — 라벨을 짧게("소비 임박", "오늘 소진") 줄이고 `whitespace-nowrap` 추가.
- [x] `5-4` 요일별 버튼(36px, 21개 밀집) — 44px(`w-11 h-11`)로 확대, 테이블 `min-width` 500px로 조정.
- [-] `5-3` 토글 스위치 높이 26px — iOS/Material 표준 토글 비율(폭:높이 ≈ 1.7:1)을 유지하려면 26px가 자연스러운 값이라, 세로 오차 허용범위(주변 여백 포함 실제 탭 영역은 더 넓음)를 고려해 보류.
- [x] `3-7` 헤더에 알림벨 추가 — 처음엔 6-2(전체화면)로 연결했다가, 이후 아카이브 정리 중 발견해 3-0/3-1과 동일한 인라인 슬라이드 패널로 통일함(2026-08-16, `_archive` 정리 참고).
- [x] `1-1`/`1-3` 프로필 사진 변경 안내 — hover 텍스트 외에 항상 보이는 카메라 배지(1-4와 동일 패턴)를 추가.
- [x] `1-1`/`2-2` 비밀번호 표시/숨김 버튼 — 44px(`w-11 h-11`) 터치 영역 추가.
- [-] `4-2` 불필요한 `pb-28` — 실제 화면엔 존재하나 영향이 경미하고(스크롤 여백만 약간 김), `4-2`는 이미 실제 흐름에서 안 쓰이는 참고 전용 화면이라 우선순위 낮음, 보류.

## P2 — 낮은 우선순위 / 검토 필요 (보류)

- [-] `6-1`(오류·복구 상태)만 데스크톱 지향(`max-w-[1400px]`) — 참고 시트 목적상 의도적, 보류.
- [-] `7-2`만 `max-w-[480px]` — 단일 업로드 플로우라 좁은 폭이 오히려 자연스러움, 보류.

## 요약

P0(4건) 전부 수정, P1(19건 중 13건 수정, 6건 근거와 함께 보류), P2(2건 의도적 보류). 전체 링크·색상토큰 자동 재검증 통과(2026-08-16).

## 의도적으로 수정하지 않는 것 (버그 아님)

- **`100. 가족코드로 참여하기추가필요`가 `2-1`과 완전히 동일한 코드**: DECISIONS.md 기존 결정("가족 코드로 참여하기"는 §13 미결이라 실제 구현 안 함, 참고용으로만 폴더 유지)에 따라 의도적으로 미구현 상태. 실제 코드 입력 UI를 새로 만들지 않음.
- **모든 화면이 `max-w-[420~600px]`로 제한되어 태블릿/데스크톱에서 좌우 여백이 큰 것**: DESIGN.md에 명시된 의도적 설계("Fixed-width central container to maintain app-like intimacy") — 버그 아님.
- **`3-6`/`4-2`/`5-5`/`6-2`/`100`이 실제 흐름에서 더 이상 링크되지 않는 것**: 인라인 오버레이·타일 그리드 등으로 대체된 뒤, 2026-08-16에 `_archive/`로 한곳에 모았음(삭제 아님). 뷰어의 "참고 전용 (아카이브)" 그룹에서 계속 열어볼 수 있음.
