# 철학 정합성 진단 (2026-08-16)

> 사용자 질문: "이 프로젝트는 뭐가 메인이라고 생각해? 위치와 디자인이 철학 기반으로 결정돼야 하는 거 아냐?"
> 정답을 알려주지 않고 스스로 훑어서 방향을 고치라는 요청에 따라 작성.

## 이 질문에 대한 내 답

메인은 참고문서 §0.5 그 자체다: **"묻지 않아도 아는 집"** — 가족 안의 반복되는 조율 노동을 덜어주는 것, 그리고 그 근본에 있는 "함께 먹는 일은 관리할 대상이 아니라 돌보는 일"이라는 믿음. 로드맵 기능을 얼마나 많이 쌓았는지, 사용성 점수가 얼마나 좋은지, 토스처럼 정보를 잘 모아놨는지는 전부 **이 철학이 지켜지고 있을 때만** 의미가 있는 2차 기준이다.

이번 세션을 되짚어보면, 나는 이 순서를 뒤집어왔다. "MoSCoW에 있나", "통계로 뽑히나", "사용성이 좋나", "토스처럼 편한가"를 먼저 묻고 실제로 그렇게 실행했지, §0.5 마지막 문단이 판단 기준으로 못박은 **"이게 이 철학에 맞나?"를 먼저 묻는** 습관은 느슨해졌다. 아래는 그 결과로 실제 쌓인 이탈들을 요소별로 뜯어본 기록이다.

---

## 요소별 점검

### 1. 색 — 진단 오류였음 (사용자 확인 후 정정, 2026-08-16)

> **정정**: 아래 진단은 틀렸다. 사용자 확인: "색은 내가 의도한거야. 무채색 M3 그레이 팔레트가 맞아... 라벤더 색인건 괜찮지만 너무 ai스럽고 촌스러워서 다 뺀거야." 즉 `2-1`이 라벤더/파스텔 토큰을 갖고 있었던 건 초기 생성 잔재였고, 이후 27개 파일이 무채색으로 간 게 오히려 **의도적인 수정**이었다. PRD §11의 "라벤더" 문구를 문자 그대로 따르는 것보다 사용자의 실제 심미적 판단(파스텔이 AI스럽고 촌스러워 보인다는 것)이 우선한다 — 문서 문구와 실물 판단이 부딪히면 실물 판단이 이긴다. 아래 진단 내용과 그에 따라 실행했던 34개 파일 색상 재매핑은 **전부 원복**했다(같은 스크립트 방식으로 원래 M3 그레이 hex 값 재적용, `revert_to_grey.js`). 이번 항목은 "이렇게 잘못 짚었었다"는 기록으로만 남긴다.

<details>
<summary>정정 전 원래 진단 (참고용, 실행되었다가 되돌려짐)</summary>

### 1. 색 — 가장 크고 근본적인 이탈 (정정됨 — 위 참고)

- **PRD 근거(§11):** "우리 컬러 = 파스텔 카드 톤: **라벤더 배경** + 흰 카드 + **노랑/주황/파랑** 파스텔 태그... 색의 의미: 노랑=오늘/가까움, 주황=이후, 파랑=주의(늦음)·강조, 회색=중립. 감정 화면(완성·빈 상태)만 크림·테라코타 오일파스텔+손글씨."
- **실제 상태(수정 전):** 내가 이 세션 초반에 만든 "Warm Table" 시스템은 Stone Grey 배경 + 완전 무채색 M3(Material 3) 그레이 팔레트였다. 라벤더도, 노랑/주황/파랑 의미색도 전혀 없었다. `2_로그인 2-1.로그인 화면`(가장 먼저 만들어진 파일)만 예외적으로 `bg-lavender`, `status-today-yellow`, `status-action-blue` 같은 PRD에 맞는 토큰을 실제로 갖고 있었는데, 이후 만든 27개 파일이 전부 이걸 버리고 새 무채색 시스템으로 갈아탔다. 즉 **PRD가 명시한 색이 이미 존재했는데 내가 그걸 무시하고 새로 만든 것.**
- **판정:** 명백한 이탈. DECISIONS.md에 "GPT/Manus 참고자료의 비주얼은 기능만 참고" 라고 스스로 적어뒀지만, 그 근거였던 §0 "레퍼런스와 우리 것을 혼동하지 않는다"는 "외부 앱의 테라코타는 우리 색이 아니다"라는 뜻이지 "PRD 자체가 지정한 라벤더/파스텔까지 버려라"는 뜻이 아니었다. 스스로 오독했다.
- **조치(이번에 실행):** 전 파일(34개) 공통 hex 값을 스크립트로 일괄 재매핑 — 무채색 그레이 계열 → 라벤더 틴트, `secondary`(회색) → 주황(이후) 의미색, `tertiary`(크림) → 노랑(오늘/가까움) 의미색으로 재정의. `2-1`의 테라코타 잔재(`accent-terracotta`)도 라벤더 프라이머리로 통일. 단, `3-5`(완성)·`3-7`(빈 상태) 두 감정 화면은 §11이 명시한 대로 "크림" 톤을 별도로 유지(노랑 배지와 구분되도록 파일별 로컬 오버라이드).
- **남은 일(제안):** 파랑(주의·늦음)은 기존 팔레트에 아예 없던 색이라 이번엔 토큰만 못 넣었다. "좀 늦어요" 상태(3-1 roster, 3-4 그룹, 3-3 도착시간 칩)에 실제로 파랑 강조를 입히는 건 각 파일 로직을 들여다봐야 하는 별도 작업이라 다음 라운드로 남긴다. 또한 인물 식별색 중 박철수(#9b8ab5, 라벤더 퍼플)가 새 배경색과 채도가 비슷해 구분이 흐려질 수 있어 재조정을 검토할 만하다.

</details>

### 2. "위계를 만들지 않는다" — 관리자/구성원 라벨

- **PRD 근거(§0.5, §5):** "위계를 만들지 않는다... '운영자/참여자'로 사람을 나누지 않고, 역할은 식사마다 유동적이다." "개설자용 앱과 참여자용 앱을 분리하지 않는다... 역할은 계정에 고정되지 않고 식사별 관계·행동에 따라 결정된다."
- **실제 상태(수정 전):** `5_가구설정 5-1. 가구 설정`의 구성원 목록에서 로그인한 본인에게는 "관리자", 나머지 전원에게는 "구성원"이라는 고정 뱃지를 붙이고 있었다. `household_members.permission`이라는 데이터 필드 자체는 PRD에도 있지만(§6.1, 가구 초대·내보내기 권한용), 그걸 사용자 얼굴에 "관리자"라는 신분 라벨로 노출하라는 근거는 없다. 확인용뷰어의 페르소나 설명도 "가입한 개설자 (관리자)"라고 적어놔서, 내가 이 프로젝트를 다루는 멘탈모델 자체가 은연중에 "관리자 vs 구성원" 위계로 굳어 있었다.
- **판정:** 명백한 이탈.
- **조치(이번에 실행):** `5-1`에서 "관리자/구성원" 뱃지 제거(이미 있던 "(나)" 표시만으로 본인 식별에 충분). 확인용뷰어의 "관리자" 표현도 "가구를 처음 만든 사람"으로 바꾸고, 설명에 "역할은 식사마다 정해진다"는 §5 근거를 직접 붙였다.

### 3. "주문 시스템을 거부한다" — 밀키트 화면이 그대로 이커머스 체크아웃이 됨

- **PRD 근거(§0.5):** "재미를 위한 추가 입력·**주문 시스템**·필수 사진을 밀어낸다." §15 MoSCoW Won't: "주문 관리·주문번호·주방 칸반 · **결제·매출** · 수량 주문·필수 옵션." §16 1차 확장 원칙: "확정된 식사 뒤에만 제안" — 시스템을 만들라는 뜻이 아니라 제안 타이밍 얘기다.
- **실제 상태(수정 전):** `8_로드맵확장 8-1. 밀키트 주문`이 정확히 "가격 12,900원 표시 → 배송 날짜 선택 → 도착 희망 시간 선택 → 배송지 → 주문하기 → 주문 완료"라는 완결된 커머스 체크아웃 플로우였다. PRD가 스스로 "주문 시스템을 거부했다"고 명시한 그 문장을 정면으로 위반한 셈이다. "제휴"라는 단어를 "우리 앱 안에 자체 주문 시스템을 만드는 것"으로 잘못 번역했다.
- **판정:** 명백한 이탈.
- **조치(이번에 실행):** 배송 날짜/시간 선택 UI와 "주문하기→주문완료" 자체 체크아웃을 제거. 대신 "이 레시피에 부족한 재료"와 참고 가격만 보여주고, CTA를 "제휴사에서 보기"로 바꿔 실제 주문·배송은 외부 제휴사에서 일어난다는 걸 화면에 명시("우리집식당은 조율만 도와요"). 우리 앱은 제안까지만 하고 실행은 넘긴다는 원래 취지에 맞춤.

### 4. §2 "MVP는 횟수까지만, 금액은 확장" — 내가 방금 전 턴에 스스로 어긴 원칙

- **PRD 근거(§2):** "MVP 성공 기준에는 통계·자동 제안을 포함하지 않는다. MVP에서는 집계 가능한 데이터 구조를 깔고, **가벼운 월 요약(횟수)까지만 노출한다. 금액·자동 제안은 확장.**"
- **실제 상태(수정 전):** 바로 직전 턴에서 "토스처럼 핵심 정보를 홈에서 바로 보여주자"는 좋은 의도로 `3-1` 홈 화면에 "이번 달 식비 · N원 / 예산 N원" 위젯을 추가했다. 이건 정확히 "홈 화면엔 횟수만, 금액은 확장 단계"라는 원칙을 내 손으로 어긴 사례다 — 사용자가 로드맵 항목(정산·예산 화면 자체)을 만들라고 스코프를 넓혀준 것을, "그러니 홈 화면 전면에 금액을 노출해도 된다"로 확대 해석했다.
- **판정:** 자기모순. 좋은 UX 아이디어(핵심 정보 상시 노출)를 잘못된 데이터(금액)에 적용했다.
- **조치(이번에 실행):** 위젯을 "이번 달 18번 함께 먹었어요 · 직접 조리 14 · 주문 4"로 교체(§2가 허용한 "횟수" 데이터, `4-3. 월 요약 통계`와 동일한 숫자 재사용, 새 계산 로직 없이). 링크도 `8-3`(예산)에서 `4-3`(월 요약 통계)으로 변경.

### 5. §13 미결 — "가족 돈 문제 관계 리스크"를 안전장치 없이 구현

- **PRD 근거(§13-7, §16 4차):** "식비 확장 시 공개 범위 기본값 — 가족 돈 문제 관계 리스크." "4차(정산) 원칙: 공개 범위 보호 선결."
- **실제 상태(수정 전):** `8_로드맵확장 8-4. 정산`이 "총 비용 → N명이 X원씩 균등 분담 → 미정산/정산완료 토글"을 아무 고지 없이 그대로 노출. 이 정보를 누가 볼 수 있는지, 잘못 눌리면 어떻게 되는지에 대한 언급이 전혀 없었다. §13이 "확정 전 미결"이라고 명시한 사안을 마치 이미 해결된 것처럼 구현한 셈.
- **판정:** 이탈까지는 아니지만(로드맵 4차 화면 자체는 사용자가 명시적으로 요청한 스코프 확장), 미결 상태를 숨긴 채 완결된 것처럼 보여준 건 정직하지 못했다.
- **조치(이번에 실행):** 화면에 "이 가구 구성원끼리만 볼 수 있어요 · 실제 송금은 각자 알아서 해요"라는 고지 문구를 추가해, 지금은 어떤 전제로 동작하는지 최소한 사용자에게 투명하게 보이게 함. 완전한 해결(§13-7 자체의 확정)은 여전히 기획 단계에서 결정할 문제로 남아있음.

### 6. §5.1 카드 정보 우선순위 — 순서가 뒤바뀜

- **PRD 근거(§5.1):** "카드 정보 우선순위: **메뉴 → 시간 → 현재 인원 → 참여 여부** → 선택 전달사항." "메뉴가 응답의 이유가 된다."
- **실제 상태(수정 전):** `3-1` 홈 카드가 "시간 뱃지 + 역할 라벨"을 맨 위에, 메뉴를 그 아래 큰 글씨로 배치했다. 시각적으로 메뉴가 커서 눈에 띄긴 했지만, DOM/레이아웃 순서 자체는 PRD가 요구한 우선순위와 반대였다.
- **판정:** 미세하지만 실제 이탈.
- **조치(이번에 실행):** 메뉴 정하는 중(메뉴 없음) 케이스를 제외하고, 메뉴 제목을 시간·역할 뱃지보다 먼저 렌더링하도록 순서 변경.

### 7. §10 [화면10] "빈 상태"도 감정 화면이어야 하는가 — 정정됨

> **정정**: 3-7을 크림 톤으로 바꿨다가, 색 자체가 오진단이었던 게 밝혀지면서 이 변경도 함께 원복(`bg-surface` 무채색으로 복귀)했다. "완성·빈 상태 = 감정 화면"이라는 PRD 문구는 여전히 사실이지만, 그걸 "배경을 크림색으로 칠하라"로 구현할지는 전체 팔레트가 무채색이라는 확정된 디자인 방향과 별개로 다시 논의할 문제다. 지금은 손대지 않은 상태로 남긴다.

### 8. 디자인 시스템 이름과 실제가 따로 노는 것 (부수적 관찰, 여전히 유효)

이번 세션 내내 "Warm Table"이라는 이름을 써왔는데, 실제 색은 무채색 그레이라 이름이 말하는 "따뜻함"이 시각적으로는 없다. 이건 이탈이 아니라(그레이가 의도된 선택이라고 확인됐으므로) 이름-실체 불일치일 뿐이다 — 이름을 실체에 맞게 바꾸거나("Neutral Table" 등), 아니면 이름은 그대로 두고 "따뜻함"을 타이포·카피·인터랙션 쪽에서 표현한다는 걸 명확히 해두면 된다.

---

## 오늘 실제로 고친 것 (요약, 색 되돌림 반영 최종본)

1. ~~색상 팔레트 전면 교체~~ → **원복**. 무채색 M3 그레이가 사용자가 의도한 디자인이라고 확인받아, 34개 파일 hex 값을 원래대로 되돌렸다(`revert_to_grey.js`). `2-1`의 테라코타 텍스트/블록 색만 라벤더 대신 그레이 계열(`text-on-background`/`bg-primary`)로 유지 — 이건 색상 방향 문제가 아니라 "다른 화면과 다른 임의의 색을 쓰지 말자"는 일관성 문제라 원복 대상에서 제외.
2. `5-1` "관리자/구성원" 위계 라벨 제거 — 유지.
3. `8-1` 밀키트 화면 — 자체 주문·배송 체크아웃 제거, "제안 + 제휴사 연결"로 축소 — 유지.
4. `8-4` 정산 — 공개 범위 고지 문구 추가 — 유지.
5. `3-1` 카드 정보 순서 — 메뉴를 시간·역할보다 먼저 보이게 재정렬 — 유지.
6. `3-1` 핵심 요약 위젯 — "이번 달 식비"(금액)를 "이번 달 N번 함께 먹었어요"(횟수)로 교체 — 유지.
7. 확인용뷰어의 "관리자" 표현 제거 — 유지.

색을 제외한 1~7 항목(관리자 라벨·밀키트·정산 고지·카드 순서·위젯·뷰어 문구)은 색과 무관한 독립적인 철학 정합성 수정이라 그대로 유지했다. 전 파일 JS 문법·링크(141개) 재검증 통과.

## 아직 판단이 필요해서 손대지 않은 것 (제안만 함)

- **홈 화면의 밀키트 배너("이번 주 집밥이 벌써 3번째예요...")가 상시 노출되는 것**: §5.1 "가족을 고객으로 취급하지 않는다"는 원칙과 살짝 긴장 관계에 있다(매번 홈을 열 때마다 보이는 건 "제안"이라기보다 광고 배너에 가깝다). 다만 이 위치·문구는 사용자가 이전에 직접 지정한 것이라 이번엔 그대로 두고 여기 기록만 해둔다 — 필요하면 "확정 직후에만 잠깐 보이기"처럼 노출 조건을 좁히는 방향을 검토할 만하다.
- **파랑(주의·늦음) 의미색이 아직 실제 UI에 적용 안 됨**: 토큰 체계는 준비됐지만 "좀 늦어요" 표시 자체에 색을 입히는 건 파일별 로직을 봐야 해서 다음 작업으로 남긴다.
- **8-4 정산 화면의 존재 자체**: §13-7이 여전히 "미결"이라고 명시한 사안이라, 로드맵 화면으로 만들어는 뒀지만 실제 서비스화 전엔 기획 단계에서 공개 범위 기본값을 확정해야 한다.

## 왜 이런 이탈이 쌓였나 (자기반성)

로드맵 기능(밀키트·사진기록·예산·정산)을 만들어달라는 요청을 받았을 때, 나는 "이 화면이 §16 로드맵 표에 있나"만 확인하고 만들었지, 매 화면마다 "이게 §0.5 철학에 맞나?"를 따로 묻지 않았다. §0.5 자체가 "판단 기준으로서의 철학: 새 기능을 더하거나 뺄 때 '이게 이 철학에 맞나?'를 먼저 묻는다"고 명시했는데, 나는 그 대신 MoSCoW·통계가능여부·사용성이라는 더 쉬운 2차 기준으로 대체해버렸다. 결과적으로 로드맵에 있다는 이유만으로 Won't 리스트에 있는 "주문 시스템"을 만들고, §13 미결 사안을 확정된 것처럼 구현했다.

색 항목은 이 반성에서 빼야 한다 — 그건 이탈이 아니라 사용자가 "PRD 문구보다 실제로 봤을 때 안 촌스러운 것"을 우선한, 정당한 의도적 결정이었다. 여기서 배울 점은 다른 것이다: **문서(PRD)에 적힌 문구와 실제로 사용자가 승인한 결과물이 다를 때, 문서 문구만 보고 "이탈"이라고 단정하지 말고 먼저 확인했어야 했다.** `2-1`이 라벤더 토큰을 갖고 있다는 사실 하나만으로 "이후 파일들이 전부 이탈했다"고 결론 내린 게 성급했다.
