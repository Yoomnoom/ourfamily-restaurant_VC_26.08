# 정보 구조 (묶음 정의) — 2026-08-16

> "홈, 식사, 기록, 가구, 알림, 설정 / 가구설정 - 가구정보, 요일별 기본응답, 손님수신동의, 정산관리 — 이런 식으로 명확한 묶음 정보가 필요하다"는 지적에 따라, 전체 앱을 이 형식으로 다시 정리한다. 지금까지의 혼란은 대부분 "이 화면이 어느 묶음 소속인지"가 코드에만 있고 문서로 명문화된 적이 없어서 생겼다.

---

## 최상위 묶음 (7개)

### 1. 홈
가족 모두가 보는 공유 화면. 역할별 액션 없음.
- 오늘/내일 식사 요약
- 미리 답해두기
- 가족이 먹고 싶어해요 (메뉴 요청)
- 오늘 뭐 먹을지 골라주세요 (투표, 있을 때만)
- 타일: 아래 다른 묶음들로 가는 지름길(밀키트·예산·기록)
→ 화면: `3-0`

### 2. 식사
"오늘 뭐 먹을지 조율하는" 개인화된 흐름 전체. 하단 탭 이름은 지금 "내 식탁"인데, 이 묶음 전체를 가리키는 이름이라 **"식사"로 통일하는 걸 고려할 것**(아래 제안 참고).
- 내 식탁(내가 만든 식사 host 카드 + 나에게 물어본 식사 guest 카드) → `3-1`
- 식사 만들기(+반복 예약, +메뉴 투표 시작, +외식/배달 추천) → `3-2`
- 참여자 링크 공유 → `3-8`
- 확정 전 요약 → `3-4`
- 오늘의 식탁 완성 → `3-5`
- 참여자 응답(개인 링크로 들어왔을 때) → `3-3`
- 무가입 응답 연결 → `3-9`
- 홈 빈 상태(오늘 식사 없을 때) → `3-7`
→ 화면: `3-1,3-2,3-3,3-4,3-5,3-7,3-8,3-9`

### 3. 기록
"지난 식사를 돌아보는" 것 전부. 통계는 기록과 분리할 이유가 없어서 한 화면으로 합침(2026-08-16).
- 식사 기록 목록(검색·비용 입력·사진·정산 진입) + 이번 달 통계(횟수) + 식비 예산 진입점
→ 화면: `5-2` (구 `4-3` 흡수)

### 4. 가구
가구 자체의 설정. **사용자가 제시한 4개 하위 항목 구조를 그대로 따름**:
- 가구 정보(가구 이름, 구성원 목록, 공동 관리자 지정, 가구 삭제)
- 요일별 기본 응답
- 손님 · 수신 동의(저장된 손님 관리)
- 정산 관리
→ 화면: `5-1`(가구정보+손님, 아래에서 4행 허브로 재구성) + `5-4`(요일별) + `8-4`(정산)

### 5. 살림
가구가 함께 쓰는 물건·소비. 냉장고와 밀키트는 "식사"도 "기록"도 아닌 별도 성격이라 묶음을 분리.
- 우리집 냉장고 · 재료 사진 분석 · 메뉴 추천 → `7-1,7-2,7-3`
- 밀키트 주문(제안형, 수익모델 핵심) → `8-1`
→ 화면: `7-1,7-2,7-3,8-1`

### 6. 알림
전체 화면이 아니라 아이콘 + 인라인 패널(슬라이드인)로만 존재 — 별도 화면 묶음이 아님.

### 7. 설정
계정 단위 설정.
- 알림 켬/끔, 휴대폰 번호, 로그아웃
→ 화면: `5-3`

---

## 하단 탭 = 최상위 묶음 중 "매일 여러 번 오갈 만한 것"만

지금 5탭: 홈 / 내 식탁 / 달력 / 냉장고 / 설정.

- **홈·식사(내 식탁)·설정**은 최상위 묶음과 1:1로 맞음.
- **달력**은 사실 위 7개 묶음 어디에도 없다 — 달력은 "식사"들을 날짜순으로 보는 다른 *보기 방식*이지 별도 묶음이 아니다. 하단 탭에 있는 건 유지(자주 씀)하되, 정보구조 문서상으로는 "식사 묶음의 캘린더 뷰"로 명시.
- **냉장고**는 "살림" 묶음의 대표 화면이라 탭으로 승격돼 있음 — 나머지 살림 화면(밀키트)은 냉장고 안에서 연결됨.
- **가구·기록**은 탭이 아니라 홈 타일/아이콘 줄로 접근 — 매일 여러 번 볼 정도는 아니라서.

## 제안 — 이름을 실제로 맞추기

- 하단 탭 "내 식탁" → **"식사"**로 이름 변경 검토(묶음 이름과 탭 이름을 일치시키면 "이 탭 안에 만들기·공유·확정이 다 있다"는 게 더 명확해짐).
- `5-1`을 실제로 "가구 정보 / 요일별 기본 응답 / 손님·수신 동의 / 정산 관리" 4행이 맨 위에 보이는 허브로 재구성(아래 실행).
