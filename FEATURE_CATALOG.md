# 기능 카탈로그 — v1 스코프 결정용 (2026-08-16)

> "가능한 다 만들고 싶은데, 못 만든다면 준비중으로 남겨두고 싶다"는 요청에 따라, 지금까지 만든 모든 것을 있는 그대로 나열한다. 각 항목에 **현재 완성도**, **실제 서비스 전환 시 추가로 필요한 것**, **내 추천(포함/준비중/제외)**을 달아뒀다. 최종 선택은 이 표를 보고 사용자가 정한다.

범례 — 완성도: 🟢 정적 목업 완전 동작(localStorage 기준) / 🟡 UI는 있으나 하드코딩 데이터·미연동 부분 있음 / 🔴 참고용 뼈대만.

---

## A. 온보딩·로그인 (PRD Must)

| 화면 | 완성도 | 실서비스 전환 시 필요한 것 | 추천 |
|---|---|---|---|
| `1-1`~`1-4` 온보딩 | 🟢 | 실제 회원가입 API(전화번호 인증), 파일 업로드(프로필 사진) 서버 저장 | **포함** |
| `2-1`·`2-2` 로그인 | 🟢 | 실제 인증(세션/토큰), 비밀번호 해시, 카카오 등 소셜 로그인 여부 결정 | **포함** |

## B. 핵심 조율 루프 (PRD Must — 이 앱의 본질)

| 화면 | 완성도 | 실서비스 전환 시 필요한 것 | 추천 |
|---|---|---|---|
| `3-0` 홈 (공유) | 🟢 | 실시간 동기화(지금은 새로고침 기준) | **포함** |
| `3-1` 내 식탁 | 🟢 | 위와 동일 | **포함** |
| `3-2` 식사 만들기 | 🟢 | — | **포함** |
| `3-3` 참여자 응답 | 🟢 | 1인1링크 토큰 실제 발급·보안(추측 불가능한 토큰) | **포함** |
| `3-4` 확정 전 요약 | 🟢 | — | **포함** |
| `3-5` 오늘의 식탁 완성 | 🟢 | — | **포함** |
| `3-7` 홈 빈 상태 | 🟢 | — | **포함** |
| `3-8` 참여자 링크 공유 | 🟡 | 카카오톡 실제 공유 API(현재 링크 텍스트만) | **포함** |
| `3-9` 무가입 응답 연결 | 🟢 | §7.6 A방식(카톡 자동연결) 백엔드 로직 | **포함** |

## C. 캘린더·통계 (PRD Must)

| 화면 | 완성도 | 실서비스 전환 시 필요한 것 | 추천 |
|---|---|---|---|
| `4-1` 달력 (월/주/오늘) | 🟢 | 하드코딩된 2024년 8월 데이터를 실제 DB 쿼리로 교체 | **포함** |
| `4-3` 월 요약 통계 | 🟡 | "18/14/4" 등 숫자가 하드코딩 — 실제 집계 쿼리 필요 | **포함** |

## D. 가구 관리 (PRD Must/Should)

| 화면 | 완성도 | 실서비스 전환 시 필요한 것 | 추천 |
|---|---|---|---|
| `5-1` 가구 설정 | 🟢 | 아래 "가구 소유권 모델"(E절) 반영해 권한 UI 추가 | **포함** |
| `5-2` 식사 기록 | 🟢 | — | **포함** |
| `5-3` 설정 (하단 탭 5번째) | 🟢 | 실제 알림 발송 인프라(FCM 등) | **포함** |
| `5-4` 요일별 기본 응답 | 🟢 | — | **포함(Could지만 이미 완성)** |

## E. 냉장고 AI 인식 — PRD 원안엔 없던 것(세션 중 별도 앱에서 이식)

| 화면 | 완성도 | 실서비스 전환 시 필요한 것 | 추천 |
|---|---|---|---|
| `7-1` 우리집 냉장고 | 🟢 | — | 재검토 |
| `7-2` 재료 사진 분석 | 🟡 | **실제 Vision API 연동**(현재 로딩 애니메이션 + 하드코딩 결과) — 비용·정확도 검증 안 됨 | 재검토 |
| `7-3` 메뉴 추천 | 🟢 (매치율 계산은 실제 로직) | 레시피 데이터베이스 실제 확보(현재 4종 하드코딩) | 재검토 |

**왜 재검토인가**: "사진 찍어서 재고 관리"는 그 자체로 지속적인 사용자 노동이다. §0.5 "따뜻하되 노동을 늘리지 않는다"는 핵심 철학과 부딪힐 수 있다 — 실사용자가 정말 매번 사진을 찍을지 검증된 적이 없다(§13 미결 질문과 같은 종류의 미검증 가정).

## F. 로드맵 확장 4종 — PRD §16에 "1차~4차 확장"이라고 명시된 것 (MVP 아님)

| 화면 | 완성도 | 단계 | 실서비스 전환 시 필요한 것 | 추천 |
|---|---|---|---|---|
| `8-1` 밀키트 주문 (제안형) | 🟢 | 1차 | **핵심 수익모델로 방금 확정**(제휴 수수료) — 실제 제휴사 API/딥링크 연동, 정산 계약 | **포함(수익모델 핵심이라 확정)** |
| `8-2` 식사 사진 기록 | 🟢 (2026-08-18 실 DB 전환 완료) | 2차 | 이미지는 canvas로 긴 변 1280px 축소 후 Storage(`meal-photos` 버킷) 업로드로 비용 완화. SNS 공유는 인스타그램·카카오톡 등 각 플랫폼 API 앱 등록·심사가 필요해 만들지 않되, 브라우저 표준 Web Share API(`navigator.share`)로 실제 공유 시트 연결(미지원 환경은 다운로드 링크로 대체) | **포함(완료)** |
| `8-3` 식비 예산 | 🟢 | 3차 | §13-7 "공개 범위 기본값" 미결 | 재검토 |
| `8-4` 정산 | 🟢 | 4차 | §13-7 미결 + "가족 돈 문제 관계 리스크"(PRD 자체 경고) + 실제 송금 연동 없음(의도적으로 안 만듦) | 재검토 |

## G. 이번 세션에 새로 생긴 기능 — PRD v0.5엔 아예 없음 (사용자 요청으로 추가)

| 기능 | 위치 | 완성도 | 추천 |
|---|---|---|---|
| 반복 식사 예약 | `3-2` 설정 → `3-1`에서 자동 생성 | 🟢 | **포함** — "매번 물어야 하는 날"을 줄이는 §0.5 철학과 직결 |
| 미리 답해두기 | `3-0` | 🟢 | **포함** — 같은 이유 |
| 메뉴 투표(가족에게 골라달라기) | `3-2`→`3-0` | 🟢 | **포함** |
| 자주 가는 곳·요즘 뜨는 곳(외식 추천) | `3-2` | 🟡 (추천 목록은 하드코딩, "참고용" 라벨 붙임) | 재검토 — 실제 식당 API 연동 여부 결정 필요 |
| 방명록(가족 한마디) | `3-0` 식탁 좌석 위 말풍선 | 🟢 | **포함** — 싸이월드 미니홈피에서 착안, 저비용으로 감성 추가 |
| 오늘 안 본 사람 표시 | `3-0` | 🟢 | **포함** — §0.5 "묻지 않아도 아는 집" 철학과 직결, 호스트가 누굴 더 챙겨야 할지 바로 앎 |

## H-1. 아이디어 단계 — 아직 안 만듦 (2026-08-17 홈 원형 식탁 비주얼 반응 중 제안됨)

`3-0` 홈의 원형 식탁+아바타 좌석 비주얼을 실제로 보고 나서 나온 후속 아이디어. 지금은 상태를 정적으로(색·배지·서성임 애니메이션·반경) 표현하는 수준까지만 구현했고, 아래는 "더 가면 좋을 방향"으로 남겨둔 것 — 지금 만들지 않음.

| 아이디어 | 내용 | 왜 지금 안 만드나 |
|---|---|---|
| 아바타 실시간 움직임 애니메이션 | 착석=먹음, 배회=미응답, 다른 동작=안 먹음을 실제 캐릭터 움직임(걷기·앉기 모션)으로 표현 | 지금은 CSS만으로 흔들림(서성임)·반경 차이(물러남)까지만 구현(🟢, 이번에 완료). 실제 걷기/앉기 모션은 스프라이트나 애니메이션 라이브러리가 필요해서 "정적 목업 + vanilla JS" 원칙을 넘어감 |
| 아바타 위 말풍선(감사 인사·먹고싶은 메뉴) | 좌석 근처에 말풍선으로 "가족이 먹고싶어해요" 요청이나 감사 메시지를 바로 표시 | `menu_requests` 데이터는 이미 있음(현재는 별도 리스트 섹션) — 말풍선으로 옮기는 건 비교적 저비용이라 **재검토 가치 있음**(다음에 할 만한 후보) |
| 아바타를 채팅창처럼 사용 | 식탁 비주얼 자체를 가족 채팅 인터페이스로 확장 | 메시지 저장·알림·모더레이션 등 실제 채팅 기능 전체가 필요 — 이 앱의 핵심("식사 조율")과 다른 별도 제품 영역이라 v1 범위 밖으로 판단, 별도 논의 필요 |

## H. 참고 전용 — `_archive/`로 이동(삭제 아님, 실제 서비스와 무관)

`100. 가족코드로 참여하기추가필요`(미구현 프로토타입), `3-6`·`4-2`·`6-2`(예전 전체화면 버전, 인라인 패널로 대체됨), `5-5. 더보기`(홈 타일 그리드에 흡수됨). **v1 판단에서 완전히 제외하고 무시해도 됨.**

## I. 도구 — 제품 아님

`0_개발도구 0. 확인용뷰어`: 우리끼리 화면을 검토하는 내부 QA 도구. 실제 서비스에 포함되지 않음.

---

## 요약 — 지금 상태로 v1을 정한다면

- **A~D(온보딩~가구관리)는 전부 포함이 명확함** — PRD Must와 정확히 겹치고 완성도도 높다.
- **E(냉장고 AI)**: 포함 여부는 "실사용자가 정말 사진을 찍을까"에 달려있다 — 검증 없이 넣으면 리스크.
- **F(로드맵 4종) 중 8-1(밀키트)만 확정 포함**(수익모델 핵심으로 결정됨). 8-2·8-3·8-4는 각자의 미결 사유(비용/공개범위/관계리스크)가 안 풀렸으니 "준비 중" 처리를 권장.
- **G(신규 4개)는 전부 포함 추천** — 실제로 조율 노동을 줄이는 방향이라 철학과 직결.
